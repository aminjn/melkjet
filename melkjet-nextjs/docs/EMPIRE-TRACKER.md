# 🏛 ردیابِ رسمیِ Empire Bible — منبعِ حقیقتِ پیاده‌سازی

> **این فایل حافظهٔ پروژه است.** هر سشنِ جدیدِ Claude اول این فایل را می‌خواند.
> با هر سندِ جدید یا فازِ جدید، همین فایل به‌روز و کامیت می‌شود. چت حافظه نیست — این فایل هست.

## 📜 قوانینِ نقض‌نشدنی (از خودِ سندها + دستورهای صریحِ امین)

1. **هیچ دادهٔ فیک، هرگز.** همهٔ اعداد از بازارِ واقعیِ ملک‌جت (آگهی‌ها/رویدادها/آمار). اگر داده نیست → صادقانه خالی/پیام، نه عددِ ساختگی.
2. دستیارِ بازی **«ملک‌جت»** است — نامِ «نورا/آرمان/سارا» ممنوع (دستور صریح کاربر).
3. در UI کاربر هرگز واژهٔ **«بازی»** نمی‌آید → «مسیرِ رشد / امپراتوری / سفرِ مالی» (Branding Rule سند).
4. هر عددِ موتور باید در سوپرادمین قابل‌تنظیم باشد و **واقعاً** موتور زنده را عوض کند (نه فیلد تزئینی).
5. هیچ Pay-to-Win؛ پرداخت فقط برای سرعت/تحلیل/ظاهر.
6. «هیچ پولی از هیچ ساخته نمی‌شود» — هر تولید/مصرف پول منبع و مقصد دارد (مالیات → خزانه، بدهی از netWorth کم می‌شود).
7. مکانیک‌های شانسی (صندوقچه/مذاکره/کوئست) **قطعی از هش** — ضدسوءاستفاده، تست‌پذیر.
8. امپراتوری **منوی مستقلِ خودش** را در سوپرادمین دارد (گروه «امپراتوری (بازی)») — هیچ‌چیز داخل REOS قاطی نشود.
9. هر فاز: تست یونیت + تستِ PG واقعی + tsc + build + کامیت + push. دیپلوی همیشه دستِ کاربر: `sudo scripts/deploy.sh` + پاک‌کردن کش آروان.
10. `Date.now()` بازی‌ساز نیست — snapshot/دوره‌ها بر پایهٔ dayNumber؛ ورودی‌های عددی فارسی/جداکننده‌دار همیشه با `digitsOf` پاک می‌شوند.
11. **کامپوننت‌های مشترکِ سایت (NeshanMap و…) رفتارِ پیش‌فرضشان قفل است** — قابلیتِ جدید فقط opt-in با propِ جدید؛ تغییرِ لودر/URL/رفتارِ سراسری ممنوع مگر بعد از تستِ همهٔ صفحاتِ مصرف‌کننده (درسِ فاز ۳۰: تغییرِ URLِ SDK نقشه، نقشهٔ صفحهٔ ملک را هم خاکستری کرد).
12. **قانونِ ۹۰/۱۰ (سند ۲۸ — فصل ۱۷):** «کاربر ERP نخریده؛ آمده تجربه کند» — ۹۰٪ کارِ تکراری را سیستم انجام می‌دهد و فقط خروجی‌اش دیده می‌شود؛ ۱۰٪ تصمیم‌های مهم (پرریسک/پرهیجان) دستِ بازیکن است. هر قابلیتِ جدید باید یکی از این چهار را بسازد: تصمیمِ جذاب، ریسک، پاداش، لذت — وگرنه زیرِ دستِ خودکارسازی/AI می‌رود و فقط نتیجه گزارش می‌شود.
13. **قانونِ رویاپردازی (دستورِ مستقیمِ امین):** هر تصمیمِ بزرگِ مسیرِ رشد باید جای «رویابافیِ» کاربر داشته باشد — انتخاب‌های هویتی/ظاهریِ بیشتر (نامِ پروژه، سبکِ نما، نامِ دلخواهِ دارایی‌ها، …) و پیش‌نمایشِ آیندهٔ رؤیا (برآوردِ فروش/سود از دادهٔ واقعی با برچسبِ برآورد). این انتخاب‌ها صرفاً هویتی‌اند و هیچ اثرِ اقتصادی ندارند (سازگار با قانون ۵). **مخاطبِ هدف ۱۴ سال به بالاست** — جذابیتِ بصری (رنگ، درخشش، آیکن، جشن) برای این مخاطب یک الزام است، نه تزئین؛ در هر فازِ جدید و در بازبینیِ فازهای قبلی اعمال شود.
14. **قانونِ طلایی (سند ۳۳ — Fun Engine): «Reality serves Fun. Never the opposite.»** — واقع‌گرایی فقط تا جایی ارزش دارد که لذت را بیشتر کند؛ هیچ دکمه‌ای صرفاً «عملیاتِ اداری» نباشد (وام = چانه‌زنی، مزایده = تنش، قرارداد = مذاکره)؛ اگر بازیکن از انجامِ کاری لبخند نمی‌زند، باید دوباره طراحی شود. هیچ صفحه‌ای شبیه اکسل نباشد.
15. **قانونِ طلاییِ دوم (سند ۳۳ — Outside The Game): «بازگشت از سرِ اشتیاق، نه ترس.»** — هیچ انرژی/کول‌داونِ اجباری/اسپمِ «برگرد»؛ فقط خبرِ مهمِ داستانی و FOMO «واقعی» (مزایده‌ای که واقعاً شروع می‌شود)؛ از دست دادنِ یک فرصت هرگز باخت نیست («فرصتِ بعدی می‌آید») — فشار باید هیجان بماند، نه استرس. + اصلِ «توجه، منبع است»: همیشه فرصت بیش از ظرفیتِ بازیکن باشد ولی قابلِ‌واگذاری به تیم/AI.

## 🗂 نقشهٔ کد (کجا چیست)

| چی | کجا |
|---|---|
| هستهٔ بازی (store دوحالته PG/فایل) | `app/lib/empire-store.ts` |
| بازار سرمایه (صندوق شاخصی/مشارکت جمعی/شاخص‌ها/ترس‌وطمع) | `app/lib/empire-market.ts` + اکشن‌های market در API بازیکن + نمای `capital` ادمین |
| نامهٔ روزانه (تولید شبانه) | `app/lib/empire-brief.ts` + بلوک ۶ساعتهٔ `cron-runner.ts` |
| API بازیکن | `app/api/empire/route.ts` |
| صفحهٔ بازی | `app/empire/page.tsx` |
| کارت ورود در پنل‌ها | `app/components/EmpireCard.tsx` → buyer/owner/pros/agency |
| مرکز فرماندهی ادمین (۸ زیربخش، شامل «بازار سرمایه») | `app/components/EmpireAdminPanel.tsx` + `app/api/admin/empire/route.ts` + منوی `app/admin/page.tsx` |
| تنظیماتِ زنده | بخش `empire` در `app/lib/reos/reos-config.ts` (کش sync `config()`) |
| فلگ روشن/خاموش/rollout | فلگ `empire` در `app/lib/reos/flags.ts` |
| تست‌ها | `scripts/reos-test.mjs` (یونیت) + `scripts/reos-store-test.mjs` (PG واقعی؛ اول `sudo service postgresql start`) |
| دودآزمای منوهای ادمین | `node scripts/admin-smoke.mjs` (روی سرور با JWT_SECRET) |

## ✅ فازهای انجام‌شده (کامیت‌ها)

| فاز | چی | کامیت |
|---|---|---|
| ۱ | تولد کامل (۵ سؤال، Dream Board، هویت، هدیه، ۴ فرصت واقعی، خرید+امضا، تصمیم معنادار، M1-M3، شکارچی، تحلیل ژتونی) | `c7650a1` |
| ۲ | نامهٔ روزانه (daily_brief طبق طرح سند) + استریک + حذف «نورا» → ملک‌جت | `7df3a5c` |
| ۳ | چرخهٔ عمر (فروش/زمین ۳مسیره/کسب‌وکار تجاری با ٪واقعی/درآمد اجاره از میانهٔ هم‌محله) + EmpireScore + ۵ لیدربرد + لیگ محله + صندوقچه + EmpireCard در ۴ پنل | `eece840` |
| ۴ | شروع سینمایی + ۶ مسیر شخصیت + سطح بی‌پایان (Rookie→Empire) + هزینهٔ مالکیت + بازارِ داغ در نامه | `86d9292` |
| ۵ | **منوی مستقل ادمین**: فرماندهی/بازیکنان(Ghost+هدیه+حذف)/اقتصاد/مأموریت/دنیا(Sync Monitor+نفوذ)/LiveOps/دسترسی | `c99dc1f` |
| باگ | ورودی حدس (رقم فارسی→صفر→۱۰۰٪ خطا) + جداکننده + کف قیمت معتبر | `24501e8` |
| ۶ | بانک: CreditScore ۰-۱۰۰۰ (باندهای سند)، وام با سقف/نرخ اعتباری، بهرهٔ روزشمار، دیرکرد ×۱.۵، netWorth−بدهی | `f786df0` |
| ۷ | مذاکرهٔ قطعی، کوئست روزانه/هفتگی شخصی، مالیات→خزانه، پیام بازگشت، «N دقیقهٔ امروز»، نردبان رؤیا، گذرنامه+اشتراک، دعوت شراکتی دوطرفه، فعال‌سازی mentorInitiates | `73203cd` |
| ۸ | سود/زیانِ دیروز (اسنپ‌شات)، ۵ مأموریت مخفی، کلکسیون، هدیهٔ بازگشت، 🔗 «آگهیِ واقعی» (Game→Lead)، تزریق رفتار بازی به دیتالیک REOS | `00fe9ef` |
| ۹ | **بازار سرمایه (جلد ۴۰)**: صندوق شاخصی REIT (واحد = «یک مترِ مجازی» به میانهٔ متریِ واقعی + سود دوره‌ای از اجارهٔ واقعی + رتبهٔ AAA-BB قطعی + کارمزد→خزانه)، مشارکت جمعی = مالکیت کسری آگهی‌های واقعی گران (ظرفیت اتمیک FOR UPDATE)، بازار ثانویه = بازخرید به NAV، شاخص‌ها، ترس/طمع از رویداد واقعی، پرتفوی+تنوع، netWorth شامل واحدها، کنسول سرمایهٔ ادمین (منوی «بازار سرمایه») | `b51e3c9` |
| ۱۰ | **تعامل/تاریخ/روایت (جلد ۴۹+۵۱+۵۲)**: `empire-engage.ts` — ردِ فعالیتِ واقعی از کلیدهای تاریخ‌دار claims → کنسول ادمین «تعامل و بازگشت» (DAU/WAU/MAU، Retention کوهورتی D1/D7/D30، نمودار ۱۴روزه، نرخ کوئست/صندوقچه، لیست ریسکِ ریزشِ باارزش‌ها)؛ Mastery ۶محوره از رفتار واقعی (چیپ‌های ★ در داشبورد)؛ «📰 روزنامهٔ ملک‌جت» = خبرِ تولیدشده از اتفاقِ واقعی (محلهٔ داغ، رکورد متری هفته، تولدها، سوددهی‌ترین فروش) + آرشیوِ رکوردهای تمدن | `1db290e` |
| ۱۱ | **تجربه و حرکت (جلد ۵۶ + ۵۴)**: لایهٔ Delight در /empire — ورودِ پلکانی، میکرواینترکشنِ همهٔ دکمه‌ها، شمارشِ متحرکِ اعدادِ ثروت (CountUp)، جشنِ موفقیت (Burst در صندوقچه/پاداش/خرید/فروشِ سودده)، احترام به prefers-reduced-motion؛ **🌆 خطِ آسمانِ امپراتوری** = قانونِ ۳ («تجربه به‌جای داده») — هر دارایی یک برجِ شب با ارتفاعِ ارزشِ روزِ واقعی؛ توضیح‌پذیریِ AI (جلد ۵۴): چیپ‌های «چرا این پیشنهاد» با سیگنال‌های واقعی (شهر/استقبال/سهم از سرمایه/پروفایل) | commit «Empire فاز ۱۱» در git log |

| ۱۲ | **شرکتِ ساختمانی (جلد ۶۱+۶۲+۶۳)**: ثبت شرکت (نام/برند/نوع، هزینه→خزانه، نشان CEO)؛ **اعتبارِ ستاره‌ای** ⭐۱-۵ فقط از رفتار واقعی (فروش سودده/مذاکره/خوش‌حسابی/پروانه/دیرکرد منفی)؛ استخدام مهندس (۳ نامزدِ هفتگیِ قطعی از هش، حقوق روزشمار → wagesPaid، مهارتِ تیم = مذاکرهٔ قوی‌تر + پروانهٔ سریع‌تر)؛ **شخصیتِ مالکِ زمین** در مذاکره (قطعی از هشِ آگهی: محافظه‌کار/منصف/قیمت‌بالا با اثرِ مشخص)؛ **پروانهٔ ساخت**: درخواست روی زمینِ برنامهٔ «ساخت» → عوارض٪ارزشِ واقعی→خزانه → بررسیِ چندروزهٔ قطعی → اعتراضِ احتمالی (توافق با غرامت یا دفاع با روزِ اضافه) → صدور + نشان First Permit | commit «Empire فاز ۱۲» در git log |

| ۱۳ | **موتورِ ساخت (جلد ۶۴–۷۲، Engine-محور)**: کلنگ روی زمینِ پروانه‌دار با نقشهٔ سازه/کیفیت (ضرایب شفاف: فلزی سریع‌تر/گران‌تر، لوکس qualityFactor بالاتر)؛ بنا = زمین×تراکم (knob)؛ **پیشرفت = روزهای پرداخت‌شده** — بی‌پولی = توقفِ کارگاه (جلد ۷۱ Cash Flow Crisis)؛ رویدادهای قطعی از هش در ایستگاه‌های ۳۰٪/۷۰٪ (پرداخت یا صبر — تا تصمیم، کار می‌ایستد)؛ **پیش‌فروش** از ۳۰٪ با سقف و قیمتِ میانهٔ متریِ واقعیِ محله منهای تخفیفِ شفاف؛ تکمیل → First Tower + تحویلِ پیش‌فروش‌ها در realized + projectsDelivered (خوراکِ اعتبارِ ⭐ شرکت)؛ **فروشِ واحد** به قیمتِ کاملِ واقعیِ لحظه؛ اتمامِ همهٔ واحدها = تحویلِ کاملِ پروژه (Project Delivered) و بسته‌شدنِ اولین Loop «زمین→ساخت→فروش»؛ netWorth پروژهٔ در ساخت = بهای تمام‌شده×کسرِ نفروخته | commit «Empire فاز ۱۳» در git log |

| ۱۴ | **Master Index (جلد ۷۳–۷۸)**: `docs/EMPIRE-ENGINE-MAP.md` — نقشهٔ فریزشدهٔ ۱۲ موتور ↔ کدِ واقعی + ۵ سطحِ Loop + ۸ Pillar + دروازهٔ Feature + Feature List (GDD فشرده) + وابستگی L0→L5؛ «📒 گزارشِ مالیِ شرکت» در داشبورد (جلد ۷۴ Reports) از شمارنده‌های واقعی (realized/اجاره/پیش‌فروش/ساخت/حقوق/خزانه/بدهی) | commit «Empire فاز ۱۴» در git log |

| ۱۵ | **گیم‌پلی تصمیم‌محور (GDD جدید فصل ۴ بخش‌های ۱–۸)**: (۱) **هدفِ پروژه** سرِ کلنگ (⚡ فروشِ سریع / 💰 حداکثرِ سود / ⭐ اعتبارِ برند) — اثرِ شفاف روی قیمت (٪ میانهٔ واقعی، knob) و سقفِ پیش‌فروش؛ هدفِ اعتبار → `stats.repProjects` → فاکتورِ جدیدِ اعتبارِ ⭐؛ (۲) **امکاناتِ میان‌ساخت** (استخر/روف‌گاردن/باشگاه/پارکینگ): هزینه = ٪ کلِ پروژه → بهای تمام‌شده، ارزش = ضریبِ شفافِ قیمتِ واحد در فروش/پیش‌فروش/اجاره (هر دو knob)؛ (۳) **«بفروش یا نگه‌دار و اجاره بده»** (rentUnits/stopRent): درآمدِ ماهانهٔ واحدهای تکمیل‌شده از میانهٔ اجارهٔ واقعیِ هم‌محله × کیفیت × امکانات؛ واحدِ اجاره‌ای تا فسخ مانعِ تحویلِ کامل؛ (۴) **اشباعِ فروشِ عمده** (bulkPriceOf): بیش از N واحد در یک سفارش → هر واحدِ اضافه ٪ ارزان‌تر (کف ۸۰٪) — «بازار به رفتارِ بازیکن واکنش نشان می‌دهد»؛ (۵) **کارنامهٔ پروژه** (projectHist + projectReportOf/projectLessonsOf): تحلیلِ پس از تحویل از اعدادِ واقعیِ خودِ پروژه (سود/بازده، تأخیر نسبت به days0، پیش‌فروش، رویدادها، استراتژی) — کارتِ «🎓 کارنامهٔ پروژه‌ها» در /empire؛ (۶) **کارتِ استخدام با اثرِ عددی** (engineerEffectsOf): مذاکره +٪، هزینهٔ رویدادِ کارگاه −٪ (مهارت≥۵۰ — اثرِ واقعی در progressBuild)، پروانه −روز؛ کنسولِ اقتصاد: کارتِ «🎯 گیم‌پلی پروژه» با همهٔ knobها (هدف‌ها/عمده/رویداد/امکانات) | commit «Empire فاز ۱۵» در git log |

| ۱۶ | **Hook + حافظهٔ مذاکره + اعتبارِ اثرگذار (سند ۱۴ — GDD فصل ۴ بخش‌های ۹–۱۶ + پیوتِ «مدل B»)**: (۱) **🔥 فرصت‌های طلاییِ امروز** — Hook ورود: N آگهیِ «واقعیِ» قیمت‌دار، انتخابِ قطعی از هشِ کاربر+روز (dailyDealPickOf)، شمارشِ معکوسِ واقعی تا مرزِ روزِ بازی؛ کارت قضاوت نمی‌کند (بعضی واقعاً زیرِ میانهٔ محله‌اند، بعضی نه) — مذاکره/تحلیلِ ژتونی/خرید روی همان کارت؛ knobهای deals.enabled/count؛ (۲) **حافظهٔ مذاکره** (بخش ۹ «هر شخصیت حافظه دارد»): stats.negoTries (هر آگهی یک بار — کلیدِ claims)، negoMemoryOf: نرخِ بردِ واقعی پایین → مالک‌ها محتاط‌تر (−۵)؛ خوش‌معامله → اعتمادِ سریع‌تر (+۳)؛ یادداشتِ حافظه در UI؛ (۳) **اعتبارِ برند اثرِ واقعی دارد** (بخش ۱۵): هر ⭐ بالای ۱ → شانسِ مذاکره +٪knob و نرخِ وام −٪knob (کفِ نصفِ نرخِ باند، repCutPct شفاف در UI)؛ (۴) **یکسان‌سازیِ فرمولِ مذاکره** (negoSkillOf مشترک بین «مذاکره» و «خرید» — رفعِ ناهمخوانیِ قدیمی: خرید قبلاً شخصیتِ مالک/تیم را حساب نمی‌کرد) | commit «Empire فاز ۱۶» در git log |

| ۱۷ | **Progression و اقتصاد (سند ۱۵ — فصل ۴ بخش‌های ۱۷–۲۰ + فصل ۵)**: (۱) **سطح‌گشاییِ قابلیت‌ها** («امکانات باید بیشتر شوند، نه اعداد»): بازارِ سرمایه از سطح ۳، ثبتِ شرکت از سطح ۴، مشارکتِ جمعی از سطح ۶ (همه knob در `unlocks`)؛ فقط ورودی‌های جدید قفل‌اند — دارایی/فروش/بازخرید هرگز؛ قفل‌ها در UI شفاف‌اند («از سطحِ X باز می‌شود»)؛ (۲) **ظرفیتِ پروژهٔ همزمان** (فصل ۵ «منابع»: ظرفیت هم دارایی است): maxProjectsOf = پایه + سطح ÷ گام (هر دو knob) — کلنگِ بیش از ظرفیت رد می‌شود، چیپِ «ظرفیتِ ساختِ همزمان X/Y» در نقشهٔ دارایی‌ها؛ (۳) **فروشِ پروژهٔ نیمه‌کاره** (فصل ۵ «پروژهٔ در حالِ ساخت هم دارایی است»): sellProject به ٪knob از بهای تمام‌شده (زمین+پرداختی‌ها) منهای مالیات→خزانه؛ با پیش‌فروشِ فعال ممنوع (تعهدِ تحویل)؛ زیان/سود در realized؛ دکمهٔ «🏳» در کارگاهِ زنده؛ کارتِ ادمین «📈 سطح‌گشایی و ظرفیت» | commit «Empire فاز ۱۷» در git log |

| ۱۸ | **Progression تکمیلی (سند ۱۶ — فصل ۶ بخش‌های ۱–۱۰)**: (۱) **پاداشِ Level Up**: هر سطحِ جدید (از XPِ واقعی) × knob `levelUpCoins` ملک‌کوین + نقطهٔ تایم‌لاین؛ اولین اجرا فقط سطح را ثبت می‌کند (بدونِ پاداشِ گذشته‌نگرِ یکجا)؛ (۲) **لیدربوردِ «📅 رشدِ این هفته»** (قانونِ صریحِ سند: دوره‌ای کنارِ دائمی تا بازیکنِ تازه هم شانس داشته باشد): weekSnap هفتگی از netWorth واقعی در اولین بازدیدِ هفته — دلتا از نقطهٔ ورودِ خودِ بازیکن؛ (۳) **Title فعال** (بخش ۹): انتخابِ عنوان فقط از نشان‌های واقعاً کسب‌شده (کلیک روی نشان)، نمایش در سربرگ و ردیف‌های لیدربورد | commit «Empire فاز ۱۸» در git log |

| ۱۹ | **اجتماعیِ الان-واقعی (سند ۱۷ — فصل ۷ بخش‌های ۱–۱۰)**: (۱) **پروفایلِ عمومیِ امپراتوری** («بازدید از شهرِ دیگران» — بخش ۹): کلیک روی هر ردیفِ لیدربورد → viewEmpire: نام/عنوان/سطح/نشان‌ها/امتیاز/ارزشِ خالص (از قیمت‌های زندهٔ واقعی)/محله‌ها/خطِ آسمانِ کوچک/شرکتِ ⭐/عضویت؛ (۲) **👏 تحسین (kudos)**: هر بازیکنِ واقعی یک‌بار برای هر امپراتوری (کلیدِ claims روی تحسین‌کننده)، شمارنده + نقطهٔ تایم‌لاین برای گیرنده، **بدونِ هیچ پاداشِ پولی** (ضدِ P2W/سوءاستفاده)؛ چیپِ 👏 در سربرگِ داشبورد | commit «Empire فاز ۱۹» در git log |

| ۲۰ | **LiveOps (سند ۱۸ — فصل ۸ بخش‌های ۱–۱۰)**: (۱) **🎪 استودیوی رویداد v1** («هیچ رویدادی نباید نیاز به آپدیت داشته باشد»): ادمین در کنسولِ LiveOps رویدادِ زمان‌دار می‌سازد (عنوان/آیکن/متریک از رفتارِ واقعیِ REOS: بازدید/ذخیره/جستجو/محله‌ها/هدف/پاداشِ کوین+XP/مدت/روشن‌وخاموش — ذخیره در `empire.events` کانفیگ) — همان لحظه کارتِ رویداد با نوارِ پیشرفتِ «واقعی» و شمارشِ معکوس در داشبوردِ همهٔ بازیکنان ظاهر می‌شود؛ claim از همان مسیرِ امنِ claim با بازسنجیِ سمتِ سرور (کلیدِ ev_id)؛ (۲) **پاداشِ نقاطِ عطفِ استریک** (بخش ۱): کوین در روزهای ۷/۱۴/۲۱/۳۰ِ ورودِ پیاپیِ واقعی (knob در LiveOps)؛ کلیدِ claim شاملِ روزِ شروعِ دوره است تا با شکستنِ زنجیره دوباره قابلِ‌دریافت شود | commit «Empire فاز ۲۰» در git log |

| ۲۱ | **UI/UX (سند ۱۹ — فصل ۹ بخش‌های ۱–۱۰)**: (۱) **کارتِ «💼 پرتفوی»** (تغییرِ نامِ صریحِ سند از Inventory): ارزشِ کلِ پرتفوی از قیمت‌های روزِ واقعی همیشه بالای کارت؛ فیلترِ نوع (زمین/آپارتمان/تجاری/ویلا) + فیلترِ **«⚠️ مشکل‌دار»** (اتفاقِ کارگاهِ معطل، اعتراضِ پروانهٔ حل‌نشده، کارگاهِ ایستاده از بی‌پولی)؛ مرتب‌سازی (جدیدترین/باارزش‌ترین/بیشترین رشد)؛ (۲) **قانونِ ۴ سؤالِ هر صفحه** (Part 05) در ENGINE-MAP ثبت شد؛ (۳) نگاشتِ ۱۰ صفحهٔ سند به پیاده‌سازیِ موجود در تراکر | commit «Empire فاز ۲۱» در git log |

| ۲۲ | **Art Direction الان-قابل‌اجرا (سند ۲۰ — فصل ۱۰ بخش‌های ۱–۱۰)**: (۱) **استپرِ بصریِ ۷ مرحلهٔ ساخت** در کارگاهِ زنده (Part 03 «هر مرحله از ظاهر قابلِ تشخیص») — از پیشرفتِ واقعیِ paidDays؛ (۲) **برجِ در حالِ ساخت در خطِ آسمان** متمایز: کم‌نور + قابِ نقطه‌چین + 🏗؛ (۳) **قانونِ «بدونِ انیمیشنِ اجباریِ طولانی» (سقف ۱.۵ث)** اعمال شد: توالیِ خرید ۳.۶ث→۱.۴ث، مکثِ نمایشیِ پیشنهاد ۳ث→۱.۲ث؛ (۴) معناشناسیِ رنگ (سبز=سود، قرمز=ضرر/هشدار، نارنجی=فرصت، طلایی=ویژه) به‌عنوان قانونِ توکن در ENGINE-MAP | commit «Empire فاز ۲۲» در git log |

| ۲۳ | **Visual Pass — پوستهٔ واقعیِ بازی (به خواستِ صریحِ امین)**: (۱) **🎮 منوی بازی** ثابت در پایینِ /empire با ۵ صفحه: 🏙 شهر / 💼 پرتفوی / 🎯 مأموریت‌ها / 📊 بازار / 🏆 رتبه‌ها — پایانِ «همه‌چیز در یک اسکرول»؛ (۲) **HUD چسبان** (سربرگِ سطح/کوین/ژتون/استریک/👏 همیشه دیدرس — فصل ۹)؛ (۳) **🗺 نقشهٔ شهر** در صفحهٔ شهر (NeshanMap شب): پینِ دارایی‌های بازیکن (مختصات از متای آگهیِ واقعی — liveInfoOf) + پینِ 🔥 فرصت‌های امروز؛ کلیک → آگهیِ واقعی؛ (۴) تب‌بارِ عمومیِ سایت داخلِ بازی پنهان (بازی منوی خودش را دارد)؛ راستی‌آزمایی با مرورگرِ واقعی: هر ۵ تب اسکرین‌شات، بدونِ خطای JS | commit «Empire فاز ۲۳» در git log |

| ممیزی | **ممیزیِ کاملِ پایانِ جلسه (به دستورِ امین)**: (۱) دو نقضِ قانونِ برندینگ در /empire رفع شد («یک بازیِ تازه…» → «فصلِ تازه‌ای از امپراتوری…» + tooltip «بازیکنان» → «امپراتورها»)؛ (۲) پوششِ knobها راستی‌آزمایی شد: هر ۸۷ کلیدِ empire در کانفیگ فیلدِ زندهٔ ادمین دارد — بدونِ شکاف؛ (۳) نقضِ قانونِ digitsOf در کلِ EmpireAdminPanel رفع شد: همهٔ ۸ نقطهٔ parse عددی (cin/setC، capIn/setCap، پروانه، امکانات، تنظیمِ بازیکن، کارمزدِ صندوق، rolloutPct، EventComposer) حالا رقمِ فارسی/عربی و «٫» و جداکنندهٔ هزارگان را می‌پذیرند (deFa/numOf) | commit «ممیزی پایان جلسه» در git log |

| ۲۴ | **بازیِ واقعاً قابل‌بازی (گزارشِ مستقیم امین: نقشه کار نمی‌کند + ساخت‌وساز دست‌نیافتنی + «گیم دکوری است»)**: (۱) **ریشهٔ خرابیِ نقشه**: URL قدیمیِ SDK نشان (`1.4.0/…/dist`) روی CDN مرده بود — همه‌جا بی‌صدا به fallback استاتیک می‌افتاد و در /empire (بی‌fallback) رو شد؛ NeshanMap حالا مسیرِ رسمیِ مستندات (`v1.9.4/neshan-sdk/v1.0.8`) را با نامزدِ پشتیبان امتحان می‌کند؛ (۲) **🏞 بازارِ زمین** (اکشن `lands` + کارتِ تبِ شهر): زمین‌های واقعیِ قیمت‌دارِ متراژدار، اول در حدِ سرمایه بعد قفل‌دارِ صادقانه — دروازهٔ موتورِ ساخت که تا امروز فقط از شانسِ فرصت‌های روزانه ممکن بود؛ خرید = buy + برنامهٔ «ساخت» یکجا؛ (۳) **🧭 مسیرِ برجِ اول**: استپرِ ۴قدمی از وضعیتِ واقعیِ بازیکن (زمین→پروانه→کلنگ→تحویل) با CTA به قدمِ بعدی — بعد از تحویلِ برجِ اول محو می‌شود؛ (۴) **نوارِ ادمینِ «مشاهدهٔ پنل» (z:9999) منوی بازی را می‌پوشاند و پرتفوی/شرکت عملاً غیرقابل‌کلیک بود** → در /empire بالای منو می‌نشیند؛ راستی‌آزمایی: حلقهٔ کامل با مرورگر و APIِ واقعی بازی شد (خرید زمین → برنامه → پروانهٔ صادرشده → نقشهٔ ۹گزینه‌ای → کلنگ → کارگاهِ ۲۱روزهٔ ۴واحدی) — بدونِ خطای JS | commit «Empire فاز ۲۴» در git log |

| ۲۵ | **تجمیع و تخریب (دستورِ صریح امین: «۶ واحدی؟ تک‌تک بخر؛ تا همه را نخریدی تخریب نه»)**: (۱) **🧩 تجمیعِ واحدها** — کلِ واحدهای ساختمانِ آپارتمانی/تجاری اول از متای واقعیِ آگهی («طبقه: X از Y» → Y)، وگرنه قطعی از هش (unitsMin..unitsMax — knob)؛ هر واحدِ بعدی به قیمتِ روزِ واقعیِ همان آگهی + ٪ پرمیومِ تجمیع (knob، شفاف در UI)؛ **مالکِ هر واحد شخصیتِ قطعیِ خودش را دارد** (کلیدِ `listingId#uN` در ownerPersonaOf/negotiationOutcome) و مذاکرهٔ per-unit؛ پول: قیمت از سرمایه، مالیات→خزانه، بهای تمام‌شده جمع می‌شود؛ ارزش/فروش/netWorth × واحدهای مالکیت‌شده؛ (۲) **🧨 تخریب** — آپارتمان/تجاری فقط با مالکیتِ کاملِ همهٔ واحدها؛ ویلایی مستقیم؛ هزینه = ٪ ارزشِ روز (knob) → `demolitionPaid` (مصرفِ شفاف)؛ دارایی → زمین با `landAreaOverride` (بنا×واحدها ÷ تراکم؛ ویلایی = متراژِ آگهی) → سه مسیرِ زمین → پروانه → کلنگ (buildPlan/startBuild با override)؛ ارزشِ تخریب‌شده تا ساخت = بهای تمام‌شده؛ نشانِ «First Demolition»؛ (۳) کارتِ ادمین «🧩 تجمیع و تخریب» (۵ knob) + اکشنِ **rename** ادمین (ویرایشِ نامِ امپراتوریِ هر بازیکن با ثبتِ ممیزی — کنارِ تزریقِ منابع/حذف/Ghost که از فاز ۵ بود)؛ راستی‌آزمایی: حلقهٔ کامل با API+مرورگرِ واقعی (خرید ۱ از ۳ → مذاکره با «مالکِ واحدِ ۲» → خرید تا ۳/۳ → تخریب → زمینِ ۸۲متری → پروانه → کلنگ) — بدونِ خطای JS | commit «Empire فاز ۲۵» در git log |

| ۲۶ | **نقشهٔ شهرِ کامل (گزارش امین: «زوم می‌کنی برمی‌گردد» + «ملکِ خریده باید پینِ متفاوت داشته باشد» + فصل ۹ City Screen)**: (۱) **ریشهٔ پریدنِ زوم**: صفحهٔ بازی هر ثانیه رندر می‌شود (شمارشِ معکوس) → آرایهٔ points هویتِ تازه → هر ثانیه بازسازیِ مارکر + fitBounds؛ حالا NeshanMap امضای دادهٔ پین‌ها را نگه می‌دارد (بدونِ تغییرِ واقعی هیچ کاری نمی‌کند) و بعد از اولین زوم/جابه‌جاییِ خودِ کاربر دیگر **هرگز** auto-fit نمی‌کند (تفکیکِ حرکتِ برنامه‌ای از کاربر با پرچم)؛ (۲) **پین‌های متمایز** (divIcon مشترک در NeshanMap — icon/color per point): 🏛 طلایی = دارایی خودِ بازیکن، 🏗 = کارگاهِ در حالِ ساخت، 🔥 نارنجی = فرصتِ امروز، 🏞 سبز = زمینِ بازار؛ (۳) **لایه‌های قابل‌تغییر** با چیپ‌های شمارش‌دار (دارایی/فرصت/زمین) + راهنمای پایینِ نقشه + ارتفاع ۳۸۰؛ (۴) کلیکِ پینِ دارایی → صفحهٔ پرتفوی؛ فرصت/زمین → آگهیِ واقعی؛ زمین‌های بازار هم روی نقشه آمدند | commit «Empire فاز ۲۶» در git log |

| ۲۷ | **زمان‌خری (دستور امین: «هر چیزِ زمان‌بری پاداشِ کم‌شدنِ زمان داشته باشد» — دقیقاً قانون ۵ سند: پرداخت فقط برای سرعت)**: ممیزیِ مکانیک‌های زمان‌دار → دو شتاب‌دهندهٔ کوین‌سوز: (۱) **⚡ پیگیریِ حضوریِ پروانه** — هر کوینِ knob یک روز از بررسی کم می‌کند (اعتراض/عوارض سرِ جایشان — انتظار قابلِ‌خرید است، نتیجه نه)؛ (۲) **⚡ شیفتِ شبانهٔ کارگاه** — هر روزِ جلوافتاده = کوین + هزینهٔ تومانیِ همان روز از سرمایه (پول غیب نمی‌شود) و **از چک‌پوینت‌های ۳۰٪/۷۰٪ فرار نمی‌کند** (منطقِ رویداد/تکمیل به توابعِ مشترکِ fireCheckpointEvent/completeIfBuilt استخراج شد و boost از همان می‌گذرد)؛ کارتِ ادمین «⚡ سرعت و زمان» (enabled + کوین/روزِ هر دو)؛ نرخ‌ها شفاف در UI (state.speed)؛ + **ممیزیِ هاردکدها**: شانس/بازهٔ تخفیفِ مذاکره (۲۵٪ پایه، ۲..۶٪) که کاربر می‌دید knob شد (`nego.baseChancePct/discountMin/discountMax` + کارتِ ادمین «🤝 مذاکره») — پیش‌فرض‌ها رفتارِ قبلی را عیناً حفظ می‌کنند؛ ⏳ ضرایبِ باندِ اعتباریِ وام (۰٫۷۵/۰٫۹/۱٫۴) و سقف‌ها (۱٫۲/۱/۰٫۷/۰٫۳) هنوز ثابتِ مستندند — تریگر: درخواستِ تنظیمِ باندها | commit «Empire فاز ۲۷» در git log |

| ۲۸ | **فروشگاهِ ملک‌کوین + نجاتِ کاشی‌های نقشه (گزارش امین: نقشهٔ خاکستری + «جای خرید سکه نیست»)**: (۱) **🪙 فروشگاهِ کوین (زرین‌پال)** — صفِ #۴ تراکر بسته شد: بسته‌ها کاملاً از ادمین (کارتِ «🪙 فروشگاه»: نام/کوین/قیمت/فعال + افزودن/حذف)، کارتِ خرید در تبِ «بازار» با یادآوریِ صریحِ بدونِ P2W (کوین فقط سرعت/تحلیل)، `/api/empire/coins` (POST ساختِ پرداخت + GET بازگشتِ درگاه: verify → `creditCoinPurchase` با **ایدمپوتنسیِ authority** تا رفرش دوبار شارژ نکند + تایم‌لاین + audit)، config-gated (بدونِ مرچنت → پیامِ صادقانه؛ مرچنت از «اتصال‌ها → زرین‌پال» که از قبل بود)؛ (۲) **نگهبانِ کاشیِ نقشه**: نقشهٔ خاکستری = کلیدِ نشان بدونِ مجوزِ «نقشهٔ پویا (Web SDK)» — حالا NeshanMap اگر بعد از ۶ ثانیه کاشی‌ای لود نشود یک‌بار با استایلِ پایهٔ «neshan» دوباره می‌سازد (شاید استایلِ شب در پلنِ کلید نباشد) و اگر باز نشد راهنمای دقیقِ فعال‌سازیِ سرویسِ کلید را روی نقشه می‌گذارد؛ راستی‌آزمایی: POST بدونِ مرچنت پیامِ درست، GET لغو → redirect ‎/empire?coins=fail، بسته‌ها در state، ۳ تستِ PG ایدمپوتنسی | commit «Empire فاز ۲۸» در git log |

| ۲۹ | **نقش‌های سایت در سناریو + قوانینِ واقعیِ ساخت (دستور امین: «تعداد واحد/طبقه با قوانین واقعی + بتونه خلاف کنه و جریمهٔ ماده۱۰۰ بده + همهٔ نقش‌ها فعلاً سیستم بازی کند»)**: (۱) **📐 طراحیِ معمار پیش از پروانه** — بازیکن طبقات و واحد در طبقه را خودش انتخاب می‌کند؛ قوانینِ شفافِ knob: سطحِ اشغال (٪زمین)، تراکم → طبقاتِ مجاز، حداقل متراژِ واحد؛ طراحی designDays طول می‌کشد (⚡ قابلِ‌تسریع)، حق‌الزحمهٔ معمار ٪هزینهٔ ساخت → servicesPaid؛ پروانه بدونِ نقشه رد می‌شود و فقط طبقاتِ قانونی را می‌پوشاند؛ (۲) **⚖️ تخلف و ماده۱۰۰** — تا ۲ طبقهٔ مازاد «می‌شود» ساخت (تصمیمِ آگاهانه با جریمهٔ برآوردیِ اعلام‌شده)؛ واحدهای مازاد از کلنگ علامت می‌خورند: پیش‌فروش فقط روی قانونی‌ها، فروش تا حلِ پرونده بسته؛ تکمیل → کمیسیونِ ماده۱۰۰ با سه راه: 💰 جریمه (متراژ مازاد × costPerM × ضریبِ knob) → خزانه/شهرداری؛ 🧑‍⚖️ وکیل (یک‌بار، قطعی از هش، حق‌الوکاله ٪جریمه، بردن = ٪کاهش)؛ 🧨 تخریبِ طبقاتِ مازاد (ازدست‌دادنِ واحدها)؛ (۳) **🏛 نقش‌های حرفه‌ای، فعلاً سیستم** (proPersonaOf قطعی از هش): دفترخانه در هر خرید (حق‌الثبت)، مشاورِ املاک برای اجاره (کمیسیونِ ۲۵٪ یک ماهِ میانهٔ واقعیِ محله — بدونِ مشاور اجاره ممکن نیست) و فروش (٪کمیسیون)، کارشناسِ رسمی در وام (هزینه از مبلغ کسر)، پیمانکار در کلنگ، وکیل در ماده۱۰۰ — همه با کارمزدِ شفاف → `servicesPaid` و تایم‌لاین؛ واگذاری به متخصصانِ واقعیِ سایت با تریگر در ENGINE-MAP؛ (۴) **🛠 بازسازیِ واقعی** — ۳ گزینهٔ knob (آشپزخانه/نما/کامل): هزینهٔ ٪ارزشِ روز → بهای تمام‌شده، ارزش‌افزودهٔ +٪ با سقف؛ در netWorth/فروش لحاظ می‌شود؛ هر گزینه یک‌بار؛ (۵) ۴ کارتِ ادمینِ جدید (نقش‌ها/طراحی‌وتراکم/ماده۱۰۰/بازسازی) + فرمِ طراحی و کارتِ ماده۱۰۰ و ردیفِ بازسازی در پرتفوی + گامِ «معمار» در مسیرِ برجِ اول؛ ۶ یونیت + ۲۱ تستِ PG + e2e زندهٔ API (پروانه بدونِ نقشه رد، قرارداد، تسریع، صدور) | commit «Empire فاز ۲۹» در git log |

| ۳۰ | **hotfix نقشه + خطِ آسمانِ خوانا (گزارش امین: «نقشه رو همه‌جای سایت از کار انداختی»)**: تغییرِ ترتیبِ SDK در فاز ۲۴/۲۸ (v1.9.4 اول) نقشهٔ همهٔ صفحات را خاکستری کرد — v1.9.4 لود می‌شود اما کاشیِ نشان نمی‌آورد؛ **بازگشت به URLِ اثبات‌شدهٔ قدیمی به‌عنوانِ اصلی** (v1.9.4 فقط پشتیبان)، حذفِ بازسازیِ خودکارِ استایل (نگهبانِ تایل فقط «راهنما» شد)، حذفِ برچسبِ پیش‌فرضِ «Leaflet» (setPrefix('') — اعتبارِ نشان می‌ماند)؛ بهبودهای بی‌خطر ماندند (پایداریِ زوم، پینِ divIcon چون opt-in است)؛ **قانونِ ۱۱ ثبت شد: رفتارِ پیش‌فرضِ کامپوننت‌های مشترک قفل است**؛ + خطِ آسمان خوانا شد: ارزشِ روز بالای هر برج، آیکنِ نوع، نامِ محله زیرِ برج، اسکرولِ افقی | commit «فاز ۳۰» در git log |

| ۳۱-۳۲ | **کلیک‌های قابل‌اعتماد + سند ۲۱ (فصل ۱۱ Audio)**: فاز ۳۱ — ریشهٔ «ده بار باید بزنم»: رندرِ هرثانیهٔ کلِ صفحه (شمارشِ معکوس) → Countdown ایزوله شد؛ خطاها فقط بالای صفحه بودند → توستِ شناور + نشانگرِ «⏳ در حالِ انجام» + ErrorFence (کرشِ رندر = کارتِ قرمزِ پیامِ دقیق) + شنودِ window.error → `/api/client-log` (خطای مرورگرِ کاربر در لاگِ pm2 با برچسبِ [client-error])؛ فرمِ معمار دیگر «بی‌صدا قفل» نمی‌شود (دلیلِ دقیقِ عدد خارج از محدوده)؛ نقشهٔ امضاشده خودکفا شد (landArea داخلِ design — کلنگ به زنده‌بودنِ آگهی وابسته نیست). فاز ۳۲ — **بازخوردِ صوتیِ سنتزشده** (`empire-sound.ts`، WebAudio بدونِ فایل): success/error/reward/levelup/coin/build/warn — قوانینِ سند: <۱ث، مثبت/منفی متمایز، شروع <100ms، حجمِ صفر، بدونِ تداخل (۱۵۰ms فاصله)، هیچ خبری فقط صوتی نیست؛ کنترلِ 🔊 کاربر در HUD (خاموش/حجم + صدای تست، localStorage) + knob ادمین `sound.enabled` | commit «فاز ۳۱/۳۲» در git log |
| ۳۳ | **سند ۲۲ (فصل ۱۲ Monetization)**: 🎨 **فروشگاهِ ظاهری** (Part 03) — قاب/نشان با ملک‌کوین (`cosmetics.items` کاملاً از ادمین: آیکن/نام/نوع/قیمت/فعال)؛ «هیچ آیتمِ ظاهری روی اقتصاد/سرعت/قدرت اثر نمی‌گذارد» — فقط نمایش: قاب روی پرسونا و نشان کنارِ نام در HUD، لیدربورد و پروفایلِ عمومی (ارزشِ show-off — Part 03 🎯)؛ خرید دائمی، خودکار-فعال‌سازیِ اولین آیتم، در تایم‌لاین ثبت. 🎁 **موتورِ پیشنهادِ هوشمند** (Part 09 — Offer Engine): `offerOf` قطعی از رفتارِ واقعی (اولین شارژ/سطحِ ۵+ بدونِ قاب/برج‌ساز با پروژهٔ تحویلی/مذاکره‌گر با ۳ برد)، حداکثر ۱ در روز، بستن با یک لمس → `offerHist` تا `cooldownDays` پنهان، `minAgeDays` روزِ اولِ بازیکنِ تازه پیشنهاد نمی‌بیند، بدونِ پاپ‌آپ/تایمرِ ساختگی (موارد ممنوعِ سند ✓). 🪙 **بستهٔ زمان‌دار** (Part 07 Bundles): فیلدِ `until` روی هر پکِ فروشگاهِ کوین (`activeCoinPacks` — تا پایانِ روزِ until، تایمرِ واقعی نه نمایشی)؛ UI «⏳ فقط تا تاریخ»؛ callbackِ پرداختِ انجام‌شده حتی بعدِ انقضا شارژ می‌شود (forVerify). knobهای ادمین: کارتِ 🎨 (ویرایشگرِ آیتم‌ها) + کارتِ 🎁 (enabled/cooldownDays/minAgeDays) + ستونِ «تا تاریخ» در ویرایشگرِ پک‌ها | این commit |

| ۳۳ب | **فیکسِ فیدبک: متراژِ جاافتاده = برآورد، نه بن‌بست.** «متراژ در آگهی ثبت نشده» حذف شد — زنجیرهٔ برآوردِ داده‌محور: فیلدِ متراژِ آگهی → `areaFromText` (بزرگ‌ترین «N متر»ِ متنِ خودِ آگهی؛ «کلنگی ۲۱۰ متری بر ۷ متری» → ۲۱۰؛ زیرِ ۳۰ = برِ کوچه، کاندید نیست) → قیمت ÷ میانهٔ متریِ واقعیِ محله (`hoodPerM`، وگرنه کلِ شهر) — همیشه با برچسبِ صادقانهٔ «برآورد از متنِ آگهی / قیمتِ متریِ محله» در UI (`areaNote`). اعمال در: قراردادِ معمار (designPlan/designStart)، تخریب، نقشهٔ ساخت (buildPlan/startBuild)، بازارِ زمین (زمین‌های بدونِ فیلدِ متراژ ولی با متراژ در عنوان هم می‌آیند) و متراژِ تجمیع/ویلا در پرتفوی. فقط اگر هیچ‌کدام جواب نداد پیامِ صادقانهٔ «قابل‌برآورد نبود» | این commit |

| ۳۴ | **سند ۲۳ (فصل ۱۳ Technical Design)**: 🧪 **Economy QA** (پیشنهادِ Part 09 — «به‌اندازهٔ تستِ فنی مهم»): `scripts/economy-qa.mjs` — ۳۰ ممیزیِ خودکارِ knobهای **زندهٔ** اقتصاد (پولِ مجانی؟ تخفیف/پاداشِ خارج از بازه؟ جریمهٔ تخلف ارزان‌تر از ساختِ قانونی؟ نرخِ ناسازگارِ بسته‌های کوین؟ بازسازیِ سودِ غیرمنطقی؟) با سه سطحِ ✓/⚠/✗؛ در `deploy.sh` بعد از build خودکار اجرا می‌شود (هشدار، نه توقفِ دیپلوی). 🛡 **سپرِ نرخِ درخواست** (قانونِ Part 04): `rateHit` خالص + سقفِ `empire.api.rateLimitPerMin` (پیش‌فرض ۱۲۰/دقیقه، ۰=خاموش، knob در کارتِ «🛡 فنی») روی POST /api/empire → 429 با پیامِ صادقانه | این commit |

| ۳۵ | **سند ۲۴ (فصل ۱۴ Analytics & Big Data)**: 🔭 **رصدخانهٔ اقتصاد** (`empire-metrics.ts`) — اسنپ‌شاتِ روزانهٔ خودکار (کرانِ اینستنس ۰، ایدمپوتنت + دکمهٔ 📸 ادمین): بازارِ واقعی (میانهٔ متریِ کل + ۱۲ محلهٔ پرنمونه، تعدادِ آگهی) + اقتصادِ بازیکنان (سرمایه/کوین/ارزشِ خالص/خزانه/حقوق/خدمات/DAU/WAU/تولدها/تمرکزِ ثروتِ ۱۰٪) — ذخیرهٔ dual-mode (kv/فایل) تا ۴۰۰ روز. `economyHealthOf`: تورمِ ۷/۳۰روزه، رشدِ نقدینگی، افتِ DAU، تمرکزِ سرمایه → هشدارهای Part 06 با آستانه‌های knob (`empire.metrics.*`). **IES** (پیشنهادِ Part 07 — Investment Engagement Score): ترکیبِ قطعیِ حضورِ ۷روزه + دارایی + پروژهٔ تحویلی + مذاکرهٔ برنده + تحلیلِ درست + شرکت + تحسین (۰..۱۰۰) — ستونِ جدولِ بازیکنانِ ادمین. منوی جدید: امپراتوری → «🔭 رصدخانهٔ اقتصاد» (KPI، روندها، هشدارها، جدولِ ۱۴روزه، مناطقِ داغ، آستانه‌ها) — تا ۲ اسنپ‌شات نباشد هیچ روندی ادعا نمی‌شود | این commit |

| ۳۶ | **سند ۲۵ (فصل ۱۵ AI Platform)**: 🧠 **داشبوردِ انسانیِ AI** (Part 10 «Human Dashboard») — منوی جدیدِ ادمین: امپراتوری → «🧠 هوشِ مصنوعی»: اقدام‌های واقعیِ ۷ روزِ اخیر از مسیرِ پیشنهاد/تحلیلِ سیستم (`aiUsageOf` از رویدادهای REOS با srcهای empire_buy/analyze/guess/crowd/assembly)، نرخِ بازشدنِ نامهٔ روزانه (امروز/دیروز)، پیشنهادهای بسته‌شده، آیتم‌های ظاهریِ فروخته، موجودیِ ژتونِ تحلیل + 🎚 **نگاشتِ سطوحِ اختیارِ AI** (Level 0-3 با وضعِ واقعیِ ملک‌جت — هیچ knob اقتصادی خودکار تغییر نمی‌کند) + 📜 **قانونِ اساسیِ AI** (۸ اصل، هر کدام با ضامنِ اجراییِ واقعی: دکترینِ «LLM توضیح می‌دهد نه تصمیم»، قابلِ‌رد بودنِ پیشنهادها، بدونِ P2W با Economy QA، رصدخانه، Explainability «چرا»، Kill Switch فلگ‌ها، Audit log) | این commit |

| ۳۷ | **اجتماعِ بازیکنان (درخواستِ مستقیم)**: 🔒 **مالکیتِ انحصاری** — هر آگهیِ واقعی فقط یک مالکِ بازیکن (`empire-social.ts`: دفترِ kv اتمیک؛ ادعا پیش از خرید، آزادسازی سرِ فروش به بازار، انتقال سرِ معامله؛ خطای صادقانه با نامِ مالک؛ backfill کرانِ روزانه — عطف به ماسبق نمی‌شود). 🏪 **بازارِ بازیکنان** — عرضهٔ دارایی با قیمتِ دلخواه (`forSale`) و خریدِ بازیکنِ دیگر با **تراکنشِ اتمیکِ دو-کاربره** (`twoUserTx`: قفلِ FOR UPDATE هر دو ردیف به ترتیبِ id): خریدار قیمت+مالیات (→خزانه)، فروشنده قیمت−کمیسیونِ مشاور (→servicesPaid) + realized؛ دارایی با ویژگی‌های واقعی‌اش (نقشه/بازسازی/واحدها) منتقل، قراردادهای شخصی صفر. 🤝 **مشارکتِ ساخت (پروژهٔ مشترک)** — مالکِ زمین/پروژه سهمِ ٪ (سقفِ knob ۴۹٪) در برابرِ آوردهٔ نقدی عرضه می‌کند؛ شریک می‌پیوندد (آورده→سرمایهٔ سازنده)؛ **سهمِ شریک از هر پیش‌فروش/فروشِ واحد/فروشِ پروژه خودکار و اتمیک تسویه می‌شود** (`settlePartnerShares`)؛ قاعدهٔ شفاف: هزینهٔ روزانهٔ کارگاه و اجاره با سازنده. 🏰 **اتحاد (کلن)** — ساخت (هزینهٔ ثبت→خزانه)/پیوستن/خروج (رهبری به قدیمی‌ترین)/تابلوی پیامِ ۶۰تایی؛ شماره‌تلفن هرگز به بازیکنان نمی‌رسد (`clanView`). **همه سطح‌گشا**: `unlocks.tradeLevel` (بازار+مشارکت، پیش‌فرض ۶) و `unlocks.clanLevel` (پیش‌فرض ۸) + کارتِ ادمینِ «🏰 اجتماع» با ۹ knob. UI: کارتِ بازارِ بازیکنان/مشارکت‌ها در تبِ بازار، کنترل‌های عرضه/جذبِ شریک روی هر دارایی، کارتِ اتحاد در تبِ رتبه‌ها — با قفلِ سطحِ صادقانه. (تبدیلِ تجاری→مغازه با درآمد از قبل بود: chooseBusiness با سیگنال‌های واقعیِ محله) | این commit |

| ۳۸ | **پوستهٔ گرافیکی از فایلِ طراحیِ کاربر (پروتوتایپِ کامل)** — فقط ظاهرِ /empire، صفر تغییرِ منطق: پس‌زمینهٔ شعاعیِ سرمه‌ای، کارتِ شیشه‌ای، دکمه/چیپِ طلاییِ گرادیانیِ قرصی، HUD با حلقهٔ طلاییِ آواتار + قرص‌های منابع + نوارِ XP گرادیانی + blur، نوارِ تبِ شیشه‌ای با تبِ فعالِ طلایی، تیترِ سِریفِ Markazi بالای هر ۵ تب (دسته‌بندی)، keyframeهای glow/shimmer. فونتِ Markazi از قبل self-host بود (public/fonts) | این commit |

| ۳۹ | **سند ۲۶ (فصل ۱۶ Cognitive AI Platform)**: 🧭 **هوشِ سرمایه‌گذاری** (`empire-intel.ts` — همه توابعِ خالصِ قطعی، طبق جمع‌بندیِ نهاییِ خودِ سند: «فقط فیچرِ قابل‌ساخت، AI پیشنهاد می‌دهد نه اجرا، بدونِ حدس، Confidence از حجمِ داده، هر پیشنهاد با دلیل»): **① ارزش‌گذاریِ ملک** (Part 03) — روی اکشنِ analyze (همان ژتونِ تحلیل): ارزشِ منصفانه = میانهٔ متریِ واقعیِ هم‌محله‌ها × متراژ (متراژِ جاافتاده → برآوردِ متنِ آگهی)، نشانِ 🟢/🟡/🟠/🔴 (منصفانه/کمی بالاتر/گران/احتمالِ حباب — باندها knob)، بازهٔ سناریو از **چارک‌های واقعیِ** قیمتِ محله (p25/p75 — نه عددِ ساختگی)، عمقِ بازار از عرضهٔ واقعی، امتیازِ سرمایه‌گذاری ۰..۱۰۰ توضیح‌پذیر، Confidence فقط از تعدادِ نمونه (سقف ۹۵؛ زیرِ minComps → صادقانه «داده کافی نیست»)، reasons. **② تصمیم‌یارِ «اگر بخری»** (Part 05 DSS) — چکِ مالیِ واقعیِ خودِ بازیکن پیش از خرید: نقدِ باقی‌مانده، هشدارِ نقدینگی <۱۰٪، دوامِ نقد با خرجِ روزانه، نسبتِ بدهی — فقط هشدار، هرگز اجرا. **③ هوشِ بازار** (Part 04) — روندِ محله‌ها از تاریخچهٔ واقعیِ رصدخانهٔ فاز ۳۵ (۳ صعودی/۳ نزولی + روندِ شهر؛ تا ۲ اسنپ‌شات نباشد هیچ روندی ادعا نمی‌شود) — کارتِ «🧭 روندِ بازارِ واقعی» در تبِ بازار. **④ جریانِ نقدی + سلامتِ مالی** (Part 10) — درآمدِ روزانه از میانگینِ درآمدِ **ثبت‌شدهٔ** خودِ دارایی‌ها، خرج از قراردادهای جاری (حقوقِ مهندس + هزینهٔ روزشمارِ ساخت + بهرهٔ وام)، پیش‌بینیِ ۷/۳۰/۹۰ روز + دوامِ نقد؛ امتیازِ سلامتِ مالی ۰..۱۰۰ با دلایل (نقدینگی/بدهی/جریان/تنوعِ نوع و محله/سودِ تحقق‌یافته). **⑤ اولویت‌های امروز** (Part 11 — حداکثر ۵) — از وضعیتِ واقعی: رویدادِ کارگاه، ماده۱۰۰، بی‌پولیِ ساخت، سررسیدِ وام، پروانهٔ صادرشده، نقشهٔ آماده، واحدِ فروش‌نرفته، داراییِ بی‌تصمیم — کارتِ «🧭 اتاقِ تحلیل» بالای تبِ شهر. اکشنِ جدیدِ `intel` + کارتِ ادمینِ «🧭 هوشِ سرمایه‌گذاری» با ۹ knob (`empire.intel.*`). ۳۲ تستِ خالص + دودآزمای E2E (ارزش‌گذاری با ۶ نمونهٔ واقعی + intel) | این commit |

| ۴۰ | **سند ۲۷ (فصل ۱۶ Parts ۱۳–۲۳)**: ⚙️ **مرکزِ خودکارسازی** (Part 13 — تنها به‌همراهِ Part 21 سیستمِ جدیدِ قابل‌ساختِ این سند): قوانینِ قابل‌تعریفِ خودِ بازیکن با ۵ قالبِ آماده (نقدینگی زیرِ آستانه / سودِ دارایی بالای ٪ / افتِ ارزش زیرِ خرید / تأخیرِ کارگاه / سررسیدِ وام) — آستانه و سطح (فقط «خبر بده» یا «پیشنهاد بده») دستِ بازیکن؛ **قانونِ سختِ خودِ سند کد شده: هیچ خرید/فروش/ساختی هرگز خودکار اجرا نمی‌شود**. ارزیابی قطعی از وضعیت/قیمت‌های واقعی (`evalRules` خالص در empire-intel)؛ بدونِ قیمتِ روز، قوانینِ قیمتی ساکت می‌مانند (نه عددِ ساختگی). CRUD اتمیک + سقفِ knob + **دفترِ ثبت** (هر قانون حداکثر یک‌بار در روز — کلیدِ روزانهٔ claims) + هشدارها و مدیریتِ قوانین داخلِ «🧭 اتاقِ تحلیل». 🧾 **هوشِ قرارداد** (Part 21 — تحلیلِ پیش از امضا، فقط از اعدادِ واقعیِ قرارداد): در بازارِ بازیکنان زیرِ هر عرضه «فروشنده X٪ بالاتر/پایین‌ترِ قیمتِ خریدِ خودش می‌فروشد» (`tradeAskCheckOf` — قیمتِ خریدِ واقعی در دفترِ بازی ثبت است) و زیرِ هر پیشنهادِ مشارکت سنجشِ «آورده در برابرِ سهمِ منصفانه از هزینهٔ واقعیِ ساخت» (`jvOfferCheckOf`؛ بدونِ کلنگ → صادقانه «نامشخص»). knobها: `empire.automation.{enabled,maxRules,logCap}` در کارتِ 🧭. ۱۲ یونیت + ۸ PG جدید + دودآزمای E2E (قانونِ واقعی فعال شد، ثبتِ یک‌بار-در-روز، نوعِ نامعتبر رد) | این commit |

| ۴۱ | **سند ۲۸ (فصل ۱۷ — Marketing/ERP → پیوتِ Tycoon)**: ثبتِ **قانونِ ۹۰/۱۰** (قانونِ ۱۲ بالا — فلسفهٔ کلِ ادامهٔ مسیر). 💎 **معاملهٔ بزرگِ هفته** (Part 07 Big Deals): هفته‌ای یک ملکِ «واقعیِ» گران‌قیمت از topPct٪ بالای بازار — انتخابِ قطعیِ **شهری** (هشِ هفته، نه per-user) تا همه بر سرِ همان یک ملک رقابت کنند و اولین برنده با مالکیتِ انحصاریِ فاز ۳۷ صاحبش شود؛ یک تلاشِ مذاکره در هفته با ۳ استراتژیِ سند (تهاجمی/متعادل/محافظه‌کار — مبادلهٔ شفافِ شانس↔تخفیف)، شانس = پایه + مهارت÷۲ + تیپِ مالک ± استراتژی (شفاف به بازیکن)، تخفیفِ برنده **سمتِ سرور** ذخیره (`bigDealWin` — ضدِ دستکاریِ استراتژیِ کلاینت) و خرید با پرچمِ bigDeal روی همان مسیرِ اتمیکِ خرید؛ شکست = قفل تا هفتهٔ بعد (تایم‌لاین 🔥/🚪). ⭐ **کمیابیِ صادقانهٔ فرصت‌ها** (Part 10 Rare Level): درجهٔ نایاب/کمیاب/ویژه/عادی فقط از فاصلهٔ واقعیِ متری با میانهٔ محله (بدونِ نمونهٔ کافی = بدونِ برچسب). 🚨 **اتاقِ بحران + ققنوس** (Part 13): تشخیصِ بحران از جمعِ سیگنال‌های واقعی (دوامِ نقد ≤ knob، وامِ معوق/نزدیک بدونِ نقد، کارگاهِ خوابیده > knob) با سطوحِ زرد/نارنجی/قرمز — بنرِ اتاقِ تحلیل با دلایل و «دوامِ نقد»؛ ورود/خروج اتمیک در تایم‌لاین و خروج = شمارندهٔ crisisRecovered + نشانِ مخفیِ **«ققنوس»** (پیشنهادِ خودِ سند: بحران داستان بسازد). knobها: `empire.bigDeal.*` (۵) + `intel.crisisRunwayDays/crisisStalledDays`. ۱۶ یونیت + ۷ PG جدید + دودآزمای E2E (انتخابِ سگمنتِ گران + قفلِ سطح + کمیابیِ واقعی + بحرانِ غیرفعالِ سالم) | این commit |

| ۴۲ | **ضابطهٔ واقعیِ ساخت (فیدبکِ مستقیم: «قوانینِ ساخت بر اساسِ متراژ و منطقه واقعی نیست»)**: طبقاتِ مجاز دیگر ضریبِ ثابتِ تراکم نیست — **پلکانی با متراژِ زمین** طبقِ عرفِ طرحِ تفصیلی (<۱۵۰م→۲ط، <۲۵۰→۳ط، <۵۰۰→۴ط، <۱۰۰۰→۵ط، بزرگ‌تر→۶ط؛ همهٔ آستانه‌ها/طبقات knob) + **تعدیلِ «منطقه» از عرفِ واقعیِ ساختِ محله**: میانهٔ کلِ طبقاتِ ساختمان‌ها از متای واقعیِ «طبقه: X از Y» آگهی‌های هم‌محله (`floorsOfMeta`/`hoodFloorsNorm` — کمتر از ۳ نمونهٔ واقعی = بدونِ تعدیل، نه عددِ ساختگی) تا سقفِ hoodBonusMax طبقه بالاتر (`legalFloorsOf`) + **ضابطهٔ «هر واحد یک پارکینگ»**: ظرفیت = همکف ÷ سرانهٔ ۲۵م × طبقاتِ قابلِ‌پارک (۲) — طرحِ بیش از ظرفیت با پیامِ صادقانه رد می‌شود؛ سطحِ اشغال همان ۶۰٪ عرفی. designPlan/designStart هر دو از یک ضابطه (عددِ نمایش = عددِ قرارداد) و پاسخِ designPlan «ruleNote» شفاف برمی‌گرداند که عدد از کجا آمد (در فرمِ معمار نمایش داده می‌شود). فرمولِ قدیمیِ buildFactor فقط fallbackِ سازگاری. ۱۲ knob جدیدِ کارتِ 📐 + ۱۱ تستِ یونیت + E2E (زمینِ ۳۰۰م + محلهٔ با عرفِ ۸ طبقه → مجاز ۶؛ ۱۸ واحدِ بی‌پارکینگ رد شد) | این commit |

| فیکس | **باگِ بحرانیِ ورود («کد درست را می‌زنم، می‌گوید اشتباه است — ۲۰ پیامک»)**: ریشه = ذخیرهٔ OTP در `Map` درون-حافظه‌ایِ «هر اینستنس»؛ پیامک را اینستنسِ A می‌فرستاد، تأیید به B/C می‌رسید (nginx بینِ ۳ اینستنس پخش می‌کند) → همیشه invalid مگر شانسی همان اینستنس. فیکس: `otp-store` بازنویسی به dual-mode اشتراکی (PG kv «otp» اتمیک؛ فایلِ مشترک در فایل‌مود) + **کولداونِ ارسالِ مجددِ سمتِ سرور** (۱۲۰ث — رفرشِ صفحه دیگر دورش نمی‌زند؛ 429 با retryIn) + ذخیرهٔ کد فقط بعدِ ارسالِ موفقِ پیامک + retryIn در پاسخِ send-otp/phone-start/shahkar تا تایمرِ UI از سرور تنظیم شود (auth/page + AuthModal). مسیرِ send-otp هم به منطقِ واحدِ lib یکی شد. + **شفافیتِ مشاور در فروش** (فیدبک: «فروش/اجاره از طریقِ مشاور/آژانس»): تأییدیهٔ فروش حالا مثلِ اجاره صریح می‌گوید «از طریقِ مشاور/آژانسِ املاکِ محله — کمیسیونِ X٪» (مکانیکِ کمیسیون از فاز ۲۹ بود؛ حالا دیده می‌شود). ۵ تستِ PG جدیدِ OTP + دودآزمای E2E (کدِ درست ✓، کولداون 429، کدِ قبلی بعدِ ردِ resend معتبر) | این commit |

| ۴۳ | **رویاپردازیِ کلنگ + برگهٔ قراردادِ مشاور/آژانس (دو فیدبکِ مستقیم)**: 🤝 **برگهٔ قراردادِ فروش/اجاره** — به‌جای confirm مرورگر، پنلِ واقعی زیرِ دارایی: انتخابِ بینِ «مشاورِ املاک» و «آژانسِ املاک» (پرسونای نام‌دارِ قطعی از هش — لیستِ agency به PRO_NAMES اضافه شد) + هزینه‌ها «به تومان» پیش از امضا (فروش: قیمتِ روز/کمیسیون/دریافتیِ خالص؛ اجاره: میانهٔ اجارهٔ واقعیِ محله/کمیسیونِ یک‌ماهه) از اکشنِ جدیدِ `agentQuote`؛ بعدِ امضا، نامِ طرف و کمیسیون در دفترچه ثبت می‌شود. ⛏ **کلنگِ رویایی (قانونِ ۱۳)**: پنلِ «رؤیای این زمین را بساز» — 🏷 نامِ پروژه (انتخابِ بازیکن، پیش‌فرض «برجِ [نامِ امپراتوری]») + 🎨 سبکِ نما (۴ گزینهٔ صرفاً ظاهری: مدرن/کلاسیک/رومی/سبز — BUILD_FACADES، صفر اثرِ اقتصادی) + جملهٔ رؤیا («X — N واحد با نمای Y در قلبِ محله») + **برآوردِ رؤیا از دادهٔ واقعی**: فروشِ کل و سودِ برآوردیِ هر گزینهٔ سازه/کیفیت از میانهٔ متریِ واقعیِ محله (estSale/estProfit با برچسبِ «برآورد — قولِ قطعی نیست»؛ بی‌داده = بی‌عدد)؛ نام و نما در Construction ذخیره و در سربرگِ کارگاه/تکمیل نمایش داده می‌شود، تایم‌لاین «کلنگ‌زنیِ «نام»» می‌گوید | این commit |

| ۴۴ | **پاسِ رویا و جذابیت روی فازهای قبلی (دستورِ مستقیم: «سندهای قبلی را هم برای رویابافی تغییر بده + مخاطبِ ۱۴+ جذابیتِ بیشتر می‌خواهد»)** — v1: 🏷 **نامِ دلخواه روی «هر» دارایی** (`nickname` + اکشنِ nickname + ✏️ در کارتِ پرتفوی — طلایی نمایش داده می‌شود و نامِ اصلیِ آگهی زیرش می‌ماند؛ هویتی، صفر اثرِ اقتصادی). 🌆 **خطِ آسمانِ جذاب‌تر**: رنگِ هر برج از نمای انتخابیِ خودِ بازیکن (مدرن/کلاسیک/رومی/سبز)، بلندترین دارایی 👑 «نگینِ امپراتوری» با قاب و هالهٔ طلایی، برچسبِ زیرِ برج = نامِ دلخواه/نامِ پروژه. 💼 **کارتِ پرتفوی**: هالهٔ سبز/قرمزِ ملایم از سود/زیانِ واقعی (یک نگاه، کلِ قصه) + آیکنِ بزرگ‌تر. ✨ میکروکپیِ رویایی در تیترِ تب‌ها («هر دارایی یک تکه از رؤیای توست»، «رؤیایت را از همین‌جا شروع کن»). ادامهٔ پاس (نقشه، مأموریت‌ها، بازار، لحظهٔ خرید) در فازهای بعدی طبقِ قانونِ ۱۳ | این commit |

| ۵۰ | **سند ۳۰ (فصل ۱۷ پارت‌های ۲۰–۲۴ + فصل ۱۸ Social + فصل ۱۹ اقتصادِ جوایز)**: 🏛 **تالارِ افتخارات** (Part 20 «The Hunt» — روان‌شناسیِ کلکسیون): کارتِ Hall of Fame در تبِ رتبه‌ها با ۳ بخش، همه از رفتارِ ثبت‌شدهٔ واقعی — **رکوردهای من** (`recordsOf`: گران‌ترین خرید/بزرگ‌ترین و پرسودترین پروژه/سودِ تحقق‌یافته/بردهای تالار/مذاکره‌ها/ققنوس؛ بی‌داده = بی‌رکورد)، **مجموعه‌ها** (`COLLECTIONS` ۵تایی: چهارگانهٔ املاک/فاتحِ محله‌ها/سلطانِ برج‌ها/شکارچیِ تالار/امپراتورِ درآمد — پیشرفتِ زنده + تکمیل = نشان که «عنوان» می‌شود؛ `applyCollections` در زنجیرهٔ stateOf کنارِ نشان‌های مخفی)، **گالریِ نشان‌ها** با انتخابِ عنوانِ فعال با یک کلیک. ⚡ **پیش‌بینیِ جایزه** (Ch19 Part 6 Reward Forecast + روان‌شناسیِ «Only X Left»): زیرِ نوارِ پیشرفتِ مسیرِ جوایز — «فقط X مانده · با سرعتِ رشدِ همین هفته‌ات (روزی ~Y)، برآورد ~N روز تا این مرحله» (`rewardForecastOf` از weekSnap «واقعی»؛ بدونِ رشدِ مثبت = هیچ ادعایی). 💢 **حریفِ قسم‌خورده** (Part 23 Nemesis — «دشمنی از برخوردهای واقعی، نه از پیش»): رقیبِ تالار که ≥nemesisWins بار (knob، پیش‌فرض ۳) از جلویش برده‌ای → چیپِ قرمزِ درخشانِ «💢 قسم‌خورده» در لابی + تیترِ دوئل («تمام رسانه‌ها رقابتِ تو و X را دنبال می‌کنند») + اعلامِ یک‌بارهٔ تایم‌لاین (`noteNemesis` — کلیدِ claims)؛ سوختِ انتقامِ فاز ۴۵ همان «دیگر اقتصادی فکر نمی‌کند» را واقعاً اجرا می‌کند. ۹ یونیت + ۴ PG جدید | این commit |
| ۶۲ | **سند ۳۱ (فصل ۲۰ — End Game، ۷ پارت)**: 🪜 **نردبانِ بی‌پایان / Role Evolution** (Part 1): `roleLayerOf` — ۸ لایهٔ نقش (معامله‌گر → مشاورِ بازار → شرکتِ ساختمانی → هولدینگ → سرمایه‌گذارِ کلان → سازندهٔ شهر → فاتحِ شهر → توسعه‌دهندهٔ افسانه‌ای)؛ هر لایه = آستانهٔ ارزشِ خالص (knob: endgame.l2..l8) + **شرطِ ساختاریِ واقعی** (پروژهٔ تحویلی/دارایی/بازارِ سرمایه/محله‌ها) تا «پول به‌تنهایی لایه نخرد»؛ نمایش در تالارِ افتخارات با شرطِ شفافِ لایهٔ بعد. 🏛 **شاخصِ میراث** (Part 2 Legacy Score): `legacyScoreOf` — ۶ جزء از دادهٔ ثبت‌شدهٔ واقعی (پروژه‌های تحویلی/دستمزدِ پرداختی/مالیات/کیفیتِ میانگینِ ساخت/تحسین/نشان‌ها) با وزن‌های knob؛ «دو نفر با ثروتِ برابر، میراثِ متفاوت». 🎞 **مستندِ مسیر** (Part 2 Player History — «انسان‌ها عاشقِ داستانِ خودشان‌اند»): `storyOf` — اولین رخدادِ هر نوع از تایم‌لاینِ واقعی، مرتبِ زمان، بدونِ ساختنِ چیزی. 🌍 **شگفتی‌های دنیا** (Part 4 World Wonders): ۶ نوعِ افتخارِ متفاوت (درآمد/سازنده/مزایده/محبوبیت/کارفرما/میراث — «فقط ثروتمندترین برنده نشود») روی رجیستریِ سراسریِ dual-mode (`.empire-wonders.json` / kv `empire_wonders`)؛ حداقلِ ثبت knob؛ **رکورد فقط با عددِ اکیداً بزرگ‌تر گرفته می‌شود** (`wondersCompute` خالص)، پلاکِ دارنده + تاریخچهٔ «دارندگانِ پیشین» (Former World Wonder) و جشنِ تایم‌لاینِ دارندهٔ جدید. 🌠 **موتورِ رؤیاها** (Part 7 Dreams Engine — قانونِ ۱۳): تختهٔ رؤیاها در تبِ مأموریت‌ها — **رؤیای شخصیِ خودِ بازیکن** (متریکِ واقعی از DREAM_METRICS + هدفِ عددی؛ سقفِ فعال knob) با نوارِ پیشرفتِ زنده از عددِ واقعی، تحقق = نشانِ اختصاصی + جشن (applyDreams در زنجیرهٔ stateOf)؛ + پیشنهادِ رؤیا از سبکِ واقعیِ خودِ بازیکن (`dreamSuggestionsOf` قاعده‌مندِ قطعی — بدونِ هزینهٔ AI). ۲۱ knob جدید در کارتِ جوایز/End Game ادمین. ۱۱ یونیتِ جدید | این commit |
| ۶۳ | **سند ۳۲ (فصل ۲۱ — Live World Engine، ۷ پارت)**: 📜 **کتابِ تاریخِ دنیا** (Part 1 World Memory + Part 2 Timeline + Part 7 History): استورِ dual-mode جدید (`empire-world.ts` — kv `empire_world`/فایل) که «فقط رخدادِ واقعاً رخ‌داده» را ثبت می‌کند: دست‌به‌دست‌شدنِ شگفتی‌ها (هوک در wondersUpdate)، تحویلِ برجِ تازه (تشخیصِ افزایشِ projectsDelivered در stateOf)، چکشِ تالارِ مزایده (مسیرِ خریدِ auction) — dedupe با روز+عنوان (ضدِ retry تراکنش/چند-instance)، سقف knob؛ نمایش: کارتِ «🗞 دنیای زنده» در تبِ شهر با **سالِ دنیا** (Part 7 World Age — از dayNumber، طولِ سال knob، نه Date.now) + آخرین رخدادها + کتابِ کاملِ تاریخ (details) + فیدِ ادمین. 🗞 **«در نبودِ شما»** (Part 2 Living World): گزارشِ واقعیِ غیبت در پیامِ خوش‌آمد — تغییرِ ٪ میانهٔ متریِ بازار از اسنپ‌شات‌های واقعیِ رصدخانه (فاز ۳۵) در بازهٔ غیبت + ۳ رخدادِ مهمِ دنیا در نبودش؛ بدونِ داده = بدونِ ادعا. 🌡 **دمای دنیا** (Part 5 Heat Index): `worldHeatOf` خالص از ۴ سیگنالِ واقعیِ امروز (بازیکنانِ فعال/رخدادهای دنیا/نبردهای زندهٔ تالار/رویدادِ استودیو) با وزن‌های knob → سرد/متعادل/داغ + «پیشنهادِ» قاعده‌مند به ادمین — فقط پیشنهاد، اجرا با ادمین (Level 0/1)؛ کارتِ 🌡 در LiveOps ادمین. 👂 **شایعاتِ منصفانه** (Part 3 Rumor System با هر ۳ شرطِ انصافِ خودِ سند): ۲ شایعه/هفته قطعی از هشِ هفته (قانون ۷) روی محله‌های واقعیِ رصدخانه؛ هر شایعه **منبع** (رسانه/تحلیلگر/رقیب) + **اعتبارِ ادعاییِ ٪** (باندِ knob)؛ بعد از ۷ روز با میانهٔ واقعیِ همان محله ارزیابی و ✓/✗ (حرکتِ زیرِ ۰٫۲٪ = «قضاوت نکن»)؛ **سابقهٔ درستیِ هر منبع** نمایش داده می‌شود تا بازیکن یاد بگیرد به چه منبعی اعتماد کند. ۱۰ knob جدید (`empire.world.*`). ۷ یونیتِ جدید | این commit |
| ۶۴ | **ممیزیِ کاربر («مزایده بین کاربرها نیست + پرتفوی همدیگر را ببینند»)**: 🔨 **مزایدهٔ بینِ بازیکنانِ واقعی** — مکملِ بازارِ ثابت‌قیمتِ فاز ۳۷: مالک دارایی را با «قیمتِ پایه + مهلتِ ۱..۷ روزه (سقف knob)» می‌گذارد (وسطِ ساخت/ماده۱۰۰ ممنوع)؛ بازیکنانِ واقعی روی هم پیشنهاد می‌دهند — هر پیشنهاد حداقل گامِ knob (۵٪) بالاتر و سرمایهٔ پیشنهاددهنده همان لحظه چک می‌شود؛ بعد از اولین پیشنهاد لغو ممکن نیست؛ **چکش** سرِ مهلت (تسویهٔ تنبل با اولین بازدیدِ فروشنده): بالاترین پیشنهادی که «سرِ چکش هم» پولش را دارد می‌بَرد — تسویه روی همان `tradeAsset` اتمیکِ دو-کاربره (auctionPrice) با مالیات/کمیسیون و بقای کاملِ پول + انتقالِ مالکیتِ انحصاری + ثبت در کتابِ تاریخِ دنیا؛ بدونِ برندهٔ توانا = بستهٔ صادقانهٔ بی‌نتیجه. UI: «🔨 مزایده» کنارِ عرضهٔ ثابت در پرتفوی + بخشِ «مزایده‌های زندهٔ بازیکنان» در بازارِ بازیکنان (بالاترین/مهلت/تعدادِ پیشنهاد + هوشِ قراردادِ فاز ۴۰ + هایلایتِ صدرنشینیِ خودم). 👤 **پروفایلِ عمومیِ کامل** — viewEmpire حالا **پرتفویِ کاملِ** بازیکن (تا ۲۰ دارایی: نام/نوع/محله/ارزشِ روز/واحدها/کسب‌وکار/در بازار/در مزایده) + لایهٔ نقشِ فاز ۶۲ + امتیازِ میراث + ۴ رکوردِ برتر را هم می‌دهد؛ مودالِ peek با گریدِ پرتفوی. knobها: `social.p2pAuctionEnabled/StepPct/MaxDays`. ۱۱ تستِ PG جدید (چرخهٔ کامل: پایه/گام/سرمایه/لغو/مهلت/چکشِ اتمیک/بی‌برنده) | این commit |
| ۶۵ | **صف #۱ تراکر — NPC Civilization نسخهٔ ۱ («شرکت‌های زندهٔ شهر»)**: 🏢 موتورِ جدید `empire-npc.ts` — همان شخصیت‌های تالارِ مزایده حالا «زندگی» می‌کنند: ۶ شرکتِ ثابتِ شخصیت‌دار (کامران سرمایه/گروهِ ناصری/شرکتِ افق/هولدینگ اطلس/گروهِ سپهر/آرمان‌سازه) با ۶ استایلِ رفتاریِ متمایز (تهاجمی/صبور/ارزش‌خر/لوکس‌باز/نوسان‌گیر/متعادل — باندِ قیمتیِ خرید و صبرِ نگه‌داری را عوض می‌کند)؛ **تیکِ روزانهٔ قطعی از هشِ (روز|شرکت)** (قانون ۷) روی آگهی‌های «واقعیِ» بازار: خرید در باندِ استایل، فروش بعد از دورهٔ نگه‌داری با «قیمتِ روزِ واقعیِ» همان آگهی → سود/زیانِ واقعی در دفترِ خودش؛ **پولِ NPC حلقهٔ کاملاً بسته** (تستِ بقا: سرمایه+بهای دارایی‌ها همیشه = سرمایهٔ شروع)؛ حداکثر یک حرکت/روز/شرکت، dedupe با d.day (ضدِ چند-instance). **رقیبِ واقعی برای بازیکن:** خریدِ NPC مالکیتِ انحصاریِ registry را می‌گیرد (`npc:*`) → کمیابیِ ملموس؛ اگر بازیکن ملکِ NPC را بخواهد، **به همان قیمتِ آگهی واگذار می‌شود** (بدونِ سورپرایزِ قیمتی — قانونِ ۴ سؤال) و دفترِ NPC فروش را ثبت می‌کند + رخدادِ کتابِ تاریخ؛ شکستِ خرید = ادعای NPC برمی‌گردد. معاملاتِ روزانهٔ شرکت‌ها به **کتابِ تاریخِ دنیا** می‌روند و کارتِ «🏢 شرکت‌های شهر» در «دنیای زنده» پرتفوی/نقد/سود-زیان/آخرین حرکاتِ هرکدام را شفاف نشان می‌دهد. knobs: `empire.npc.{enabled,count,startCapital,actChancePct,maxAssets}`. ۹ یونیتِ جدید (قطعیت، بقای پول، باندِ استایل، تک‌مالکی، dedupe، فروشِ سوددار، خاموشی) | این commit |
| ۶۶ | **صف #۲ تراکر — Season Engine نسخهٔ ۱ («فصلِ دنیا»)**: 🌱 فصلِ فعالِ knob-محور (`empire.season`: شناسه/نام/آیکون/داستان/روزِ شروع/طول/معیار/جوایزِ ۳ رتبه) — «هیچ متایی دائمی نیست»: ادمین با عوض‌کردنِ id فصلِ جدید را از LiveOps شروع می‌کند، بدونِ دیپلوی. **بیس‌لاینِ ورود** (`seasonSnap`): اولین دیدارِ بازیکن در بازهٔ فصل، ارزشِ خالص/پروژه/بردِ تالار/درآمدش ثبت می‌شود و پیشرفتِ فصل = **دلتای واقعی از همین نقطه** (شانسِ برابرِ تازه‌وارد — ثروتِ قبلی نمی‌شمرد؛ رشدِ منفی هم صادقانه نمایش داده می‌شود). ۴ معیارِ قابلِ‌انتخاب: growth/projects/auctionWins/income (`seasonValueOf` خالص). جدولِ فصل از اسنپ‌شاتِ روزانهٔ «واقعیِ» بازیکنان (بدونِ قیمت‌گیریِ سنگین). پایانِ فصل: نتیجه **یک‌بار در `empire_world` منجمد** می‌شود (`seasonFinalize` — set-once)، رتبه‌های ۱..۳ جایزهٔ کوین را با کلیدِ claims می‌گیرند (`claimSeasonReward` — بدونِ P2W، فقط کوین) و قهرمانِ فصل به کتابِ تاریخِ دنیا می‌رود. UI: کارتِ فصل در تبِ رتبه‌ها (داستان/مهلت/رتبه و مقدارِ من/جدولِ ۱۰تایی/دکمهٔ جایزه) + ۱۱ knob در LiveOps ادمین. ۳ یونیتِ جدید | این commit |
| ۶۷ | **صف #۳ — World Feed تعاملی (پیشنهادِ سند ۳۲ فصل ۲۱ Part 2)**: فیدِ کتابِ تاریخِ دنیا حالا «قابلِ‌تعامل» است — هر رخداد شمارهٔ امپراتوری/شرکتِ مرتبطش را دارد (`WorldEvent.no` در همهٔ نقاطِ ثبت: شگفتی/تحویل/چکشِ تالار/مزایدهٔ بازیکنان/قهرمانِ فصل/معاملاتِ NPC) و از همان‌جا: **👏 تبریک** (همان kudos یک‌بارهٔ فاز ۱۹ — بدونِ رفتن به لیدربورد) + **⭐ دنبال‌کردن** (`following` روی EmpireData با سقف؛ toggle؛ خود-دنبالی ممنوع؛ صفر اثرِ اقتصادی) — خبرِ دنبال‌شده‌ها در فید با قابِ طلایی و برچسبِ «⭐ دنبال‌شده» هایلایت می‌شود؛ دنبال‌کردنِ شرکت‌های NPC هم ممکن است (فقط هایلایت). «دلیلِ ورود دیگر فقط کار نیست؛ کنجکاوی است.» ۴ تستِ PG جدید | این commit |
| ۶۸ | **صف #۵ نسخهٔ v1 — چندشهری از دادهٔ واقعی (فصل ۲۱ P6 «Expansion» به صادقانه‌ترین شکل)**: 🏙 شهرها **داینامیک از locationِ آگهی‌های واقعی** (`cityOf`: «شهر، محله» → شهر؛ متای «شهر» مقدم) — نه جدولِ ساختگیِ شهرها: شهرِ جدید (کیش، مشهد و…) به‌محضِ رسیدنِ داده‌اش خودکار در دنیا ظاهر می‌شود و «شخصیتش» همان آمارِ واقعیِ بازارش است (`cityStatsOf`: تعدادِ آگهیِ فعال + میانهٔ قیمت، مرتب به اندازهٔ بازار) — کارتِ «🏙 شهرهای دنیا» در دنیای زنده. خرید حالا **شهرِ واقعیِ دارایی** را ثبت می‌کند (`EmpireAsset.city`؛ دارایی‌های قدیمی بدونِ شهر صادقانه شمرده نمی‌شوند) و مجموعهٔ ششم «**فاتحِ شهرها**» (داراییِ واقعی در ۲ شهرِ متفاوت → نشان/عنوان) به COLLECTIONS اضافه شد. فیلترِ فرصت‌ها به تفکیکِ شهر و «قوانین/اقتصادِ متفاوتِ هر شهر» → تریگرِ حجمِ دادهٔ چندشهری در تراکر می‌ماند. ۳ یونیتِ جدید | این commit |
| ۷۰ | **عقب‌افتاده‌ها — دولتِ زنده (فصل ۲۱ «Government هر روز تصمیم می‌گیرد» + Future Engine «آینده مبهم ولی اعلام‌شده») + بیمهٔ کارگاه (صفِ GDD)**: 🏛 **مصوبهٔ هفتهٔ شهر** — `govDecreeOf(week)` قطعی از هشِ هفته (قانون ۷) در دامنه‌های «محدودِ» knob (`empire.gov`: شانس/سقفِ دلتای مالیات/سقفِ دلتای نرخِ وام؛ گام‌های ربعِ واحد — تلنگرِ اقتصادی، نه شوک): این هفته یا مالیاتِ نقل‌وانتقال یا نرخِ وام کمی بالا/پایین می‌رود؛ **همیشه یک هفته زودتر اعلام می‌شود** (خطِ «📣 اعلامِ پیشاپیش — هفتهٔ بعد» در دنیای زنده — انصافِ خودِ سند: آینده مبهم ولی غافلگیریِ ناعادلانه ممنوع) + ثبت در کتابِ تاریخ. اجرا = نمایش: `effectiveTransferTaxPct()` در «همهٔ ۱۱ نقطهٔ» مصرفِ مالیات (خرید/فروش/معاملهٔ بازیکنان/مزایدهٔ P2P/واحدها/مشارکتِ جمعی/فروشِ پروژه) و دلتای وام مستقیم در `loanTermsFor` (همان نرخی که بازیکن می‌بیند همان اجرا می‌شود). 🛡 **بیمهٔ کارگاه** — `insureBuild`: حقِ بیمه = ٪knob از هزینهٔ ساخت (مصرفِ شفاف → دفترِ جدید `insurancePaid` — قانون ۶)؛ پوشش: هزینهٔ رویدادهای قطعیِ کارگاه coveragePct٪ کمتر (بعدِ تخفیفِ تیمِ ماهر؛ در متنِ رویداد شفاف نوشته می‌شود «بیمه X٪ را پوشش داد»)؛ دکمهٔ «🛡 بیمهٔ کارگاه» کنارِ شیفتِ شبانه در کارتِ ساخت؛ فقط پیش از وقوع. knobs: `empire.gov.*` (۴) + `empire.insurance.*` (۳). ۴ یونیتِ جدید | این commit |
| ۷۱ | **سند ۳۳ (نهایی — فصل ۲۲ World Architecture + فصل ۲۳ Fun/Dopamine/Zero-Dead-Time/Identity + اعلامِ Phase 2 Production Design با ۱۴+ فصلِ اجرایی)**: 📜 **دو قانونِ طلاییِ جدید ثبت شد** (قوانین ۱۴ و ۱۵ بالا — «Reality serves Fun» و «بازگشت از اشتیاق نه ترس» + اصلِ توجه). 📖 **کتابِ زندگی (The Biography — پیشنهادِ پایانیِ Identity Engine)**: `biographyOf` قاعده‌مند و قطعی (بدونِ هزینهٔ AI) — فصل‌های رواییِ واقعی: تولد/مسیر/رؤیا، اولین مالکیت، «سقوط و بازگشت» (بحران+ققنوس)، اولین برج، تالار، قهرمانیِ فصل، شگفتی + **Personality Evolution از رفتارِ واقعی** (وام+بیمه→«ریسکِ حساب‌شده»، نرخِ بردِ مذاکره، عاشقِ تالار، سازنده) + **آمار-به‌مثابهِ-داستان** («N واحدِ مسکونی تحویل داده‌ای» از projectHist و «N میلیون دستمزدِ تیم» — نه عددِ خشک)؛ بخشِ «📖 کتابِ زندگیِ تو» در تالارِ افتخارات؛ با هر نقطهٔ عطف، فصلِ تازه. 🎑 **Real World Integration lite**: `occasionOf` از تقویمِ «واقعیِ» فارسی (Intl fa-IR-u-ca-persian — بدونِ dep): نوروز/شبِ یلدا/پایانِ سال/جمعه → خطِ حال‌وهوا در دنیای زنده؛ فقط حس، صفر اثرِ اقتصادی؛ روزِ عادی = هیچ (صادقانه). ۵ یونیتِ جدید | این commit |
| ۷۲ | **ممیزیِ کاربر (صفحه‌بندی + صداقتِ بخش‌ها)**: 📑 **پایانِ اسکرولِ بی‌پایان** — هر تب زیرصفحه‌های کوتاه گرفت: شهر (⚡ امروز / 🔥 فرصت‌ها / 🏞 زمین و ساخت / 🎪 رویدادهای هفته / 🗺 نقشه و آسمان)، بازار (📈 سرمایه / 🏪 بازیکنان / 🏦 بانک / 🪙 فروشگاه)، رتبه‌ها (🏆 رقابت / 🏛 تالار / 🏰 اتحاد)، مأموریت‌ها (🎯/🎁/🌠) — چیپ‌ها با شمارِ «واقعی». 🌍 **تبِ جدیدِ «دنیا»** در منوی پایین: دنیای زنده + مصوبه + شایعات + شهرها + شرکت‌های شهر + روزنامه — از شکمِ تبِ شهر بیرون آمد. 🔨 **صداقتِ صحنه**: مزایدهٔ تمام‌شده دیگر تختهٔ مردهٔ نبرد (رقبای خط‌خورده + «چکش کوبیده شد») را نشان نمی‌دهد — کارتِ جمع‌وجورِ نتیجه + شمارش تا بازگشایی؛ حالتِ خالیِ صادقانه برای رویدادها/فرصت‌ها. 🔢 **اعدادِ بی‌معنا درست شد**: «سالِ ۲۳۰» → سالِ نسبی از **روزِ تولدِ دنیا** (`worldEpochOf` set-once در kv دنیا)؛ «روزِ ۲۰٬۶۴۵» فید → «امروز/دیروز/N روز پیش»؛ «مصوبهٔ هفتهٔ ۲٬۹۴۹» → هفتهٔ نسبیِ دنیا + ثبتِ یک‌بار-در-هفته؛ «دوامِ نقد ۱۸۶٬۱۹۲ روز» → «بیش از ۳ سال (خرجِ روزانه ناچیز)»؛ شمارشِ «۱۰۹:۱۱:۵۳» → «۴ روز و ۱۳ ساعت»؛ «برآورد: ؟؟؟» تالار → «برآورد: پنهان 🤫». 🧪 تست‌های مالیات مصوبه-آگاه شدند (نرخِ ثابتِ ۱٪ hardcode بود و با مصوبهٔ −۰٫۵ همین هفته می‌شکست). ۲ یونیتِ جدید | این commit |
| ۷۳ | **فیدبکِ کاربر (نقشه/مزایده/شرکت‌ها)**: 📍 **مختصاتِ ماندگارِ دارایی** — lat/lng آگهی در لحظهٔ خرید روی EmpireAsset ذخیره می‌شود (پینِ نقشه دیگر به زنده‌ماندنِ آگهی وابسته نیست؛ تخریب/تجمیع هم مختصات را نگه می‌دارد چون in-place است) + چیپِ نقشه صادقانه «X از Y» می‌گوید و توضیح می‌دهد داراییِ بدونِ مختصات چرا روی نقشه نیست. 🔨 **بازارِ بازیکنان با یک بازیکن هم زنده است** — عرضه/مزایده/مشارکتِ «خودت» هم با نشانِ «👑 مالِ تو» در فهرست دیده می‌شود (پیش‌تر e.userId===userId کلاً skip می‌شد → بازار همیشه خالی به نظر می‌رسید)؛ روی مالِ خودت دکمهٔ خرید/پیشنهاد نیست. 🤝 **خرید از شرکت‌های شهر بالاخره دکمه گرفت** — سرورش از فاز ۶۵ بود ولی هیچ UIای نداشت: در تبِ دنیا هر ملکِ شرکت با قیمتِ روزِ آگهیِ واقعی + دکمهٔ «بخر» (واگذاریِ شفاف به قیمتِ آگهی؛ آگهیِ رفته از بازار = «آگهی فعال نیست»). ۱ تستِ جدید | این commit |
| ۷۴ | **فیدبکِ کاربر (مأموریت‌های مخفی بن‌بست بود)**: 🕯 «۵ مأموریتِ مخفی در انتظارِ کشف…» هیچ جهتی نمی‌داد — حالا هر مأموریتِ کشف‌نشده یک **سرنخِ معمایی** دارد (بازشو با «سرنخ‌ها 🤫» در تالارِ افتخارات): جهت را می‌گوید، نام/شرط/عدد را لو نمی‌دهد («کسی که سرِ میزِ مذاکره کوتاه نمی‌آید…»). کشف همچنان فقط با رفتارِ واقعی و خودکار است (جلد ۲۶ + قانونِ ۱۴: کشف باید لذت بسازد، نه بن‌بست) | این commit |
| ۷۶ | **فیدبکِ کاربر (قانونِ ۴ — دورهٔ رویدادها knob نبود)**: ⏱ **دورهٔ برگزاریِ معاملهٔ بزرگ و تالارِ مزایده** حالا knob است (`bigDeal.periodDays` و `auction.periodDays`، پیش‌فرض ۷) — ۷=هفتگی، ۱=هر روز رویدادِ تازه؛ انتخابِ ملک/تخفیف/مهلت/«یک تلاش در دوره»/چرخشِ رقبا همه با همین دوره می‌چرخند. دو ردیفِ جدید در فرماندهی (بخشِ معاملهٔ بزرگ + تالار). عنوانِ کارت‌ها در بازی داینامیک شد («معاملهٔ بزرگِ هفته/روز/دورهٔ Nروزه» از periodFa سرور) و متن‌های «هفتهٔ بعد» → «دورِ بعد». چیپِ زیرصفحه → «رویدادها». (قبلاً هم قابلِ‌توجه: knobهای پارامتریِ هر دو رویداد از فاز ۴۱/۴۵ در فرماندهی بود — چیزی که نبود «دوره» بود.) | این commit |
| ۷۷ | **فیدبکِ کاربر (ممیزی: ردِ اشتباهِ «گول/نخور» + یادنگرفتن از اصلاحِ دستی + بی‌اندازه‌بودنِ ML)**: 🧠 **یادگیریِ اصلاحی** — برگرداندنِ تصمیمِ خودکار توسط ادمین حالا واقعاً مدل را اصلاح می‌کند: unlearn از کلاسِ غلط (۲×) + آموزشِ ۳برابری در کلاسِ درست (`correctFromAdmin`؛ حکمِ قبلی از items API پاس می‌شود). 🚫 **ردِ خودکار سخت‌گیرتر**: فقط نشانهٔ اسپمِ واقعی (شماره/لینکِ تماس در متن) مجوزِ ردِ بی‌انسان دارد — قیمتِ نامعتبر/توضیحِ کوتاه/شباهتِ واژه‌ای (اجارهٔ توافقی، «گولِ آگهیِ فیک را نخور») به صفِ بازبینیِ انسانی می‌رود نه رد. 🤖 پرامپتِ AI هم یاد گرفت که عبارت‌های بازاریابیِ رایج ادعای اصالت‌اند نه تقلب؛ مردد = review نه reject. 📊 **کارنامهٔ قابل‌اندازه‌گیریِ ML** در ممیزی: نمونه‌های هر کلاس، واژگانِ شناخته، **دقت روی داوریِ انسانیِ اخیر** (پنجرهٔ ۱۰۰تاییِ pred-vs-final — تنها معیارِ صادقانه)، شمارِ اصلاح‌های دستی، سهمِ تصمیمِ ML/AI، آموزشِ دستیِ کل. ۲ یونیتِ جدید | این commit |
| ۷۸ | **فیدبکِ کاربر (سه‌گانه)**: 🌍 **دنیا قابل‌کلیک شد** — چیپِ هر شهر → آگهی‌های واقعیِ همان شهر (`/listings?q=شهر`)، هر شایعه → «خودت قضاوت کن: آگهی‌های واقعیِ همان محله»، مصوبهٔ شهر → دو دکمهٔ اقدام (فرصت‌های امروز / بازارِ بازیکنان)؛ + خرید از شرکت‌ها (فاز ۷۳) و 👏/⭐ فید (فاز ۶۷) از قبل. 🤖 **معمای qwen حل شد**: متنِ خطای هر تماسِ ناموفق حالا در دفترِ مصرف ثبت و در «آخرین تماس‌ها» نمایش داده می‌شود (err) + **مدارشکنِ مدل** (۴ شکستِ پیاپی → ۱۰ دقیقه دورزدنِ مستقیم به fallback — دیگر هر درخواست یک تماسِ سوخته و تأخیرِ ۹۰ثانیه‌ای خرجِ مدلِ خراب نمی‌کند) + **حالتِ صرفه‌جویی قبل از اعمال، مدل را تستِ زنده می‌کند** و خراب را رد می‌کند (تا ۴ کاندید). 🗺 **انتخابِ شهرِ سایت**: هر ۳۱ استانِ ایران با شهرهایشان (تقسیماتِ رسمی) + جستجوی استان («مازند» → همهٔ شهرهای مازندران با سرصفحهٔ استان) | این commit |
| ۷۹ | **فیدبک (qwen هنوز + گیرِ «در حال اعمال»)**: ⏱ تستِ اتصالِ متن تایم‌اوتِ ۲۰ثانیه گرفت (`timeoutMs` در chatComplete/فکتوری aiFor) + **پاسخِ خالی = مردود** (قبلاً مدلِ خرابی که ۲۰۰ با متنِ خالی می‌داد «قبول» می‌شد)؛ حالتِ صرفه‌جویی AbortController ۲۵ثانیه‌ای سمتِ کلاینت دارد + شمارندهٔ «تستِ X از Y» — دکمه هرگز بی‌نهایت «در حال اعمال» نمی‌ماند. 🔁 `chatCompleteRaw` (مسیرِ چت/اعتبارِ کاربر) هم مدارشکن + ثبتِ متنِ خطا + fallback به mini گرفت — تجربهٔ چتِ کاربر پشتِ مدلِ خراب نمی‌میرد | این commit |

| ۴۹ | **دو فیدبکِ مستقیم (اثرِ واقعیِ تجمیع + نمایشِ اجارهٔ برج)**: 🧩 **واحدهای تجمیعی درآمد را ضرب می‌کنند** («۳ واحدِ دیگر خریدم، نه اجاره می‌شود داد نه کسب‌وکار — هیچ اثری ندارد»): فرمولِ درآمد به یک منبعِ واحدِ خالص (`assetMonthlyIncomeOf`) منتقل شد که هم واریزِ روزشمار (accrueRentFor) و هم نمایش (stateOf) از آن می‌خوانند — عددِ صفحه = عددِ واریز؛ حالا اجاره/کسب‌وکار × تعدادِ واحدهای مالک (۴ واحدِ کلینیک = ۴× درآمد) — پاداشِ واقعیِ تجمیع بدونِ مدیریتِ واحدبه‌واحد (قانونِ ۹۰/۱۰)؛ چیپِ وضعیت «در N واحد (درآمد ×N)» و سطرِ تجمیع «هر واحدِ تازه درآمد را هم ضرب می‌کند» می‌گوید. 🏙 **درآمدِ اجارهٔ برجِ تکمیل‌شده دیده می‌شود** («۱۲ واحد را اجاره دادم ولی نشان نمی‌دهد»): زیرِ سطرِ واحدهای پروژهٔ تکمیل‌شده، خطِ سبزِ «💰 اجارهٔ N واحد: ماهانه X · روزشمار روزی Y — تا امروز Z واریز شده · قسطِ بعدی تا H ساعت» (incomeSinceH به مسیرِ برج هم رسید)؛ بی‌نمونه = پیامِ صادقانه. ۶ یونیت جدید | این commit |

| ۴۸ | **جوایزِ پولِ واقعی + کیف‌پول (درخواستِ مستقیم: «کیف‌پولِ متصل به سایت + مرحله‌بندیِ فرمولی + ۵۰۰ خرج شد ۲۰۰ برگردد + تبِ تمیزِ سوپرادمین»)**: 🎁 **نردبانِ مرحله‌ای** (`empire-rewards.ts`): آستانهٔ مرحلهٔ k = ۱۰۰ میلیارد (ارزشِ خالص) × thresholdGrowth^(k-1) (پیش‌فرض ×۴ — «همه زود به مراحلِ بعد نرسند»)؛ جایزهٔ k = ۳م تومانِ «واقعی» × rewardGrowth^(k-1) (پیش‌فرض ×۱٫۵، با سقفِ ۲۰م) — دقیقِ خواستهٔ کاربر: مرحلهٔ ۱ = ۱۰۰ میلیارد → ۳ میلیون تومان. 💧 **استخرِ پایدارِ ساختاری**: سقفِ کلِ پرداختی = payoutPct٪ (پیش‌فرض ۴۰٪ = نسبتِ ۲۰۰ از ۵۰۰) از **درآمدِ واقعیِ تأییدشدهٔ درگاه** (هر verify موفقِ زرین‌پالِ کوین با همان authority ایدمپوتنت در `recordRealRevenue` ثبت می‌شود)؛ درخواستِ فراتر از ظرفیتِ آزادِ استخر (استخر − پرداخت‌شده − معلق) همان لحظه صادقانه رد می‌شود — هیچ بدهیِ معلقی ساخته نمی‌شود و مدل ریاضیاتاً هرگز بیشتر از سهمش نمی‌پردازد. 🛡 **گاردهای ضدسوءاستباه** (پولِ واقعی + خطرِ پمپِ ارزش با معاملهٔ P2P بینِ اکانت‌ها): حداقل سطح (۵) + حداقل سنِ اکانت (۷ روز) + سقفِ ماهانهٔ هر بازیکن (۱۰م) + مراحل فقط به‌ترتیب + هر مرحله یک‌بار (کلیدِ claims `rw_k`) + **هیچ پرداختی خودکار نیست — صفِ تأییدِ انسانی** با اسنپ‌شاتِ متریک‌های لحظهٔ ادعا (ارزشِ خالص/سطح/سنِ اکانت) جلوی چشمِ ادمین؛ تأیید → واریز به **سطلِ «پاداشِ» کیف‌پولِ یکپارچهٔ سایت** (`reos/wallet` — همان کیف‌پولِ چندسطلیِ REOS با دفترِ تراکنش) + خبر در دفترچهٔ بازیکن؛ شکستِ واریز → برگشتِ اتمیکِ تعهد (`revertApproval`). 👤 **UI بازیکن** (تبِ مأموریت‌ها): کارتِ «🎁 مسیرِ جوایزِ واقعی» — موجودیِ کیف‌پول، نوارِ پیشرفت به هدفِ بعدی، نردبان با وضعیتِ هر مرحله (قابلِ‌دریافت/در انتظار/واریز شد/قفل)، دکمهٔ دریافت → «پس از تأییدِ ملک‌جت به کیف‌پولت واریز می‌شود». 🖥 **تبِ اختصاصیِ سوپرادمین** «🎁 جوایزِ واقعی و کیف‌پول» (بخشِ جدیدِ EmpireAdminPanel + منوی empireRewards): ۵ کارتِ آمار (درآمد/استخر/پرداخت‌شده/معلق/ظرفیتِ آزاد) + صفِ تأیید/رد با یک کلیک + ۱۱ knob زنده + **پیش‌نمایشِ زندهٔ نردبان** با همان اعدادِ در حالِ ویرایش + جمعِ جایزهٔ تکمیلِ کاملِ مسیر + آخرین درآمدهای ثبت‌شده. ۷ یونیت + ۱۱ PG جدید | این commit |

| ۴۷ | **دو فیدبکِ مستقیم (شفافیتِ درآمد + گسترشِ کسب‌وکار + سود/زیانِ صندوق‌ها)**: 💰 **اجارهٔ روزشمارِ شفاف** («اجاره مبهم است»): stateOf برای هر داراییِ درآمدزا `incomeMonthly/incomeDaily/rentSampled/incomeSinceH` می‌دهد (دقیقاً همان فرمول‌های واریزِ accrueRentFor — عددِ نمایش = عددِ واریز)؛ چیپِ وضعیتِ اجاره حالا می‌گوید: «ماهانه X · روزشمار روزی Y — تا امروز Z واریز شده · قسطِ بعدی تا N ساعت»؛ بدونِ نمونهٔ واقعیِ اجاره در محله/شهر → پیامِ صادقانهٔ «فعلاً واریزی نداریم». 🏪 **درآمدِ معلومِ کسب‌وکار** («کلینیک زدم، معلوم نیست درآمد دارد یا نه»): همان چیپ برای کسب‌وکار (ماهانه = ۲× اجارهٔ میانهٔ محله × احتمالِ موفقیت) + احتمالِ موفقیت کنارِ نام. 🏢 **کسب‌وکارها از نقش‌های واقعیِ سایت** («تعدادشان کم است — کلِ نقش‌ها باشند»): `BUSINESS_TYPES` با ۱۴ نوع (مشاور املاک/آژانس/دفتر معماری/پیمانکاری/کارشناسی رسمی/دفتر حقوقی/دفتر اسناد رسمی/خدمات مالی و بیمه/فروشگاه مصالح + کافه/رستوران/فروشگاه/کلینیک/دفتر خدماتی)، هر کدام واژهٔ هم‌صنفیِ q برای شمارشِ رقبای «واقعیِ» محله از دایرکتوریِ سایت (فرمولِ احتمال همان قبلی — سیگنالِ واقعی)؛ فهرست از stateOf (`bizTypes`) به UI می‌رود. 📈 **سود/زیانِ صریحِ صندوق‌ها و استخرها** (پیامِ دوم: «کاربر نمی‌فهمد سود می‌کند یا ضرر»): زیرِ هر صندوق/استخرِ دارای سهم، جعبهٔ رنگیِ «پولی که دادی → ارزشِ الان → 📈 در سودی / 📉 در زیانی (X٪)» + یادآوریِ واریزِ جداگانهٔ سودِ اجاره؛ بالای بازارِ سرمایه چیپِ جمعِ کل («دادی X → الان Y · سود/زیان Z»). ۲ یونیت جدید | این commit |

| ۴۶ | **سه فیدبکِ مستقیمِ کاربر (نظم/مشاور/دفاع)**: 🗂 **بازچینیِ کارتِ دارایی** («گرافیک درهم است — نظم بده»): کارت از یک ردیفِ flex-wrap شناور به **سه سطرِ ثابت** تبدیل شد — سطر ۱ هویت و ارزش (راست: آیکن+نام+لقب+محله/خرید/درآمد؛ چپ: ارزشِ روزِ درشتِ طلایی + رشد٪)، سطر ۲ وضعیت/تصمیمِ جاری (مسیرِ زمین/کسب‌وکار/سه‌گانه)، سطر ۳ اکشن‌های همیشگی با جداکنندهٔ خط‌چین (آگهیِ واقعی، تحلیل، و فروش همیشه یک‌جا). دکمه‌های سه‌گانهٔ آیکنی برچسبِ متنی گرفتند («💰 اجاره با مشاور»). 🤝 **دیده‌شدنِ مشاور/آژانس** («همچنان نشان نمی‌دهد» — بخشی کدِ کهنهٔ سرور بود که با دیپلویِ ec7feb0 حل شد): دکمهٔ فروش صریح شد «🏷 فروش با مشاور/آژانس»، برگهٔ قرارداد قاب/سایهٔ طلاییِ پررنگ‌تر گرفت و بعد از بازشدن **scrollIntoView** می‌شود تا در موبایل زیرِ کارت گم نشود. ⚖️ **دفاع در کمیسیون قابلِ کلیک شد** («دفاع قابل کلیک نیست»): اعتراضِ پروانه حالا دوراهیِ واقعیِ قانونِ ۱۲ است — «🤝 توافق (غرامت → خزانه، بستنِ فوری)» یا «⚖️ دفاع در کمیسیون» (`defendObjection` — رایگان، +extraDays به بررسی، راهِ توافق بسته می‌شود، تایم‌لاین ⚖️ + دفترچه)؛ بعد از دفاع بنر «در انتظارِ رأیِ کمیسیون». اکشنِ `permitDefend` + گاردِ settleObjection. برچسبِ پیگیریِ کوینی خواناتر («یک روز جلوتر»). ۴ تستِ PG جدید | این commit |

| ۴۵ | **سند ۲۹ (فصل ۱۷ Parts ۱۴–۱۹ + بازطراحیِ کاملِ مزایده «Auction Saga» در ۵ پارت + جمع‌بندیِ نهاییِ خودِ سند)**: 🏛 **تالارِ مزایدهٔ هفته** — ستونِ تازهٔ گیم‌پلی، تک‌نفره در برابرِ رقبای شخصیت‌دار (نه NPC Civilization — «چند بازیگرِ آماری» طبقِ تصمیمِ معماریِ سند ۲۷): هفته‌ای «یک» ملکِ واقعی از **باندِ میانیِ بازار** (پرسنتایلِ ۲۰–۸۰؛ عمداً متمایز از سگمنتِ لوکسِ معاملهٔ بزرگ و بدونِ هم‌پوشانی با آن — excludeId)، انتخابِ قطعیِ شهری از هشِ هفته. **انواعِ مزایده** (Part 1 «همه یکسان نیستند»): بانکی/دولتی/شرکتی/ماترک/اضطراری — هرکدام قیمتِ شروعِ خودش (٪ knob-پایه از قیمتِ آگهی) و دولتی = نفوذپذیر. **«ارزشِ واقعی هیچ‌وقت گفته نمی‌شود»**: فقط برآوردِ بازه‌ای از نمونه‌های واقعیِ هم‌محله با برچسبِ «برآورد» (قانون ۱؛ بی‌نمونه = «؟؟؟» صادقانه)؛ قیمتِ آگهی تا لحظهٔ چکش پنهان است و آخرِ کار «رو می‌شود». **۵ رقیبِ ثابتِ شخصیت‌دار** (Part 5 — کامران عجول/ناصریِ صبور/افقِ وسواسی/اطلسِ منطقی/سپهرِ لحظهٔ‌آخر) با بودجهٔ **پنهانِ** قطعی از هش + **حافظه/انتقام**: هر بردِ قبلی از جلوی یک رقیب سقفش را revengePct٪ بالا می‌برد (تا ۳ برد — «دیگر اقتصادی فکر نمی‌کند»؛ چیپِ قرمزِ 😤 در لابی). **لابی** (Part 2): شایعه‌های قطعی دربارهٔ بودجهٔ رقبا که ~۴۰٪شان دروغ‌اند و بعدِ چکش راست/دروغشان رو می‌شود («هیچ خبری کاملاً قابلِ اعتماد نیست»). **نبردِ زنده** (Parts 3–4 + خوداصلاحیِ صریحِ سند «منوی Bid Style نه — سبک از رفتار تفسیر شود»): چهار حرکتِ طبیعی — پیشنهاد (گامِ knob)، حملهٔ سنگین (جهشِ knob — رقبای ترسو جا خالی می‌دهند)، صبر (مجری می‌شمارد: «بار اول… بار دوم…» — ۳ سکوت = چکش؛ رقبا شاید با هم بجنگند)، خروجِ آگاهانه («کنار کشیدن هم تصمیم است» — اگر برندهٔ رقیب گران‌تر از قیمتِ واقعی خریده باشد، بازی صادقانه می‌گوید برندهٔ واقعی تو بودی). واکنشِ رقبا قطعی از هشِ (کاربر+هفته+راند+حرکت)، شخصیت‌محور (عجول زود می‌پرد، صبور فقط نزدیکِ چکش)، حداکثر ۲ پیشنهادِ رقیب در راند (ریتمِ سند: «هر چند ثانیه یک اتفاق») + سرنخِ رفتاری به‌جای عدد (Reading The Room: «مدام با تلفن حرف می‌زند…»). **Auction Influence** (پیشنهادِ خودِ سند + Part 18 «بهترین امتیازها earned هستند»): ۰..۵٪ فقط از رفتارِ واقعی (فروشِ سودده ×۳ / پروژهٔ تحویلی / خوش‌حسابیِ بی‌دیرکرد / ققنوس / سطحِ ≥۱۰) — در مزایدهٔ دولتی سقفِ مؤثرِ رقبا را می‌تراشد؛ خریدنی نیست (قانون ۵). برد → `auctionWin` سمتِ سرور (ضدِ دستکاری، مثلِ bigDealWin) + خرید با پرچمِ auction روی مسیرِ اتمیکِ خرید به «قیمتِ چکش» (زیرِ قیمتِ آگهی = پاداش؛ بالاترش = ریسکِ واقعیِ گران‌خری) + تیترِ رسانه‌ایِ صادقانه («با هوش خرید» / «گران خرید») + نشانِ مخفیِ **«چکشِ طلایی»** + حافظهٔ رقبا++. یک ورود در هفته (کلیدِ claims — خودِ «شرکت کنم؟» اولین تصمیم)؛ رانِ نیمه‌کاره قابلِ ادامه؛ گاردِ سرمایه («پیشنهادی که نتوانی بپردازی، بلوفِ خطرناکی است»). UI سینمایی (قانونِ سند: «کاربر باید حس کند داخلِ فیلم است» + مخاطبِ ۱۴+ قانونِ ۱۳): سالنِ تیرهٔ گرادیانی با هالهٔ بنفش، قیمتِ درشتِ درخشان، چیپِ رقبا با حالتِ زنده (👑 صدرنشین / خط‌خورده «رفت»)، دفترِ وقایعِ زنده، دکمه‌های حرکت با مبلغِ دقیقِ تومانی. اکشن‌های `auction/auctionEnter/auctionMove` + ۱۱ knob (`empire.auction.*`) در کارتِ ادمین. ۲۱ یونیت + ۹ PG جدید | این commit |

### سند ۲۱ (فصل ۱۱ — Audio Design، بخش‌های ۱–۱۰)
✅ **فاز ۳۲ (قابل‌ساختِ الان):** بازخوردِ صوتی (Part 02/06) با سنتزِ WebAudio — بدونِ فایل، بدونِ حجم (Part 09 ✓)؛ تنظیماتِ کاربر: خاموش/روشن + حجم + تستِ فوری (Part 05 ✓)؛ دسترسی‌پذیری: همهٔ پیام‌ها متنی‌اند و هیچ اطلاعی فقط صوتی نیست (Part 08 ✓)؛ «هیچ صدایی فقط برای پرکردنِ سکوت نیست» (Part 10 ✓)؛ کلیدِ سراسریِ ادمین.
⏳ **باقی با تریگر:** موسیقیِ ده‌گانه/Dynamic Music (Part 01/07)، صداهای محیط و Spatial (Part 03)، گوینده/Voice چندزبانه (Part 04)، همگام‌سازیِ تنظیمات بینِ دستگاه‌ها (Cloud Save) → همه نیازمندِ اسِت‌های صوتیِ واقعی و کلاینتِ World/اپ (Visual Bible Pass 2 / نسخهٔ اپ) — سنتز جای موسیقی را نمی‌گیرد؛ فایلِ صوتی هم فعلاً نداریم و نمی‌سازیم.

### سند ۲۲ (فصل ۱۲ — Monetization، بخش‌های ۱–۱۰)
📜 **قوانینِ لازم‌الاجرا (Part 01/02/10 — با قوانینِ ۵/۶ تراکر هم‌راستا):** بدونِ Pay-to-Win؛ تمامِ محتوای اصلی بدونِ پرداخت؛ پرداخت فقط راحتی/شخصی‌سازی/زمان؛ Gold (=ملک‌کوین) هرگز Cash نامحدود/Reputation/Level/بردِ رقابتی نمی‌خرد؛ قیمت‌ها شفاف؛ هیچ خریدِ اجباری؛ اصلِ طلایی: «بازیکن باید بعد از خرید احساسِ رضایت کند».
✅ **فاز ۳۳ (قابل‌ساختِ الان):** فروشگاهِ ظاهری (Part 03 — قاب/نشان، صفر اثرِ اقتصادی، دیده‌شدن در لیدربورد/پروفایل)؛ Offer Engine (Part 09 — قطعی از رفتارِ واقعی، ۱/روز، قابلِ‌بستن، cooldown، بدونِ تایمرِ ساختگی)؛ بسته‌های زمان‌دارِ فروشگاهِ کوین (Part 07 — until واقعی از ادمین). Gold economy (Part 02) از فاز ۲۸ برقرار بود (زرین‌پال → ملک‌کوین → فقط سرعت/تحلیل/ظاهر).
⏳ **باقی با تریگر:** **Season Pass دوسطحی** (Part 04 — Free/Premium/Elite) → Season Engine/Quest Studio (صفِ #۲؛ بدونِ موتورِ فصل، Pass صادقانه نیست)؛ **VIP «حسابِ حرفه‌ای»** (Part 05 — Heatmap قیمت/پیش‌بینیِ بازار/هشدارِ فرصت/مقایسهٔ مناطق — «اطلاعاتِ بهتر، نه قدرت») → روی سیستمِ پلن‌های موجودِ سایت + فصلِ ۱۴ (Analytics)؛ **Rewarded Ads** (Part 06) → شبکهٔ تبلیغاتِ واقعی نداریم — هرگز تبلیغِ اجباری؛ **اسکینِ ساختمان/شهر و Event Store با Event Token** (Part 03/08) → Visual Bible Pass 2 + Season Engine؛ **بسته‌های مناسبتی (نوروز/یلدا) و Bundle شخصی‌سازی‌شدهٔ خودکار** (Part 07) → ادمین همین حالا پکِ زمان‌دار می‌سازد؛ نسخهٔ خودکارش با Offer Engine v2.

### سند ۲۳ (فصل ۱۳ — Technical Design، بخش‌های ۱–۱۰)
📜 **قوانینِ لازم‌الاجرا:** تمامِ منطق روی سرور، کلاینت فقط UI؛ هیچ Timestamp کلاینتی معتبر نیست؛ تراکنش‌ها Atomic؛ اولین تراکنشِ معتبر برنده؛ Rate Limit روی همهٔ Endpointها؛ Zero-Downtime Deploy؛ هیچ Feature بدونِ تست/مانیتورینگ؛ اصلِ طلایی: «هر میانبرِ امروز، هزینهٔ چندبرابرِ فردا».
✅ **از قبل برقرار (معماریِ فعلی):** منطقِ ۱۰۰٪ سرورساید (empire-store) + کلاینتِ صرفاً نمایشی ✓ · تراکنشِ اتمیک با قفلِ `FOR UPDATE` در PG (kvMutate) و «اولین برنده» ✓ · dayNumber سرورساید (قانون ۹) ✓ · Auth روی همهٔ اکشن‌ها ✓ · بکاپِ روزانهٔ PG+فایل (backup.sh + cron) ✓ · دیپلویِ بدونِ داون‌تایم (pm2 reload + health-check + build با nice) ✓ · Rollback = git revert + deploy ✓ · مانیتورینگ (پنلِ سلامت + pm2 + nginx LB) ✓ · Crash-report کلاینت (ErrorFence + /api/client-log → pm2) ✓ · رگرسیون = ۸۲۱ تست + دودآزمای ۴۷ نما ✓ · CDN/Cache (آروان + کش‌های in-memory) ✓.
✅ **فاز ۳۴ (قابل‌ساختِ الان):** Economy QA خودکارِ پیش از انتشار (Part 09) + Rate Limit با knob (Part 04).
⏳ **باقی با تریگر (معماریِ هدفِ مقیاسِ میلیونی — الان over-engineering است):** میکروسرویس‌ها/K8s/Kafka/Service Mesh (Part 01/02/05) → تریگر: ترافیکِ پایدارِ ده‌ها هزار کاربرِ همزمان یا تیمِ چندنفره؛ ClickHouse/OpenSearch/Redis جدا (Part 03) → فصلِ ۱۴ (Analytics) + رشدِ داده؛ Envelope یکسانِ Response + requestId + WebSocket/Delta-Sync (Part 04/07) → بازنویسیِ شکنندهٔ همهٔ APIها، فقط همراهِ نسخهٔ اپ؛ اپِ Flutter چندسکویی + SSL Pinning/Anti-Tamper + Asset Streaming (Part 06/08) → تصمیمِ محصولِ «اپِ موبایل»؛ Closed/Open Beta و تستِ ده‌هزارنفره (Part 09) → وقتی جمعیتِ واقعی داریم. (وب‌سایتِ فعلی خودش «کلاینتِ وب» است — سرور مرجعِ همه‌چیز.)

### سند ۲۴ (فصل ۱۴ — Analytics & Big Data، بخش‌های ۱–۱۰)
📜 **اصلِ طلایی:** «آنچه اندازه‌گیری نشود، قابل‌بهبود نیست» — هیچ تصمیمِ مهمی بدونِ داده؛ هیچ دادهٔ شخصیِ غیرضروری.
✅ **از قبل برقرار:** ثبتِ رویدادِ رفتاری با timestamp (REOS event log — Part 02) · Player DNA/پروفایلِ رفتاری (identity/dna از فاز ۱ — پیشنهادِ Part 05 ✓) · Feature Store و آموزشِ ML درون-استک از رویدادهای واقعی (reos/features + train + automl — Part 09 v1) · champion/challenger و فلگ‌ها (زیرساختِ v1 آزمایش — Part 08) · کنسولِ تعامل: DAU/WAU/MAU/Retention/churnRisk (فاز ۱۰ — Part 07 v1) · Comeback Engine (اقدامِ بازگشتِ Part 07) · داشبوردهای مدیریتی: فرماندهی/اقتصاد/سرمایه/LiveOps/سلامتِ سیستم (Part 04 نسخهٔ واقع‌بینانه).
✅ **فاز ۳۵ (قابل‌ساختِ الان):** پیشنهادِ طلاییِ Part 01 «تاریخچهٔ کاملِ بازار» + سلامتِ اقتصادِ Part 06 + IESِ Part 07 → رصدخانهٔ اقتصاد (بالا). صفِ قدیمیِ «price_history» با همین فاز باز شد (تاریخچهٔ میانهٔ متری روزانه ذخیره می‌شود).
⏳ **باقی با تریگر:** ClickHouse/Kafka/Data Lake/Stream Processing (Part 03) → حجمِ میلیونیِ رویداد؛ A/B Testing کامل با گروه‌بندیِ تصادفی (Part 08) → جمعیتِ آماریِ کافی (سنجهٔ فعلی: DAU رصدخانه)؛ Churn/LTV Prediction با مدلِ ML (Part 07/09) → داده و جمعیتِ کافی (نسخهٔ قاعده‌مندِ churnRisk هست)؛ Dashboard Builder (Part 04 پیشنهاد) → وقتی داشبوردهای ثابت کم بیایند؛ per-property price history و Session-tracking دقیقِ کلاینت (Part 02 click/swipe/zoom) → با نسخهٔ اپ/Analytics DB جدا؛ رعایتِ ناشناس‌سازی (Part 10) در رصدخانه برقرار است (فقط جمع‌ها ذخیره می‌شوند، نه رفتارِ فردی).

### سند ۲۵ (فصل ۱۵ — AI Platform Architecture، بخش‌های ۱–۱۰)
📜 **دکترینِ مرکزیِ سند (لازم‌الاجرا — از روزِ اولِ امپراتوری برقرار بوده):** «LLM هرگز تصمیمِ اقتصادی نمی‌گیرد؛ فقط توضیح می‌دهد» — هستهٔ تصمیم = ML/آمار/قواعدِ قطعی. AI کمک‌کننده است نه جایگزینِ بازیکن؛ همهٔ پیشنهادها قابلِ‌رد؛ بدونِ برتریِ ناعادلانه.
✅ **از قبل برقرار:** همهٔ مکانیک‌ها قطعی/قاعده‌مند (هش/فرمول/میانهٔ واقعی) و GapGPT فقط تولیدِ متن ✓ · Feature Store و آموزشِ وزن‌ها از رویدادهای واقعی (reos/features + train — Part 02/03 v1) ✓ · champion/challenger با promote/rollback خودکار (automl = همان Shadow/Canary — Part 03 Stage 7/8 v1) ✓ · Orchestrator/Gateway (reos/orchestrate + gateway — Part 01 v1) ✓ · Kill Switch (فلگِ empire + فلگ‌های لایه‌های REOS — Part 10) ✓ · Explainability (ستونِ «چرا» + اطمینان٪ + دلایلِ عددی — فاز ۱۱) ✓ · Governance عملی: Level 0/1 (اقتصاد فقط دستِ ادمین)، Level 2 (پیشنهادِ قابلِ‌رد)، Level 3 (فقط ترتیبِ نمایش) ✓ · پایشِ سلامتِ اقتصاد (رصدخانهٔ فاز ۳۵ = AI Economy Monitor نسخهٔ قاعده‌مند) ✓ · ضدسوءاستفاده (rate-limit + suspension + دودآزما) v1 ✓.
✅ **فاز ۳۶ (قابل‌ساختِ الان):** Human Dashboard + نگاشتِ سطوحِ اختیار + قانونِ اساسیِ AI (بالا).
⏳ **باقی با تریگر:** **RL Platform + Digital Twin اقتصاد + Continuous Simulation** (Part 06/09) → نیازمندِ جمعیت/دادهٔ کافی و زیرساختِ شبیه‌سازی — تا آن روز، Economy QA و رصدخانه همان نقشِ محافظ را قاعده‌مند بازی می‌کنند؛ **World Model سه‌سطحی** (Part 08) → نسخهٔ عملیِ فعلی = رصدخانه (Global) + hoodPerM (Regional) + EmpireData (Player)؛ گرافِ کامل با NPC Civilization؛ **Multi-Agent runtime + پرسوناهای Agent** (Part 07) → وقتی چند مدلِ واقعیِ مستقل داشتیم — الان تفکیکِ ماژولی همین است (store/market/engage/metrics)؛ **Online Inference دو-خوشه‌ای CPU/GPU + AI Budget + Prediction Cache** (Part 05) → با آمدنِ مدل‌های سنگین/GPU؛ **Model Registry مستقل + نسخه‌بندیِ تصمیم‌ها** (Part 04) → با چندمدلی‌شدنِ واقعی؛ **Churn/LTV/Fraud ML** (Part 01) → دادهٔ کافی (نسخهٔ قاعده‌مندِ churnRisk/suspension هست). فصلِ بعدیِ پیشنهادیِ سند: AI Product Layer (قابلیت‌های رو-به-کاربر).

### سند ۲۶ (فصل ۱۶ — Cognitive AI Platform، Partهای ۰۱–۱۲)
📜 **جمع‌بندیِ نهاییِ خودِ سند (بعد از چند خوداصلاحی):** نه «پلتفرمِ AI عمومی/COS»، نه ده‌ها فیچرِ هم‌پوشان — چند سیستمِ هوشِ قابل‌ساختِ داخلِ بازی؛ قوانین: AI تصمیم نمی‌گیرد فقط پیشنهاد می‌دهد؛ بدونِ حدس (همه از ML/آمار/دادهٔ واقعی)؛ بدونِ دادهٔ کافی اعتمادِ بالا ممنوع؛ هر پیشنهاد دلیل دارد؛ آینده هرگز قطعی اعلام نمی‌شود؛ هیچ اسمِ شخصیتِ ثابتی برای AI (با قانونِ ۲ تراکر هم‌راستا — دستیار همان «ملک‌جت» است).
✅ **از قبل برقرار:** فرصت‌های شخصیِ روزانه (Part 06 نسخهٔ Hook — فاز ۱۶: dailyDeals قطعی و قضاوت‌نکرده) · مذاکرهٔ هوشمند (Part 07 — فاز ۷/۱۲/۱۶/۲۹: شخصیتِ مالک، حافظهٔ مذاکره، اثرِ اعتبار) · بریفینگِ روزانه (Part 02 «Daily Executive Briefing» — نامهٔ ملک‌جت/گزارشِ صبح از فاز ۲/۴) · تحلیلِ پروژه پیش از ساخت (Part 09 Project Planner — buildPlan/designPlan فازهای ۱۳/۲۹ با هزینه/زمان/سود/سناریو) · کارنامهٔ پروژه (گزارشِ پایانِ Part 09 — projectHist فاز ۱۵) · گزارشِ مالیِ شرکت (فاز ۱۴) · Explainability سراسری (فاز ۱۱).
✅ **فاز ۳۹ (قابل‌ساختِ الان):** ارزش‌گذاریِ ملک (Part 03) + تصمیم‌یارِ خرید (Part 05) + هوشِ بازار از تاریخچهٔ واقعی (Part 04) + جریانِ نقدی/سلامتِ مالی (Part 10) + اولویت‌های امروز (Part 11) — همه در بالا.
⏳ **باقی با تریگر:** **گفت‌وگوی آزاد با Copilot / Scenario و Goal Planning زبانی** (Part 01 — «۵ میلیارد دارم چه بخرم؟») → LLM فقط روی خروجیِ همین توابعِ قطعی متن بسازد (GapGPT)؛ دکترینِ سند ۲۵ رعایت می‌شود ولی UX چت نیازمندِ طراحیِ جدا؛ **Market Narratives / Opportunity Forecast** (Part 04/06 پیشنهادها) → نیازمندِ تاریخچهٔ چندهفته‌ایِ واقعیِ رصدخانه — با انباشتِ اسنپ‌شات‌ها بازمی‌گردد؛ **AI Memory/Player Intelligence کامل + Investment DNA/مربی** (Part 08 — خودِ سند نصفش را «ایده، نه فیچر» خواند) → نسخهٔ v1 همان identity/dna/IES موجود است؛ **تحلیلِ رفتارِ فروشندهٔ واقعی/احتمالِ پذیرش در مذاکره** (Part 07 تکمیلی) → دادهٔ رفتاریِ مالکانِ واقعی نداریم — عددِ ساختگی نمی‌سازیم (قانون ۱)؛ **NPC Activity در تحلیل‌ها** (Part 01/04) → قفل به NPC Civilization (صفِ #۱).

### سند ۲۷ (فصل ۱۶ — Parts ۱۳–۲۳: خودکارسازی، گزارش، NPC/رویداد/رسانه/رقبا/شهر/شهروندان، دستیار، قرارداد، اعتبار)
✅ **فاز ۴۰ (قابل‌ساختِ الان):** مرکزِ خودکارسازی (Part 13) + هوشِ قرارداد (Part 21) — بالا.
✅ **از قبل برقرار:** مرکزِ گزارش (Part 14) → گزارشِ مالیِ شرکت (فاز ۱۴) + کارنامهٔ پروژه (فاز ۱۵) + پرتفوی با فیلتر/مرتب‌سازی (فاز ۲۱) + رصدخانهٔ ادمین (فاز ۳۵)؛ خلاصهٔ مدیریتیِ ابتدای گزارش = بریفینگ/اولویت‌های فاز ۳۹ · شبکهٔ خبری (Part 17) → روزنامهٔ ملک‌جت (فاز ۱۰ — newsOf فقط از رویدادهای واقعی؛ قانونِ «هیچ خبرِ ساختگی» از روزِ اول) + اخبارِ شخصی = نامهٔ روزانه/تایم‌لاین · دستیارِ اجرایی (Part 20) → اولویت‌های حداکثر-۵ با ترتیبِ بحرانی‌اول (فاز ۳۹) + Offer Engine قابل‌بستن (فاز ۳۳) · اعتبار و اعتماد (Part 22) → اعتبارِ ⭐ شرکت از رفتارِ واقعی با اثرِ واقعی روی مذاکره/نرخِ وام (فازهای ۱۲/۱۶) + trust حرفه‌ای‌های سایت؛ «جبران‌پذیری» برقرار است (ستاره از رفتارِ جاری بازمی‌گردد).
⏳ **قفل به NPC Civilization (صفِ #۱ — تصمیمِ معماری این‌جا ثبت شد):** NPCهای بازار (Part 15)، رویدادهای پویا با اثرِ قیمتی (Part 16 — قیمت‌های ما از آگهی‌های «واقعی» می‌آیند؛ رویدادِ مصنوعیِ قیمت‌ساز = نقضِ قانونِ ۱؛ نسخهٔ بی‌خطرش: استودیوی رویدادِ فاز ۲۰ روی پاداش/مأموریت)، رسانهٔ اثرگذار بر تقاضا (Part 17 پیشنهاد)، شرکت‌های رقیب (Part 18)، تکاملِ شهر (Part 19 — نسخهٔ واقعی‌اش همین حالا هست: ارزش/عرضهٔ محله‌ها از بازارِ واقعی هر روز تغییر می‌کند و در روندِ محله‌ها دیده می‌شود؛ نسخهٔ شبیه‌سازی‌شده NPC می‌خواهد)، رفتارِ شهروندان (Part 23). **پاسخِ سؤالِ معماریِ خودِ سند (۱۰۰هزار NPC یا چند صد + آمار؟): گزینهٔ ۲** — تقاضا/عرضهٔ MelkJet از دادهٔ واقعیِ بازار می‌آید و NPC فقط مکملِ حسِ رقابت است؛ چند صد بازیگرِ آماری کافی است و قانونِ ۱ (بدونِ دادهٔ ساختگی در نقشِ دادهٔ واقعی) خط‌کشِ پیاده‌سازی‌اش خواهد بود.
⏳ **باقی با تریگر:** خروجیِ PDF/Excel و گزارشِ بازهٔ دلخواه/ذخیره‌شده (Part 14) → نیازِ واقعیِ کاربر؛ یادگیریِ دستیار از ردکردن‌ها (Part 20) → دادهٔ کافیِ رفتاری؛ Ruleهای ترکیبی (و/یا — Part 13 Rule Builder کامل) → اگر قالب‌های ساده کم آمدند.

### سند ۲۸ (فصل ۱۷ — Marketing System تا Crisis: Parts ۱–۱۳ + پیوتِ «Tycoon نه ERP»)
📜 **پیوتِ تعیین‌کنندهٔ سند (بعد از Part 6):** قانونِ ۹۰/۱۰ + چهار شرطِ هر سیستم (تصمیم/ریسک/پاداش/لذت) — به‌عنوانِ **قانونِ ۱۲** تراکر ثبت شد؛ «اگر کاری برای مدیرعاملِ واقعی خسته‌کننده است، برای بازیکن هم هست».
✅ **از قبل برقرار (Partهای ERPگونهٔ ۱–۶ → برای متخصصانِ واقعیِ سایت ساخته شده‌اند، نه گیم‌پلی):** Marketing (کمپینِ SMS/ایمیل با AI + سیستمِ promotion/featured) · CRM (فازهای Sales OS: پروفایل/تایم‌لاین/امتیاز/فیلتر/فایل) · Sales Pipeline (کانبانِ کشیدنی + احتمالِ خرید + هشدارِ رکود) · Property Listing (هستهٔ سایت) · Contract (نسخهٔ بازی: هوشِ قراردادِ فاز ۴۰) — طبقِ قانونِ ۹۰/۱۰ این‌ها نباید گیم‌پلیِ بازیکن شوند. Opportunity Discovery با تحقیقِ پولی (Part 10 Due Diligence = ژتونِ تحلیل) و شخصی‌سازی از رفتار (identity/suggest) هم از قبل بود.
✅ **فاز ۴۱ (قابل‌ساختِ الان):** معاملهٔ بزرگِ هفته (Part 7) + کمیابیِ فرصت‌ها (Part 10) + اتاقِ بحران/ققنوس (Part 13) — بالا.
⏳ **باقی با تریگر:** **Bidding War بینِ بازیکنان** (Part 8) → نسخهٔ تک‌نفرهٔ کاملش در فاز ۴۵ ساخته شد (تالارِ مزایدهٔ سند ۲۹)؛ نسخهٔ چندبازیکنهٔ همزمان به WebSocket/جمعیت قفل است؛ **مذاکرهٔ چندبعدیِ کامل با زبانِ بدن/بلوف** (Part 9) → نسخهٔ فعلی: تیپِ مالک + حافظه + استراتژیِ Big Deal؛ نسخهٔ چندمرحله‌ای با UX جلسه بعد از بازخوردِ واقعیِ بازیکنان؛ **Secret Opportunities/زنجیرهٔ سرنخ** (Part 11) → نیازمندِ Quest Studio (صفِ #۲) و رسانهٔ اثرگذار؛ **Hostile Takeover / Boss Battle شرکت‌ها** (Part 12) → تصاحبِ شرکتِ «بازیکنانِ واقعی» مسئلهٔ رضایت/انصاف دارد و تصاحبِ NPC قفل به NPC Civilization (صفِ #۱)؛ رزرو/بیعانه (Part 5) → اگر روزی لازم شد، وضعیتِ due-pending روی مالکیتِ انحصاری.

### سند ۲۹ (فصل ۱۷ Parts ۱۴–۱۹ + بازطراحیِ کاملِ مزایده «Auction Saga» + جمع‌بندیِ نهاییِ خودِ سند)
📜 **دو خوداصلاحیِ تعیین‌کنندهٔ سند (لازم‌الاجرا):** ① Part 17 «هیئت‌مدیره» را خودِ سند رد کرد («دوباره از ذهنِ یک طراحِ ERP فکر کردم») و قانونِ تازه گذاشت: **«اگر این بخش را حذف کنیم، آیا بازی سرگرم‌کننده‌تر می‌شود؟»** — به‌جایش «لحظه‌های خاص» (خیانت/فرارِ سرمایه‌گذار/دزدیدنِ مدیر) که همه به NPC قفل‌اند. ② در طراحیِ مزایده: **«منوی Bid Style نسازید — سبک باید از رفتارِ طبیعیِ بازیکن تفسیر شود»** و **«هر ۱۰–۲۰ ثانیه یک تصمیم یا اتفاق»** و **«کاربر باید حس کند داخلِ یک فیلم است، نه فرمِ ثبتِ عدد»**.
✅ **فاز ۴۵ (قابل‌ساختِ الان):** تالارِ مزایدهٔ هفته — کلِ چرخهٔ «قبل/داخل/بعد» از جمع‌بندیِ نهاییِ خودِ سند: اطلاع‌رسانی و تحقیق (لابی + برآوردِ بازه‌ای + شایعه) / رقابتِ زنده با تایمرِ چکش و ورودِ/خروجِ رقبا و بلوفِ طبیعی / بعدِ مزایده: انتقالِ مالکیت، واکنشِ رسانه‌ای، اعتبار (نفوذ)، حافظهٔ رقبا — بالا. قوانینِ اصلیِ سند کد شد: «بردن همیشه بهترین نتیجه نیست» (گران‌خری = تیترِ منفی)، «باختن همیشه بدترین نیست» (خروجِ به‌موقع = تأییدِ صادقانه)، «قیمت تنها عاملِ برد نیست» (نفوذِ earned در دولتی)، «هیچ دو مزایده‌ای شبیه هم» (نوع/رقبا/سقف/شایعه از هشِ هفته+آگهی).
✅ **از قبل برقرار:** رویداد/فاجعهٔ غیرمنتظره (Part 16) → استودیوی رویدادِ فاز ۲۰ (پاداشی، بدونِ دستکاریِ قیمتِ واقعی — قانونِ ۱) + بحرانِ فاز ۴۱ (بحران از تصمیمِ خودِ بازیکن) + رویدادهای قطعیِ کارگاه (فاز ۱۳)؛ «کارتِ تصمیم» = الگوی موجودِ رویدادِ کارگاه/پیشنهادِ قابلِ‌رد؛ Legacy/میراث (Part 15) نسخهٔ هویتی → نامِ پروژه/لقبِ دارایی/نگینِ امپراتوری/تایم‌لاینِ زندگی (فازهای ۴۳/۴۴) + projectHist؛ شبکهٔ اطلاعات (Part 18) نسخهٔ صادقانه → شایعاتِ لابیِ همین فاز + روزنامهٔ ملک‌جت (فقط رویدادِ واقعی) + intel فاز ۳۹؛ «تماسِ نیمه‌شب باید نتیجهٔ رفتار باشد نه تصادف» (Part 19 خوداصلاحی) → suspense/نامهٔ روزانه از وضعیتِ واقعیِ خودِ بازیکن.
⏳ **باقی با تریگر:** **Corporate War چندمسیره** (Part 14 — جنگِ قیمتی/رسانه‌ای/استعداد؛ برد از ۳ مسیر) → قفل به NPC Civilization (صفِ #۱؛ جنگ با «بازیکنِ واقعی» مسئلهٔ انصاف/رضایت دارد — همان تصمیمِ Hostile Takeover سند ۲۸)؛ **پروژه‌های Legacy چندهفته‌ای با فازبندی و رویدادِ شهری** (Part 15 کامل) → Season Engine (صفِ #۲) + جمعیتِ فعال؛ **لحظه‌های خاصِ سینمایی** (جایگزینِ Part 17: خیانتِ شریک/فرارِ سرمایه‌گذار/دزدیدنِ مدیرِ ارشد) → NPC + نظامِ روایت؛ **شبکهٔ خبرچینِ کامل** (Part 18: سرچشمه‌ها/کیفیت/لو رفتن) و **تماسِ نیمه‌شب** (Part 19: تایمرِ تصمیمِ کوتاه/تماسِ جعلی/افسانه‌ای) → NPC + رسانهٔ اثرگذار؛ **مزایدهٔ چندبازیکنهٔ همزمانِ زنده** (رقابتِ real-time بینِ بازیکنانِ واقعی، Auction Season/Festival/سالنِ گرافیکیِ قدم‌زدنی) → WebSocket/جمعیتِ همزمان + Visual Pass 2 — نسخهٔ تک‌نفرهٔ فعلی عمداً همان مکانیک را با رقبای آماری می‌سازد تا مسیرِ ارتقا به چندنفره باز بماند (exclusivity و claims از الان مشترک‌اند)؛ **مزایدهٔ محرمانه/دعوت‌نامه‌ایِ VIP** (Part «Secret Auctions») → با VIP/حسابِ حرفه‌ای (سند ۲۲ Part 05).

### سند ۳۰ (فصل ۱۷ پارت‌های ۲۰–۲۴ + فصل ۱۸ Social آغاز + فصل ۱۹ Game Economy & Reward Engine)
📜 **هم‌گراییِ تعیین‌کننده:** طرحِ تفصیلیِ «Dynamic Reward Economy» این سند (استخر = ٪ از درآمدِ واقعی، نسبتِ ۵۰۰→۲۰۰، تبِ مستقلِ سوپرادمین با Pool/Paid/Remaining، ضدفارم/چنداکانت، Manual Review، کیف‌پولِ چندسطلی، نمایشِ Progress و جشن) **پیش از رسیدنِ سند، در فاز ۴۸ ساخته شده بود** — انطباق تقریباً یک‌به‌یک است؛ فقط پیشنهادهای تکمیلی‌اش (Forecast و «Only X Left») در فاز ۵۰ اضافه شد.
✅ **فاز ۵۰ (قابل‌ساختِ الان):** تالارِ افتخارات/مجموعه‌ها (Part 20) + پیش‌بینیِ جایزه (Ch19 Part 6) + حریفِ قسم‌خورده (Part 23) — بالا.
✅ **از قبل برقرار:** Reward Pool/Wallet/تبِ ادمین/تأییدِ انسانی (فاز ۴۸) · عناوینِ کسب‌شدنی (فاز ۱۸ + مجموعه‌های فاز ۵۰) · رکوردها/روزنامهٔ اخبارِ «فقط مهم» از رویدادِ واقعی (فاز ۱۰ — همان قانونِ «حسِ وای» فصل ۱۸ Part 1) · Smart Offer v1 قطعی از رفتارِ واقعی + قابلِ‌بستن + cooldown (فاز ۳۳ — Part 2 فصل ۱۹) · قوانینِ Fair Monetization (Part 3 = قانونِ ۵ تراکر از روزِ اول؛ «پول سرعت می‌خرد نه پیروزی») · Player Marketplace درون‌بازی با کارمزد/مالیات (فاز ۳۷ — نسخهٔ ارزِ داخلی که خودِ سند هم برای پولِ واقعیِ P2P ممنوعش می‌کند) · PGO نسخهٔ واقعی (فصل ۱۸ Part 2): مالکیتِ انحصاری/بازارِ بازیکنان/رقابتِ معاملهٔ بزرگ و تالار همین حالا فرصت‌ساز بینِ بازیکنانِ واقعی‌اند · ECS نسخهٔ v1 = IES + نفوذِ کسب‌شده + امتیازِ امپراتوری (همه از رفتارِ واقعی).
⏳ **باقی با تریگر:** **Random World per-Save** (Part 21) → با معماریِ ملک‌جت (یک دنیای «مشترکِ» متصل به بازارِ واقعی، نه Saveهای جدا) ناسازگار است — تنوعِ دنیا از خودِ بازارِ واقعی می‌آید (قیمت‌ها/عرضه هر روز عوض می‌شوند)؛ اگر روزی «فصل/سناریو» خواستیم، از Season Engine (صف #۲) با Seed قطعی؛ **Chain Reaction کامل + حافظهٔ دنیا** (Part 22) → اثرِ قیمتیِ مصنوعی = نقضِ قانونِ ۱؛ نسخهٔ واقعی (خبر/رقابت/اشباعِ فروشِ عمده/journal) هست، دومینوی شبیه‌سازی‌شده قفل به NPC Civilization (صف #۱)؛ **Lucky Discovery روی نقشه** (Part 24) → نیازمندِ موتورِ محتوایِ مخفی — کاندیدِ ترکیب با Quest Studio (صف #۲؛ نسخهٔ کنونی: فرصت‌های طلایی/کمیابی/مزایده از دادهٔ واقعی)؛ **فیدِ جهانیِ زندهٔ رویدادهای بازیکنان + Trending + FOMO عددی** (فصل ۱۸ Part 1 کامل) → با جمعیتِ فعالِ کافی (زیرساختِ روزنامه هست)؛ **Adaptive Reward AI / Auto-Freeze خودکار** (Ch19 Part 1) → عمداً «تأییدِ انسانی» را نگه می‌داریم (قانونِ Level 0/1 فاز ۳۶: اقتصاد فقط دستِ ادمین)؛ نسخهٔ هشدارها در رصدخانه هست؛ **Tier Budget/Max Winners/Seasonal Reset/Economic Index** → افزودنی‌های آیندهٔ همان `empire-rewards` وقتی جمعیت و درآمدِ واقعی بطلبد؛ **Creator Store با پولِ واقعی، Business Club چندسطحی، CEO Pass با تمِ فصلی** → صفِ VIP/پلن‌ها + Season Engine (صف #۲)؛ **بخش‌های پایانیِ سند (Online Architecture/Guild/End Game/AI Citizens/...)** → منتظرِ سندهای تفصیلیِ بعدی.

### سند ۳۱ (فصل ۲۰ — End Game کامل، ۷ پارت)
✅ **فاز ۶۲ (قابل‌ساختِ الان):** نردبانِ بی‌پایان/لایه‌های نقش (Part 1) + شاخصِ میراث و مستندِ مسیر (Part 2) + شگفتی‌های دنیا با انواعِ افتخار (Part 4) + موتورِ رؤیاها با رؤیای شخصی (Part 7) — بالا.
✅ **از قبل برقرار:** «هیچ You Win وجود ندارد» (ساختارِ سطح/لیگ/جوایز از روزِ اول بی‌سقف است) · Prestige بدونِ Reset (هیچ مکانیکِ ریست نداریم — دقیقاً موضعِ خودِ سند) · Historical Projects (کارنامهٔ پروژه‌ها projectHist + پلاکِ رکوردها) · Hall of Fame (تالارِ افتخارات فاز ۵۰ + لیدربوردها) · «Complexity > Money» نسخهٔ v1: بحران/اعتصاب‌های ساخت/وام/مالیات با بزرگ‌شدن بیشتر می‌شوند (فاز ۱۳/۴۱) و واگذاری به مهندس/مدیر از فاز ۱۲ هست.
⏳ **باقی با تریگر:** **Mega Projects چندمرحله‌ای + کنسرسیوم** (Part 3) → نیازمندِ اقتصادِ مشترکِ چندبازیکنه (سهم/اختلاف/خروجِ وسطِ پروژه) — قفل به تکمیلِ اتحادها/آلیانسِ فاز ۳۷ + جمعیتِ فعال؛ نسخهٔ تک‌نفرهٔ پروژهٔ بزرگ همین حالا با موتورِ ساخت هست؛ **دلگیشنِ اجباری/هیئت‌مدیره** (Part 5 «از توانِ یک انسان خارج است») → گیتِ سختِ ظرفیت با قانونِ ۹۰/۱۰ باید مرحله‌ای تست شود؛ ظرفیتِ پروژهٔ همزمان و استخدام از قبل هست، اجباری‌کردنش با رشدِ جمعیت؛ **صحنهٔ جهانی/چندشهری** (Part 6) → دادهٔ واقعیِ فعلی عمدتاً یک‌شهری است؛ با پوششِ چندشهریِ آگهی‌های واقعی (تریگر: دادهٔ کافیِ ≥۲ شهر) شخصیتِ شهرها از خودِ بازارِ واقعی ساخته می‌شود، نه جدولِ ساختگی؛ **Community/Impossible/Hidden Dreams کامل** (Part 7) → رؤیای جمعی قفل به اتحادها؛ نسخهٔ مخفی از قبل با مأموریت‌های مخفی هست؛ رؤیای «تقریباً غیرممکن» با رشدِ جمعیت به WONDER_DEFS اضافه می‌شود.

### سند ۳۳ (نهایی — فصل ۲۲/۲۳ + Phase 2 «Production Design» با فصل‌های اجرایی ۱–۱۴)
📜 خودِ سند اعلام می‌کند: «Game Design ~۹۵٪ کامل است؛ از این‌جا عمق با پیاده‌سازیِ دقیق به دست می‌آید، نه سیستمِ جدید» — دقیقاً همان مسیری که این پروژه از فاز ۱ رفته است.
✅ **فاز ۷۱ (قابل‌ساختِ الان):** دو قانونِ طلایی (۱۴/۱۵) + کتابِ زندگی/Personality Evolution + مناسبت‌های واقعیِ تقویم — بالا.
✅ **از قبل برقرار (نگاشتِ فصل‌به‌فصل):** فصل ۲۲ P1 «One World» = معماریِ ما از روزِ اول (یک دنیای مشترک، بدونِ Lobby/Instance؛ شهرها داینامیک از دادهٔ واقعی — فاز ۶۸) · P2 «قیمت دلیل دارد» = بنیادِ قانونِ ۱ (همهٔ قیمت‌ها از بازارِ واقعی + توضیح‌پذیری «چرا» فاز ۱۱/۳۹ + Layered Complexity = سطح‌گشاییِ فاز ۱۷) · فصل ۲۳ Fun = قانونِ ۹۰/۱۰ + مذاکره/تالار/بانکِ شخصیت‌دار/رویدادهای کلنگ که همه «تصمیم‌دار»ند · Dopamine = صندوقچه/چکش/Near-miss تالار («برندهٔ واقعی تو بودی»)/فرصت‌های طلایی/نشان‌های مخفی · Zero Dead Time = اولویت‌های روز + openActions + suspense · Outside The Game = قوانینِ نامهٔ روزانه/روزنامهٔ «فقط مهم»/بدونِ انرژی از روزِ اول · Monetization فصل ۴ = قوانینِ ۵ (بدونِ P2W) + فروشگاهِ ظاهری + Verified≈پلن‌های سایت · First 10 Minutes/Day1/Week1 ≈ Opening Experience فاز ۴ + مسیرِ برجِ اول + منتورِ درجا (mentorLine) — بدونِ Tutorial · First Year Stages ≈ STAGES + لایه‌های نقشِ فاز ۶۲ · Notifications=قوانینِ نامه · Guild=اتحاد · Trading=بازارِ بازیکنان+مزایدهٔ P2P · Profile=فاز ۶۴ · Achievements=نشان‌ها/مجموعه‌ها (داستانی) · Leaderboard=۶ جدول+لیگ.
⏳ **باقی با تریگر:** **Supply&Demand کامل + Heat Map شهری** (فصل ۲۲ P2) → نیازمندِ تراکمِ دادهٔ عرضه/تقاضای واقعی (شمارِ آگهیِ فعال به‌ازای محله را داریم؛ «تقاضا» با جمعیتِ بازیکن/لیدِ واقعی معنا می‌گیرد)؛ **Living Citizens Engine سه‌سطحی** (فصل ۲۲ P3) → همان NPC Civilization فاز ۲+ (شهروندانِ آماری) — قفل به جمعیت/ظرفیتِ سرور، معماریِ سه‌سطحیِ سند ثبت شد؛ **Creator Marketplace/Service Economy/Franchise/آموزش** (فصل ۴ P2) → صفِ Creator Store (سهمِ ۷۰/۳۰ خودِ سند پیشنهاد داده — منتظرِ تصمیمِ قیمت‌گذاری)؛ **Real Business Economy سه‌لایه** (فصل ۴ P3) → بعد از Creator Store؛ **Architectural Skins/Skyline Cosmetic** → Visual Pass 2؛ **هوای واقعی** → نیازمندِ APIِ هواشناسی (تصمیمِ افزودنِ سرویسِ خارجی)؛ **Chat/Friend System کامل** (فصل‌های ۸/۱۰) → فالو (فاز ۶۷) و تابلوی اتحاد هست؛ گفت‌وگوی سراسری با جمعیت + نیازِ جدی به ماژولِ نظارت؛ **PDF کتابِ زندگی** → خروجیِ چاپیِ biographyOf وقتی خواستی (فنی ساده، تصمیمِ محصول).

آخرین وضعیت تست: **۵۹۹ یونیت + ۵۱۴ PG = ۱۱۱۳ سبز + Economy QA ۳۰ ممیزی.** دودآزمای ادمین: ۴۷ نما، همه سبز.

### سند ۳۲ (فصل ۲۱ — Live World Engine کامل، ۷ پارت + اعلامِ ورود به Production Design)
✅ **فاز ۶۳ (قابل‌ساختِ الان):** کتابِ تاریخِ دنیا + سالِ دنیا (P1/P2/P7) + گزارشِ «در نبودِ شما» (P2) + دمای دنیا با پیشنهادِ ادمین (P5) + شایعاتِ منصفانه با منبع/اعتبار/ارزیابی (P3) — بالا.
✅ **از قبل برقرار:** دنیای «مشترکِ» متصل به بازارِ واقعی یعنی دنیا واقعاً بدونِ بازیکن حرکت می‌کند (قیمت‌ها/آگهی‌ها هر روز عوض می‌شوند — Living World به صادقانه‌ترین شکل) · روزنامهٔ «فقط مهم» (فاز ۱۰) = MelkJet Daily · استودیوی رویدادِ بدونِ دیپلوی (فاز ۲۰) = Admin Live Console v1 · شایعاتِ لابیِ تالار (فاز ۴۵) · رصدخانه/هشدارها (فاز ۳۵) = Live Dashboard · World Evolution نسخهٔ واقعی: اشباعِ فروشِ عمده (فاز ۱۵) + رقابت روی همان ملکِ واقعی (فاز ۳۷/۴۱/۴۵).
⏳ **باقی با تریگر:** **World Seasons/Seasonal Story** → Season Engine (صف #۲) با Seed قطعی؛ متای فصلی نیازمندِ جمعیت؛ **World Feed تعاملیِ کامل** (P2 پیشنهاد) → زیرساختِ روزنامه+kudos هست؛ فیدِ قابل‌فالو با جمعیتِ فعال؛ **AI Live Director** (P5 پیشنهاد) → نسخهٔ v1 همین پیشنهادهای قاعده‌مندِ دمای دنیاست؛ طراحِ خودکارِ رویداد عمداً پشتِ قانونِ Level 0/1 (فاز ۳۶) می‌ماند — LLM فقط پیش‌نویس، تأیید با ادمین (کاندیدِ ترکیب با Quest Studio صف #۲)؛ **PGW/Creator Program سه‌لایه (P4)** → صفِ Creator Store؛ چالش‌ساز/تورنمنت‌ساز قفل به Quest Studio؛ معماریِ سه‌لایهٔ Private/Community/Official ثبت شد؛ **Endless Expansion (P6)** → همان تریگرِ چندشهریِ فاز ۶۲ + اصلِ «هر Expansion یک لایهٔ گیم‌پلیِ جدید، نه فقط نقشه» ثبت شد — بازارِ سرمایه عملاً Expansion 1 بود؛ **Nothing Is Sacred (P7)** → نسخهٔ نرمش هست (شگفتی‌های قابلِ‌گرفتن، بحران، رکودِ بازارِ واقعی)؛ زوالِ کامل قفل به NPC Civilization با اصلِ «همیشه راهِ بازگشت»؛ **فصل‌های بعدی (Production Design: World Server/AI Simulation/Microservices/DB)** → منتظرِ سندهای تفصیلی — نگاشت به معماریِ فعلی هنگامِ رسیدنِ هر سند.

### سند ۱۲ (جلد۷۳–۷۸ + سه تصمیمِ معماریِ نهایی)
**تصمیم‌های لازم‌الاجرا:** ۱) لیستِ Engineها فریز — Engine جدید فقط با جای مشخص در Core Loop؛ بقیه Subsystemاند. ۲) Master Index جدا از Bible (→ ENGINE-MAP). ۳) ترتیبِ استودیویی: GDD کامل → Technical → Content → Visual؛ «تا Feature List کامل نشود Engine جدید نه».
✅ جلد۷۳ (Construction Engine spec) — پیاده‌شده در فاز ۱۳ و در ENGINE-MAP نگاشت شد (چندپروژه/State/Pause ✓؛ ⏳ وزنِ مرحله‌ای/Health مرکب). ✅ جلد۷۴ (Economy) — بقای پول از فاز ۱ + گزارشِ مالی این فاز (⏳ چنددفتری/ترازنامه). ✅ جلد۷۵ (Asset) — EmpireAsset+تاریخچهٔ تغییرناپذیر (⏳ تجهیزات/برند به‌عنوان دارایی). ✅ جلد۷۶ (Human) هسته — Engineer/ownerPersona (⏳ NPC کامل — همان قفل‌گشا). ✅ جلد۷۷ (Core Loop) و ✅ جلد۷۸ (Pillars) — در ENGINE-MAP به‌عنوان قانونِ اساسیِ طراحی ثبت و انطباقِ ساخته‌ها نگاشت شد.

### سند ۱۳ (GDD جدید — Chapter 04 Complete Feature List بخش‌های ۱–۸)
📌 **بازساختارِ سندها:** دیگر «جلد» نداریم — GDD فصل‌های ۰۱–۱۰، بعد TDD-001+. این سند = فصل ۴ (فهرستِ کاملِ قابلیت‌ها).
📜 **قانون‌های تازهٔ طراحی (در ENGINE-MAP هم ثبت شد):** بازیکن CEO است نه حسابدار/اپراتور؛ «اگر بازیکن از انجامِ کاری لذت نمی‌برد → AI انجامش دهد»؛ هر سیستم فقط ۳–۸ تصمیمِ مهم؛ «اگر یک قابلیت باعث نشود بازیکن تصمیمِ جالبی بگیرد، اصلاً واردِ GDD نمی‌شود»؛ بازار باید به رفتارِ بازیکن واکنش نشان دهد.
🚫 **ردشده به تصمیمِ خودِ سند (هرگز نسازیم):** قابلیت‌های ERPوار بخش‌های ۱–۲ (دفترِ کل، ترازنامه، مغایرتِ بانکی، انبارِ چندسطحی، حضورغیاب و…) — «برای بازی افتضاح است»؛ فقط نسخهٔ تصمیم‌محورشان معتبر است.
✅ **فاز ۱۵ ساخته شد:** هدفِ پروژه (بخش ۸) + امکاناتِ میان‌ساخت و «بفروش یا اجاره بده» (بخش ۴) + اشباعِ فروشِ عمده (بخش ۵ «بازارِ واکنش‌دار» نسخهٔ ۱) + تحلیلِ پس از پروژه (بخش ۴/۸) + کارتِ استخدام با اثرِ عددی (بخش ۳).
⏳ **باقی از این سند (با تریگر):** رقبای شخصیت‌دار و فرصت‌های زمانی بازار (بخش ۵) → NPC Civilization؛ تیم‌سازی سریع/حرفه‌ای/اقتصادی + فرهنگِ سازمانی + بحرانِ سه‌گزینه‌ای شرکت (بخش ۳) → وقتی چندپروژگی و تیمِ بزرگ رایج شد؛ DNA زمین/شایعه‌ها («شنیده می‌شود مترو…»)/زمین‌های داستانی (بخش ۶) → نیازمندِ لایهٔ خبر/شایعهٔ راستی‌آزمایی‌شده (Event Engine v2)؛ گسترش به شهرِ جدید/تغییرِ مسیرِ فعالیت (بخش ۷) → وقتی چند بازیکنِ سطح‌بالای واقعی داریم؛ بازارِ هدف (خانواده/لوکس/دانشجو…) و فازبندیِ سرمایه‌گذاری (بخش ۸) → نیازمندِ Customer Engine.

### سند ۱۴ (GDD فصل ۴ بخش‌های ۹–۱۶ + پیوتِ بزرگِ «مدل B»)
📌 **پیوتِ تعیین‌کننده (لازم‌الاجرا برای همهٔ فازهای بعد):** MelkJet **بازی** است نه شبیه‌ساز — «Tycoon + Strategy + Collection + Live Economy» (مدل B: مونوپولی‌گو/کلش‌وار، نه Football Manager). آزمونِ هر قابلیت: «آیا کاربر می‌گوید *فقط ۵ دقیقهٔ دیگر*؟» + هر قابلیت باید تصمیم/ریسک/فرصتِ جدید بسازد. ترتیبِ طراحی: **Hook → Daily Gameplay → Meta Progression → پشت‌صحنه**. واژه‌های ERP/مدیرعامل/حسابداری/فرآیندِ اداری ممنوع مگر مستقیماً لذت بسازند. Big Data محصولِ طبیعیِ رفتارِ بازیکن است، نه چیزی که حس شود.
✅ **فاز ۱۶ ساخته شد:** Hook فرصت‌های محدودِ روزانه (بخش ۱۰ + «۹ شب چه می‌بیند»)، حافظهٔ مذاکره (بخش ۹)، اعتبارِ اثرگذار روی مذاکره/بانک (بخش ۱۵ §۶). مذاکرهٔ چندگزینه‌ای (قبول/کمتر/اقساط/مشارکت/ترک) نسخهٔ فعلی: مذاکرهٔ قطعی + شخصیتِ مالک + حافظه — گزینه‌های اقساط/مشارکت با NPC/قرارداد می‌آیند.
⏳ **باقی از این سند (با تریگر):** رقبای شخصیت‌دار که پروژه می‌سازند و روی قیمت اثر می‌گذارند + رقابت بر سرِ زمین/مشتری/نیرو + M&A (بخش ۱۱) → NPC Civilization؛ بازاریابی/کمپین (بخش ۱۲) و مشتریِ شخصیت‌دار/رضایت/وفاداری (بخش ۱۳) → Customer Engine (بدونِ شبیه‌سازیِ تقاضا، اثرِ صادقانه ندارند)؛ R&D/مسیرِ توسعه/مزیتِ تخصصی (بخش ۱۴) → همان صفِ Prestige/Skill Tree؛ شراکت/سرمایه‌گذار (بخش ۱۶) → خودِ سند آن را «تلهٔ شبیه‌ساز» خواند — فقط نسخهٔ Gamified (مثلِ مشارکتِ جمعیِ موجود)؛ اعتبارِ محلی/تخصصی (بخش ۱۵ §۸/۹) → وقتی چندشهر فعال شد؛ مذاکره با دولت/طبقهٔ اضافه (بخش ۹ §۵) → Event Engine v2.

### سند ۱۵ (فصل ۴ بخش‌های ۱۷–۲۰ «بستنِ Core Gameplay» + فصل ۵ «قوانینِ اقتصاد»)
📌 **Core Gameplay رسماً بسته شد** — متا-لوپ: کشف → تصمیم → سود/زیان → رشد → فرصتِ جدید؛ ۴ حلقه (۳دقیقه‌ای/۲۰دقیقه‌ای/چندساعته/روزانه)؛ **۷ ستونِ نهایی: کشفِ فرصت، معامله، ریسک، رقابت، رشد، جمع‌آوریِ دارایی، رویدادهای زنده** — قابلیتی که به هیچ‌کدام کمک نکند وارد نمی‌شود. طراحی «Opportunity-محور، نه Task-محور». فصل ۵ فقط قانون است، نه عدد (اعداد → Game Balance بعداً).
✅ **فاز ۱۷ ساخته شد:** سطح‌گشاییِ قابلیت‌ها + ظرفیتِ پروژهٔ همزمان (بخش ۱۹ + فصل ۵ منابع) + فروشِ پروژهٔ نیمه‌کاره (فصل ۵ دارایی‌ها/درآمد #۱۰).
📗 **قوانینِ فصل ۵ که از قبل برقرارند:** بقای پول، نقدینگی>ثروت (بحرانِ نقدینگیِ کارگاه)، هزینهٔ فرصت، درآمدِ غیرتضمینی، ریسکِ واقعی، هزینه‌ها (مالیات/بهره/نگهداری/حقوق)، ۱۰ راهِ درآمد (فروش/اجاره/فلیپ/پیش‌فروش/مشارکت/رویداد/مأموریت/…)؛ **ارزها**: سازگار با سقفِ سند — قابلِ‌خرج فقط Cash (تومانِ شبیه‌سازی) و ملک‌کوین؛ XP و اعتبار غیرقابلِ‌خرید. Gold پرمیوم → با زرین‌پال (صف، بدونِ P2W).
⏳ **باقی با تریگر:** مزایده (فصل ۵ بخش ۸ — همان صفِ مناقصه/مزایده؛ رقابتِ زنده‌اش → NPC)؛ رویدادهای زندهٔ فصلی/جهانی و Community Goal (بخش ۱۸) → Season Engine/Quest Studio (صف #۲)؛ چالش‌های روزانهٔ رقابتی → وقتی چند بازیکنِ فعالِ همزمانِ واقعی داریم؛ شهرهای سطح‌گشا و «اعتبارِ محلی» → چندشهر؛ تورم/رکودِ سراسری → فقط از سیگنالِ واقعیِ بازار (نه شبیه‌سازی)؛ سهامِ شرکت‌ها (فصل ۵ بخش ۹ #۶) → NPC/چندبازیکنه.

### سند ۱۶ (فصل ۶ — Progression & Progress، بخش‌های ۱–۱۰)
📗 **ممیزی: بیشترِ فصل از قبل ساخته بود** — سطح/XP ✓، سطح‌گشاییِ قابلیت‌ها و ظرفیت ✓ (فاز ۱۷)، دستاوردها/مخفی‌ها ✓، کلکسیون ✓، مأموریتِ روزانه/هفتگی ✓، ۵ لیدربرد + لیگِ محله + پاسپورت ✓، شخصی‌سازیِ شرکت (نام/برند/رنگ) ✓.
✅ **فاز ۱۸ شکاف‌ها را بست:** پاداشِ Level Up (بخش ۱ #۸) + لیدربوردِ دوره‌ایِ «رشدِ این هفته» (بخش ۷) + Title فعال از نشان‌ها (بخش ۹).
⏳ **باقی با تریگر:** «جدولِ مرکزیِ مأموریت‌ها» (نکتهٔ خودِ سند در بخش ۶) = همان Quest Studio (صف #۲ — اولویتش بالا رفت)؛ Season Pass (بخش ۸، Free/Premium بدونِ P2W) → بعد از زرین‌پال + Season Engine؛ شهرهای سطح‌گشا و «برترین شهرِ فصل» → چندشهر؛ دفترِ مرکزی/هلدینگ (بخش ۲) → Arc 4؛ رتبهٔ دوستان → گرافِ اجتماعی؛ Endgame (بخش ۱۰: مگاپروژه/تصاحبِ شهر/رقابتِ هلدینگ‌ها) → NPC Civilization + چندبازیکنهٔ واقعی؛ آیتم‌های تزئینی/قاب → فروشگاهِ ظاهریِ زرین‌پال.

### سند ۱۷ (فصل ۷ — Multiplayer & Social، بخش‌های ۱–۱۰)
📌 **آزمونِ فصل (از خودِ سند):** «اگر این قابلیت حذف شود، آیا بازیکن دلیلی برای تعامل با دیگران از دست می‌دهد؟»
✅ **فاز ۱۹ (با بازیکن‌های واقعیِ موجود):** بازدید از امپراتوریِ دیگران از لیدربورد + 👏 تحسینِ یک‌باره. از قبل: دنبال‌کردن/کامنت/کالکشن (لایهٔ REOS Community)، دعوتِ شراکتیِ دوطرفه (Referral — بخش ۹ #۱۰)، رتبه‌بندی‌ها، رأی/رکوردهای روزنامه.
⏳ **باقی با تریگر (همه نیازمندِ جمعیتِ واقعی‌اند — جمعیتِ ساختگی ممنوع):** دوستان/رتبهٔ دوستان/هدیهٔ روزانه → گرافِ دوستی (وقتی چند بازیکنِ فعالِ همزمان)؛ چت (خصوصی/شهر/هلدینگ) → زیرساختِ پیامِ سایت هست، نسخهٔ درون‌بازی با جمعیت؛ هلدینگ/Guild + پروژه‌های گروهی + رقابتِ هلدینگ‌ها (بخش‌های ۳/۵) → NPC Civilization یا جمعیتِ واقعی؛ معاملهٔ بین‌بازیکنی و Marketplace درون‌بازی (بخش‌های ۴/۷ — با کارمزد و ضدِ انتقالِ ناعادلانه) → چندبازیکنهٔ واقعی (توجه: MarketplaceِNo «واقعی» همین سایتِ ملک‌جت است)؛ PvP/دوئل/Matchmaking (بخش ۶ — «تصمیمِ بهتر، نه پولِ بیشتر») → پایه‌اش با لیدربوردِ هفتگی آماده است، دوئل با گرافِ دوستی؛ رویدادهای اجتماعی/مناسبتی (بخش ۸) → Season Engine (صف #۲)؛ مرکزِ اعلان + Push (بخش ۱۰ — «اعلان فقط وقتی تصمیم/فرصتِ واقعی هست» — همین قانونِ mentorLine) → وقتی PWA/پوش فعال شد.

### سند ۱۸ (فصل ۸ — LiveOps & Events، بخش‌های ۱–۱۰)
📗 **از قبل داشتیم:** ورودِ روزانه (استریک + صندوقچهٔ متغیر + نامهٔ روزانه)، رویدادِ روزانهٔ واقعی (محلهٔ داغ/رکوردها/فرصت‌های طلایی با شمارشِ معکوس)، چالشِ روزانه/هفتگی (کوئست‌ها)، لیدربوردِ هفتگی، هدیهٔ بازگشت (Comeback)، رویدادِ غافلگیرِ کارگاه.
✅ **فاز ۲۰:** استودیوی رویدادِ بدونِ دیپلوی (بخش ۸ — هستهٔ Live Content Management بخش ۱۰) + پاداشِ نقاطِ عطفِ استریک (بخش ۱).
⏳ **باقی با تریگر:** تمِ هفتگیِ یکتا + رویدادهای فصلی/مناسبتی با تغییرِ ظاهر (بخش‌های ۳/۴) → Season Engine (استودیوی رویداد پایه‌اش شد؛ تمِ بصری → Visual Bible/Pass 2)؛ Limited-Time Offers و بسته‌ها (بخش ۵) → زرین‌پال (بدونِ P2W)؛ چالشِ ۳۰دقیقه‌ای/هلدینگ/جهانی (بخش ۶) → جمعیتِ واقعی؛ x2 XP هفتگی → knob آینده روی استودیو؛ Push Notification با سقفِ ۳/روز و ساعتِ استراحت (بخش ۹) → زیرساختِ PWA/پوش؛ آرشیو/آمارِ مشارکتِ رویدادها → نسخهٔ ۲ی استودیو.

### سند ۱۹ (فصل ۹ — UI/UX، بخش‌های ۱–۱۰)
📌 **قانونِ ۴ سؤالِ هر صفحه (لازم‌الاجرا، در ENGINE-MAP):** الان وضعیت چیست؟ چه فرصت/مشکلی هست؟ بهترین اقدامِ بعدی چیست؟ با کدام دکمه؟
📗 **نگاشتِ ۱۰ صفحه به وضعِ موجود:** Main Menu/«۲ کلیک تا گیم‌پلی» = /empire مستقیم ✓؛ HUD = سربرگِ فشرده (سطح/کوین/ژتون/استریک/👏) ✓؛ City Screen = خطِ آسمان + Hook + پیشنهاد (نسخهٔ نقشه‌محورِ «اپِ املاکِ زنده» → Art Direction/Visual Bible)؛ Property Details = صفحهٔ آگهی + تحلیلِ ژتونی + Twin ✓ (تاریخچهٔ قیمت → صفِ price_history)؛ Project Management = کارگاهِ زنده (نوار/هشدارِ بحران/تصمیمِ بالادست) ✓؛ Market = /search + بازارِ سرمایه ✓؛ Portfolio ✓ فاز ۲۱؛ Profile = پروفایلِ عمومی + پاسپورت + Title ✓؛ Shop → زرین‌پال (فقط ظاهری/سرعت، بدونِ P2W)؛ Settings کاملِ درون‌بازی (صدا/گرافیک/Cloud Save) → نسخهٔ اپ/PWA.
✅ **فاز ۲۱:** کارتِ پرتفوی (نامِ جدید + ارزشِ کل + فیلترِ نوع و «مشکل‌دار» + مرتب‌سازی).

### سند ۲۰ (فصل ۱۰ — Art Direction، بخش‌های ۱–۱۰)
✅ **فاز ۲۲ (قوانینِ الان-قابل‌اجرا):** استپرِ بصریِ مراحلِ ساخت + برجِ در حالِ ساختِ متمایز در خطِ آسمان + حذفِ انیمیشن‌های اجباریِ بلند (سقفِ ۱.۵ث) + ثبتِ معناشناسیِ رنگ‌ها (وضعِ موجودِ /empire از قبل منطبق بود: #7c6 سود، #e88 ضرر، #e7a14a فرصت/هشدار، طلایی ویژه) + احترام به reduced-motion از فاز ۱۱.
⏳ **بدنهٔ سنگینِ بصری → Visual Bible / Pass 2 (فقط با تأییدِ امین):** محیطِ زندهٔ شهر (شب/روز/آب‌وهوا/عابر/خودرو — Part 02)، طراحیِ کاراکترها (Part 04 — فعلاً پرسوناهای متنی/ایموجی)، VFX آب‌وهوا، دوربین/زوم/نقشهٔ شهریِ «اپِ املاکِ زنده» (Part 09 + نکتهٔ Part 03 فصل ۹)، ساختمان‌های Landmark. Design System کامل (پالت/فونت/فاصله/Radius/Shadow — پیشنهادِ Part 08) → همان Pass، پایه‌اش CSS varهای موجودِ سایت است.

## 📚 وضعیت جلدها — سند به سند

### سند ۱ (جلد۱ Vision + جلد۲ فصل۱–۵)
✅ کامل: قوانین طلایی، Core Loop، معرفی، مهمان، ۵ سؤال، Dream Board، هویت+DNA+Confidence، تولد+نام+نشان، هدیه با جملهٔ سند، تایم‌لاین، ۴ فرصت (یکی برجسته+«اگر جای تو بودم»)، متن‌های خرید+امضا، «تو مالک هستی»+پاداش‌ها، تصمیم معنادار، M1(از رویداد واقعی)/M2/M3، شکست آموزشی، تعلیق جلسه، Identity Evolution (bump ها)، AI Journal.
⏳ مانده: Legacy Page مستقل، Life Goals صریح (نردبان رؤیا پوشش جزئی می‌دهد)، پوش‌نوتیف صبحگاهی (نیاز به push infra)، رقابت با AI Builder.

### سند ۲ (فصل۶/۷/۷.۵ + جلد۲.۵/۲.۶ + جلد۳ فصل۱–۴)
✅: چهار نوع ارزش، Welcome Package §6.3، Property Hunter §6.4، Land System §6.7، Business §6.9 (٪ از رقابت واقعی دایرکتوری)، اقتصاد ضدتورمی (هزینهٔ مالکیت/مالیات)، District League، Referral شراکتی §7.4، Viral Moments §7.12، dedup/گراف/فید (زیرساخت REOS از قبل).
⏳: City War، Company System، Mentor Community، Social Feed اختصاصی بازی، FOMO «فرصت‌های هفتهٔ قبل ۸٪ رشد» (نیاز به تاریخچهٔ قیمت per-listing)، فروش آیتم ظاهری (نیاز به درگاه پرداخت زرین‌پال — pending پروژه).

### سند ۳ (GDD جلد۱–۸)
✅: Opening سینمایی+۶ شخصیت، مأموریت تولد (شامل مذاکرهٔ مرحلهٔ ۵)، کوئست روزانه/هفتگی قطعی، سطح بی‌پایان+مراحل، هزینهٔ مالکیت، Empire Passport، لیدربردها.
⏳ (بزرگ — منتظر سند تفصیلی/جمعیت): سیستم ۱۸گانهٔ کوئست کامل + **Quest Studio ادمین** (جلد۲)، Prestige/Skill Tree (جلد۳)، **NPC Universe** (جلد۴ — پیش‌نیازِ دولت و جنگ شرکتی)، مزایده/سهام پروژه/صندوق (جلد۵)، نقشهٔ گرافیکی جهان (جلد۶)، گیلد/PvP/تورنمنت (جلد۷)، Season/LiveOps Studio+تقویم (جلد۸).

### سند ۴ (جلد۹ ادمین + جلد۱۰ کسب‌وکار + جلد۱۱ روانشناسی)
✅: مرکز فرماندهی ۷بخشی (جلد۹ نسخهٔ ۱: World/Players/Economy/RealEstate-Sync/LiveOps/Analytics-lite)، ۸ موتور روانشناسی جلد۱۱ (مالکیت‌آیین، پیشرفت خرد، هویت، میراثِ تایم‌لاین، کنجکاوی صندوقچه/مخفی، وابستگی سالم).
⏳: NPC Studio، شبیه‌ساز تورم، Ghost «Time Machine»، جلد۱۰ عمیق (کارمند/برند/فرانچایز/State Machine کامل کسب‌وکار)، Psychology Studio (Friction/Emotion heatmap — نیاز به حجم کاربر).

### سند ۵ (جلد۱۶–۲۱)
✅ جلد۱۶: بانک/CreditScore/وام/مالیات کامل v1. جلد۱۸ جزئی (اعتبار/گراف اعتماد از REOS trust).
⏳: Crowdfunding/صندوق/بیمه/بازار ثانویه (جلد۱۶ بقیه)، شهر هوشمند (جلد۱۷ — پیش‌نیاز NPC/شهروند)، منتورشیپ/آکادمی/قرارداد (جلد۱۸)، اتصال B2B جلد۱۹ (بانک/بیمهٔ واقعی — نیاز به شریک واقعی)، NPC Civilization (جلد۲۰)، تاریخ زنده (جلد۲۱ — پایه‌اش تایم‌لاین/آرشیو هست).

### سند ۶ — این سند (جلد۲۴–۲۸)
✅ جلد۲۶: Daily Loop کامل (نامه+کوئست+صندوقچه+استریک+سود دیروز+مخفی+کلکسیون+Comeback+رؤیا+زمان امروز). جلد۲۸ هسته: پل Game→Lead (لینک آگهی واقعی) + تزریق رفتار به دیتالیک ML.
⏳: جلد۲۴ دولت/انتخابات/مناقصه و جلد۲۵ گیلد/M&A/لیگ شرکت‌ها → **پیش‌نیاز: NPC Civilization + جمعیتِ بازیکن** (ساختنش الان = صفحهٔ خالی/فیک = ممنوع). Battle Pass/تقویم LiveOps → منتظر Season Engine.

### سند ۷ — جدیدترین (جلد۳۳–۴۰: Engineering Bibles)
ماهیت: Blueprint معماریِ مقیاس ۵۰M (Event Bus/CQRS/Saga/Multi-Region/MLOS/ADE/قانون اساسی/بانکداری عمیق/بورس املاک/REIT) + قولِ بلوک ۱۰-۱۵ جلدی Experience Design.
وضعیت: **مرجعِ معماری آینده** — روی VPS تک‌سروریِ فعلی صادقانه قابل‌پیاده‌سازی نیست و خود سند هم آن را نقشهٔ فاز رشد می‌داند. آنچه از روحش الان رعایت شده: event-driven بودن REOS، dual-mode PG، فلگ‌ها/canary (rollout%)، AutoML/registry/drift، تست‌های واقعی. جلد۳۹ (بانکداری) و ۴۰ (REIT/بورس املاک) وقتی نسخهٔ تفصیلی گیم‌پلی‌شان آمد روی بانکِ موجود سوار می‌شوند.

### سند ۸ (جلد۴۰–۴۸ + ایدهٔ نقشهٔ جهانی/Block 8)
**دو قانون معماری جدید (از خودِ سند — از این به بعد لازم‌الاجرا):**
- هر موتور دو خروجی دارد: Simulation Output (دنیای زنده) + Gameplay Output (تجربهٔ بازیکن).
- «Every Entity Has a Location. Every Action Happens in a Workspace.» — هر موجودیت مختصات دارد (آگهی‌های واقعی دارند؛ موجودیت‌های جدید هم باید داشته باشند).

✅ **جلد۴۰ (بازار سرمایه) — فاز ۹، پیاده‌سازیِ کاملِ بخشِ real-data:** صندوق REIT شاخصی (فصل۸)، Crowdfunding کسری روی آگهی واقعی (فصل۷)، بازار ثانویه=NAV (فصل۱۱)، شاخص‌ها (فصل۱۲)، پرتفوی+تنوع (فصل۱۳)، سود دوره‌ای از اجارهٔ واقعی (فصل۱۵)، روان‌شناسی ترس/طمع از رویداد واقعی (فصل۱۶)، رتبهٔ AAA-BB قطعی (فصل۱۷)، خروج=بازخرید (فصل۱۸)، کنسول سرمایهٔ ادمین (فصل۱۹)، KPIها: MarketCap/AUM/حجم (فصل۲۰).
⏳ جلد۴۰ باقی: IPO/اوراق/سهام شرکت (فصل۶/۱۰) و صندوق زیرساخت (فصل۹) → پیش‌نیاز: شرکت‌ها/نهادها (NPC).
⏳ **جلد۴۱ (نهادها/M&A)، جلد۴۴ (نیروی کار)، جلد۴۵ (NPC زنده)** → همگی زیرمجموعهٔ NPC Civilization — ساختن الان = جمعیتِ ساختگی = ممنوع. جلد۴۵ خودش سندِ تفصیلیِ همان چیزی است که در صف #۱ منتظرش بودیم.
⏳ **جلد۴۲ (صنعت ساخت/WBS/CPM)** — ERP ساختِ واقعی؛ پایه‌اش پنل سازنده (builder-store با پروژه/واحد/مایلستون واقعی) موجود است؛ نسخهٔ گیم‌پلی منتظر NPC/پیمانکار.
⏳ **جلد۴۳ (زنجیرهٔ تأمین)** — نکتهٔ real-data: «شاخص قیمت مصالح» از بازار مصالحِ واقعی ملک‌جت قابل‌ساخت است (تریگر: وقتی فروشگاه‌های مصالح قیمتِ کافی ثبت کردند).
⏳ **جلد۴۶ (DNA شهرها)** — پروفایل شهر از دادهٔ واقعی (میانهٔ متری/تعداد آگهی/رشد) تا حدی در شاخصِ شهرهای فاز ۹ ساخته شد؛ هویت بصری → Block 8.
⏳ **جلد۴۷ (MelkGIS)** — سیستم عصبی جغرافیایی؛ زیرساخت موجود: geo-store، geo-heatmap REOS، نقشهٔ نشان. نسخهٔ کامل (Parcel/Layer/Timeline) پروژهٔ بزرگِ مستقل — منتظر سندِ Block 8 و تصمیمِ کاربر.
⏳ **جلد۴۸ (Workspace OS)** — فقط اعلام شده؛ محتوایش هنوز نیامده.
📌 **Block 8 (World Experience — نقشهٔ جهانی/3D/Simulation Mode/Time Machine)**: خودِ سند می‌گوید «جای آن الان نیست» و در Block مستقل ثبتش کرده — هر وقت جلدهایش آمد، Phase B شروع می‌شود.

### سند ۹ (جلد۴۸–۵۳)
**قانونِ سه‌گانهٔ جدیدِ سند (از جلد ۵۲):** هر اتفاقِ مهم سه خروجی دارد — Simulation (اثر روی موتور) + News (روایت) + Memory (ثبتِ همیشگی). وضعیتِ فعلی: Memory = لاگِ رویدادهای REOS + تایم‌لاین/claims (پاک نمی‌شوند)؛ News = روزنامهٔ فاز ۱۰؛ Simulation = رویدادمحوریِ REOS.

✅ **جلد۴۹ (Engagement/Retention) — فاز ۱۰ + قبلی‌ها:** کوئست روزانه/هفتگی، استریک، صندوقچه، نشان‌ها/مخفی، کلکسیون، لیگ‌ها، Comeback (فازهای ۲–۸) + حالا: کنسول Engagement ادمین (فصل ۱۹/۲۰: DAU/WAU/MAU/D1/D7/D30/churn) و Mastery (فصل ۵). ⏳ فصلی/Battle Pass/رویداد زنده → Season Engine (صف #۲)؛ Retention AI پیشنهاددهنده → فعلاً لیستِ churn + هدیهٔ دستی ادمین.
✅ **جلد۵۲ (روایت/Event Bus) — فاز ۱۰ نسخهٔ ۱:** روزنامهٔ ملک‌جت از رویدادهای واقعی (بدونِ اسکریپت). ⏳ روزنامهٔ per-city، شایعه، افکار عمومی، سنجشِ NPC → منتظر NPC.
✅ **جلد۵۱ (زمان/تاریخ) — جزئی:** Memory Layer (رویدادها/تایم‌لاین حذف‌نشدنی)، آرشیو رکوردها (فصل ۹)، ردِ فعالیت تاریخ‌دار. ⏳ تاریخچهٔ قیمتِ هر آگهی (نیاز به snapshot دوره‌ای — تریگر: تصمیم برای جدول price_history)، نقشهٔ تاریخی/نسل‌ها → NPC/Block 8.
⏳ **جلد۴۸ (Workspace OS):** روحش تا حدی هست (پنل‌ها + منوی مستقل + AI copilot در پنل‌ها)؛ نسخهٔ کامل (Command Palette، پنجره‌های شناور، Multi-Workspace، همکاری Realtime) = بازطراحی کامل UI → Block 8/Phase B با تأیید کاربر.
⏳ **جلد۵۰ (سازمان/گیلد چندبازیکنه):** «واحدِ بازی = سازمان» — نیازمندِ جمعیتِ همزمانِ بازیکنانِ واقعی؛ ساختنش الان = گیلدهای خالی. تریگر: چند ده بازیکنِ فعالِ واقعی. زیرساختِ آماده: referral دوطرفه، لیدربرد، trust.
⏳ **جلد۵۳ (دنیای خودگردان):** «The World Never Waits» — همین حالا جزئی برقرار است (هزینهٔ مالکیت/بهرهٔ وام/درآمد اجاره/سود صندوق در غیابِ کاربر جلو می‌روند + Comeback). نسخهٔ کامل (شرکت‌های خودمختار) → NPC.

### سند ۱۰ (جلد۵۳–۵۷ + سه بازنگری معماری)
**قوانینِ معماریِ رسمیِ سند (تا امروز ۴ قانون):** ۱) Every Entity Has a Location ۲) Everything Is Extendable ۳) Never Show Data If You Can Show Experience ۴) Every Object Must Be Inspectable. + اصلِ World-first (دنیا حولِ بازیکن نمی‌چرخد) + از جلد ۵۴ هر جلد لایهٔ Production Blueprint دارد.

✅ **جلد۵۶ (UX/Motion/Delight) — فاز ۱۱ نسخهٔ ۱:** motion پلکانی، میکرواینترکشن، CountUp پول، جشنِ موفقیت، خطِ آسمان (قانون ۳ روی دارایی‌ها). ⏳ صدا/هپتیک، دوربینِ سینمایی، Scene-محوری کامل، حالتِ 3D/World Mode → Block 8 (نیازمند موتور نقشه/3D و تصمیمِ کاربر).
✅ **جلد۵۴ (انسان در برابر AI) — روحش برقرار:** AI همه‌جا مشاور است نه تصمیم‌گیر (تحلیل/Twin/پیشنهاد با اطمینان٪)، تصمیمِ نهایی با کاربر؛ حالا توضیح‌پذیری (چیپ‌های چرایی) هم اضافه شد؛ مانیتورِ AI ادمین (ai-cost/مدل‌ها) از قبل. ⏳ تیمِ چند-AI شخصیت‌دار (AI CFO/Lawyer/…) → نیازمند تعریفِ agentهای جدید در AI Studio.
⏳ **جلد۵۳ (دنیای خودگردان کامل):** NPC-قفل؛ برقرارِ فعلی: اقتصادِ بازیکن در غیابش جلو می‌رود + cron نامه‌ها. «AI شرکت/دولت/شهر» → تمدن NPC.
⏳ **جلد۵۵ (Platform SDK/افزونه/Marketplace):** بازطراحیِ Core در مقیاسِ تیمی — روی این کدبیس تک‌مخزنه فعلاً واقع‌بینانه نیست؛ روحِ فعلی: flags، config زنده، منوهای ماژولار. تریگر: تیمِ چندنفره/تصمیمِ کاربر.
⏳ **جلد۵۷ (CityOS/ترافیک/زیرساخت/Digital Twin شهر):** نیازمند GIS کامل (جلد۴۷) + NPC + Block 8؛ «Inspectable» تا حدِ لینکِ آگهیِ واقعی و پروندهٔ بازیکن برقرار است.

### سند ۱۱ (جلد۵۸–۶۵ + قفلِ نقشه‌راه روی گیم‌پلی)
**قانونِ ۵ سند:** از جلد ۶۱ هر جلد باید به ۴ سؤال جواب دهد (بازیکن امروز چه می‌کند/چه مأموریتی جلو می‌رود/چه Unlock می‌شود/چطور اعتیادآورتر). **قولِ سند:** تا تحویلِ اولین برج، هیچ Feature خارج از چرخهٔ اولین پروژه.

⏳ **جلد۵۸ (معماری جهانی World→Country→…)**: درختِ geo فعلی استان→شهر→منطقه→محله است؛ لایهٔ کشور/ارز/قانونِ چندکشوری → تریگر: ورودِ Region دوم. ⏳ **جلد۵۹ (Enterprise/Government/Platform Economy)**: خودِ سند هم آن را به بعدِ نسخهٔ ۱ موکول کرد — فقط ثبت.
⏳ **جلد۶۰ (Workflow خودِ بازی)**: MelkJet برای پنل‌ها workflow-store دارد؛ نسخهٔ گیم‌پلی (Trigger/Condition/Action روی رویدادهای بازی + Builder بصری) → صف، بعد از چرخهٔ اولین پروژه.
✅ **جلد۶۱ — فاز ۱۲**: شرکت+برند+اعتبار ستاره‌ای+استخدام (دفتر/تجهیزات → با جلد ۶۴). ✅ **جلد۶۲ هسته**: زمین از آگهی‌های واقعی + شخصیتِ مالک + مذاکره + توضیحِ AI (رنگ‌بندی نقشه/رقیبِ زنده → NPC/نقشه). ✅ **جلد۶۳ هسته**: پروانه (درخواست/عوارض/اعتراض/کمیسیون/صدور). ⏳ ورودِ سه‌بعدی شهرداری/بازرسِ متحرک → Block 8.
✅ **جلد۶۴–۷۲ (هستهٔ Engine) — فاز ۱۳**: چرخهٔ کاملِ ساخت+نقدینگی+پیش‌فروش+فروش (بالا). ⏳ باقی از این جلدها: انتخابِ برند/پیمانکارِ چندگزینه‌ای برای MEP (جلد ۶۷ جزئیات)، هویتِ نما/سبکِ معماری با Preview (جلد ۶۸ — بخشی qualityFactor پوشش می‌دهد؛ Preview بصری → Visual Bible/Pass 2)، Project Health مرکب، گزارشِ شبانهٔ کارگاه در نامهٔ روزانه، Customer Engine (شخصیتِ خریدارِ واحد — مثل ownerPersona)، Auction/Tender Engine (مناقصه/مزایده/مشارکت به‌جای خریدِ مستقیم — «شروعِ هر پروژه خودش بازی است»).
📌 **تصمیمِ سند: از این به بعد جلدها فقط Engine یا Core Loop** — سناریو/محتوا بعداً (Scenario Packs). Visual/Art Bible به‌عنوان Pass 2 جداگانه.

## 🎯 صفِ بعدی (به ترتیب پیشنهادی)

1. **NPC Civilization (جلد۲۰/۴ + حالا ۴۱/۴۴/۴۵)** — قفل‌گشای دولت (۲۴)، جنگ شرکتی (۲۵)، شهر (۱۷)، نهادها (۴۱)، نیروی کار (۴۴). جلد۴۵ سندِ تفصیلیِ آن است؛ طرح: Digital Citizen با DNA قطعی + اقتصاد خودگردان روی آگهی‌های واقعی.
2. **Quest Studio + Season Engine (جلد۲/۸/۹)** — ادمین بدون کدنویسی مأموریت/رویداد بسازد؛ زیرمنوی جدید امپراتوری.
3. ~~Crowdfunding/صندوق~~ ✅ (فاز ۹). باقی از جلد۱۶: **بیمه** — روی بانک موجود.
4. **درگاه پرداخت زرین‌پال** → بعدش فروشگاه آیتم ظاهری + Empire Pass (بدون P2W).
5. Prestige + Skill Tree (جلد۳) وقتی بازیکنِ سطح‌بالا واقعی وجود داشت.
6. شاخص قیمت مصالح از بازار مصالح واقعی (جلد۴۳) — وقتی دادهٔ قیمتِ کافی جمع شد.

## 🔁 روالِ هر سند جدید (برای سشن‌های آینده)

1. سند را **کامل** بخوان (چند Read با offset).
2. همین فایل را به‌روز کن: جلد جدید + وضعیت + آنچه قابل‌ساختِ «واقعی» است.
3. فقط چیزهایی را بساز که با دادهٔ واقعی کار کنند؛ بقیه را با دلیل و Trigger در ⏳ ثبت کن.
4. تست یونیت+PG، tsc، build، کامیت (تریلر استاندارد)، push، و این فایل را هم در همان کامیت به‌روز کن.
