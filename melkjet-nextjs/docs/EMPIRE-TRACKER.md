# 🏛 ردیابِ رسمیِ Empire Bible — منبعِ حقیقتِ پیاده‌سازی

> **این فایل حافظهٔ پروژه است.** هر سشنِ جدیدِ Claude اول این فایل را می‌خواند.
> با هر سندِ جدید یا فازِ جدید، همین فایل به‌روز و کامیت می‌شود. چت حافظه نیست — این فایل هست.

## 📜 قوانینِ نقض‌نشدنی (از خودِ سندها + دستورهای صریحِ امین)

1. **هیچ دادهٔ فیک، هرگز.** همهٔ اعداد از بازارِ واقعیِ ملک‌جت (آگهی‌ها/رویدادها/آمار). اگر داده نیست → صادقانه خالی/پیام، نه عددِ ساختگی.
2. دستیارِ بازی **«ملک‌جت»** است — نامِ «نورا/آرمان/سارا» ممنوع (دستور صریح کاربر).
3. در UI کاربر هرگز واژهٔ **«بازی»** نمی‌آید → «مسیرِ رشد / امپراتوری / سفرِ مالی» (Branding Rule سند).
4. هر عددِ موتور باید در سوپرادمین قابل‌تنظیم باشد و **واقعاً** موتور زنده را عوض کند (نه فیلد تزئینی).
5. هیچ Pay-to-Win؛ پرداخت فقط برای سرعت/تحلیل/ظاهر.
6. «هیچ پولی از هیچ ساخته نمی‌شود» — هر تولید/مصرف پول منبع و مقصد دارد (مالیات → خزانه، بدهی از netWorth کم می‌شود).
7. مکانیک‌های شانسی (صندوقچه/مذاکره/کوئست) **قطعی از هش** — ضدسوءاستفاده، تست‌پذیر.
8. امپراتوری **منوی مستقلِ خودش** را در سوپرادمین دارد (گروه «امپراتوری (بازی)») — هیچ‌چیز داخل REOS قاطی نشود.
9. هر فاز: تست یونیت + تستِ PG واقعی + tsc + build + کامیت + push. دیپلوی همیشه دستِ کاربر: `sudo scripts/deploy.sh` + پاک‌کردن کش آروان.
10. `Date.now()` بازی‌ساز نیست — snapshot/دوره‌ها بر پایهٔ dayNumber؛ ورودی‌های عددی فارسی/جداکننده‌دار همیشه با `digitsOf` پاک می‌شوند.
11. **کامپوننت‌های مشترکِ سایت (NeshanMap و…) رفتارِ پیش‌فرضشان قفل است** — قابلیتِ جدید فقط opt-in با propِ جدید؛ تغییرِ لودر/URL/رفتارِ سراسری ممنوع مگر بعد از تستِ همهٔ صفحاتِ مصرف‌کننده (درسِ فاز ۳۰: تغییرِ URLِ SDK نقشه، نقشهٔ صفحهٔ ملک را هم خاکستری کرد).

## 🗂 نقشهٔ کد (کجا چیست)

| چی | کجا |
|---|---|
| هستهٔ بازی (store دوحالته PG/فایل) | `app/lib/empire-store.ts` |
| بازار سرمایه (صندوق شاخصی/مشارکت جمعی/شاخص‌ها/ترس‌وطمع) | `app/lib/empire-market.ts` + اکشن‌های market در API بازیکن + نمای `capital` ادمین |
| نامهٔ روزانه (تولید شبانه) | `app/lib/empire-brief.ts` + بلوک ۶ساعتهٔ `cron-runner.ts` |
| API بازیکن | `app/api/empire/route.ts` |
| صفحهٔ بازی | `app/empire/page.tsx` |
| کارت ورود در پنل‌ها | `app/components/EmpireCard.tsx` → buyer/owner/pros/agency |
| مرکز فرماندهی ادمین (۸ زیربخش، شامل «بازار سرمایه») | `app/components/EmpireAdminPanel.tsx` + `app/api/admin/empire/route.ts` + منوی `app/admin/page.tsx` |
| تنظیماتِ زنده | بخش `empire` در `app/lib/reos/reos-config.ts` (کش sync `config()`) |
| فلگ روشن/خاموش/rollout | فلگ `empire` در `app/lib/reos/flags.ts` |
| تست‌ها | `scripts/reos-test.mjs` (یونیت) + `scripts/reos-store-test.mjs` (PG واقعی؛ اول `sudo service postgresql start`) |
| دودآزمای منوهای ادمین | `node scripts/admin-smoke.mjs` (روی سرور با JWT_SECRET) |

## ✅ فازهای انجام‌شده (کامیت‌ها)

| فاز | چی | کامیت |
|---|---|---|
| ۱ | تولد کامل (۵ سؤال، Dream Board، هویت، هدیه، ۴ فرصت واقعی، خرید+امضا، تصمیم معنادار، M1-M3، شکارچی، تحلیل ژتونی) | `c7650a1` |
| ۲ | نامهٔ روزانه (daily_brief طبق طرح سند) + استریک + حذف «نورا» → ملک‌جت | `7df3a5c` |
| ۳ | چرخهٔ عمر (فروش/زمین ۳مسیره/کسب‌وکار تجاری با ٪واقعی/درآمد اجاره از میانهٔ هم‌محله) + EmpireScore + ۵ لیدربرد + لیگ محله + صندوقچه + EmpireCard در ۴ پنل | `eece840` |
| ۴ | شروع سینمایی + ۶ مسیر شخصیت + سطح بی‌پایان (Rookie→Empire) + هزینهٔ مالکیت + بازارِ داغ در نامه | `86d9292` |
| ۵ | **منوی مستقل ادمین**: فرماندهی/بازیکنان(Ghost+هدیه+حذف)/اقتصاد/مأموریت/دنیا(Sync Monitor+نفوذ)/LiveOps/دسترسی | `c99dc1f` |
| باگ | ورودی حدس (رقم فارسی→صفر→۱۰۰٪ خطا) + جداکننده + کف قیمت معتبر | `24501e8` |
| ۶ | بانک: CreditScore ۰-۱۰۰۰ (باندهای سند)، وام با سقف/نرخ اعتباری، بهرهٔ روزشمار، دیرکرد ×۱.۵، netWorth−بدهی | `f786df0` |
| ۷ | مذاکرهٔ قطعی، کوئست روزانه/هفتگی شخصی، مالیات→خزانه، پیام بازگشت، «N دقیقهٔ امروز»، نردبان رؤیا، گذرنامه+اشتراک، دعوت شراکتی دوطرفه، فعال‌سازی mentorInitiates | `73203cd` |
| ۸ | سود/زیانِ دیروز (اسنپ‌شات)، ۵ مأموریت مخفی، کلکسیون، هدیهٔ بازگشت، 🔗 «آگهیِ واقعی» (Game→Lead)، تزریق رفتار بازی به دیتالیک REOS | `00fe9ef` |
| ۹ | **بازار سرمایه (جلد ۴۰)**: صندوق شاخصی REIT (واحد = «یک مترِ مجازی» به میانهٔ متریِ واقعی + سود دوره‌ای از اجارهٔ واقعی + رتبهٔ AAA-BB قطعی + کارمزد→خزانه)، مشارکت جمعی = مالکیت کسری آگهی‌های واقعی گران (ظرفیت اتمیک FOR UPDATE)، بازار ثانویه = بازخرید به NAV، شاخص‌ها، ترس/طمع از رویداد واقعی، پرتفوی+تنوع، netWorth شامل واحدها، کنسول سرمایهٔ ادمین (منوی «بازار سرمایه») | `b51e3c9` |
| ۱۰ | **تعامل/تاریخ/روایت (جلد ۴۹+۵۱+۵۲)**: `empire-engage.ts` — ردِ فعالیتِ واقعی از کلیدهای تاریخ‌دار claims → کنسول ادمین «تعامل و بازگشت» (DAU/WAU/MAU، Retention کوهورتی D1/D7/D30، نمودار ۱۴روزه، نرخ کوئست/صندوقچه، لیست ریسکِ ریزشِ باارزش‌ها)؛ Mastery ۶محوره از رفتار واقعی (چیپ‌های ★ در داشبورد)؛ «📰 روزنامهٔ ملک‌جت» = خبرِ تولیدشده از اتفاقِ واقعی (محلهٔ داغ، رکورد متری هفته، تولدها، سوددهی‌ترین فروش) + آرشیوِ رکوردهای تمدن | `1db290e` |
| ۱۱ | **تجربه و حرکت (جلد ۵۶ + ۵۴)**: لایهٔ Delight در /empire — ورودِ پلکانی، میکرواینترکشنِ همهٔ دکمه‌ها، شمارشِ متحرکِ اعدادِ ثروت (CountUp)، جشنِ موفقیت (Burst در صندوقچه/پاداش/خرید/فروشِ سودده)، احترام به prefers-reduced-motion؛ **🌆 خطِ آسمانِ امپراتوری** = قانونِ ۳ («تجربه به‌جای داده») — هر دارایی یک برجِ شب با ارتفاعِ ارزشِ روزِ واقعی؛ توضیح‌پذیریِ AI (جلد ۵۴): چیپ‌های «چرا این پیشنهاد» با سیگنال‌های واقعی (شهر/استقبال/سهم از سرمایه/پروفایل) | commit «Empire فاز ۱۱» در git log |

| ۱۲ | **شرکتِ ساختمانی (جلد ۶۱+۶۲+۶۳)**: ثبت شرکت (نام/برند/نوع، هزینه→خزانه، نشان CEO)؛ **اعتبارِ ستاره‌ای** ⭐۱-۵ فقط از رفتار واقعی (فروش سودده/مذاکره/خوش‌حسابی/پروانه/دیرکرد منفی)؛ استخدام مهندس (۳ نامزدِ هفتگیِ قطعی از هش، حقوق روزشمار → wagesPaid، مهارتِ تیم = مذاکرهٔ قوی‌تر + پروانهٔ سریع‌تر)؛ **شخصیتِ مالکِ زمین** در مذاکره (قطعی از هشِ آگهی: محافظه‌کار/منصف/قیمت‌بالا با اثرِ مشخص)؛ **پروانهٔ ساخت**: درخواست روی زمینِ برنامهٔ «ساخت» → عوارض٪ارزشِ واقعی→خزانه → بررسیِ چندروزهٔ قطعی → اعتراضِ احتمالی (توافق با غرامت یا دفاع با روزِ اضافه) → صدور + نشان First Permit | commit «Empire فاز ۱۲» در git log |

| ۱۳ | **موتورِ ساخت (جلد ۶۴–۷۲، Engine-محور)**: کلنگ روی زمینِ پروانه‌دار با نقشهٔ سازه/کیفیت (ضرایب شفاف: فلزی سریع‌تر/گران‌تر، لوکس qualityFactor بالاتر)؛ بنا = زمین×تراکم (knob)؛ **پیشرفت = روزهای پرداخت‌شده** — بی‌پولی = توقفِ کارگاه (جلد ۷۱ Cash Flow Crisis)؛ رویدادهای قطعی از هش در ایستگاه‌های ۳۰٪/۷۰٪ (پرداخت یا صبر — تا تصمیم، کار می‌ایستد)؛ **پیش‌فروش** از ۳۰٪ با سقف و قیمتِ میانهٔ متریِ واقعیِ محله منهای تخفیفِ شفاف؛ تکمیل → First Tower + تحویلِ پیش‌فروش‌ها در realized + projectsDelivered (خوراکِ اعتبارِ ⭐ شرکت)؛ **فروشِ واحد** به قیمتِ کاملِ واقعیِ لحظه؛ اتمامِ همهٔ واحدها = تحویلِ کاملِ پروژه (Project Delivered) و بسته‌شدنِ اولین Loop «زمین→ساخت→فروش»؛ netWorth پروژهٔ در ساخت = بهای تمام‌شده×کسرِ نفروخته | commit «Empire فاز ۱۳» در git log |

| ۱۴ | **Master Index (جلد ۷۳–۷۸)**: `docs/EMPIRE-ENGINE-MAP.md` — نقشهٔ فریزشدهٔ ۱۲ موتور ↔ کدِ واقعی + ۵ سطحِ Loop + ۸ Pillar + دروازهٔ Feature + Feature List (GDD فشرده) + وابستگی L0→L5؛ «📒 گزارشِ مالیِ شرکت» در داشبورد (جلد ۷۴ Reports) از شمارنده‌های واقعی (realized/اجاره/پیش‌فروش/ساخت/حقوق/خزانه/بدهی) | commit «Empire فاز ۱۴» در git log |

| ۱۵ | **گیم‌پلی تصمیم‌محور (GDD جدید فصل ۴ بخش‌های ۱–۸)**: (۱) **هدفِ پروژه** سرِ کلنگ (⚡ فروشِ سریع / 💰 حداکثرِ سود / ⭐ اعتبارِ برند) — اثرِ شفاف روی قیمت (٪ میانهٔ واقعی، knob) و سقفِ پیش‌فروش؛ هدفِ اعتبار → `stats.repProjects` → فاکتورِ جدیدِ اعتبارِ ⭐؛ (۲) **امکاناتِ میان‌ساخت** (استخر/روف‌گاردن/باشگاه/پارکینگ): هزینه = ٪ کلِ پروژه → بهای تمام‌شده، ارزش = ضریبِ شفافِ قیمتِ واحد در فروش/پیش‌فروش/اجاره (هر دو knob)؛ (۳) **«بفروش یا نگه‌دار و اجاره بده»** (rentUnits/stopRent): درآمدِ ماهانهٔ واحدهای تکمیل‌شده از میانهٔ اجارهٔ واقعیِ هم‌محله × کیفیت × امکانات؛ واحدِ اجاره‌ای تا فسخ مانعِ تحویلِ کامل؛ (۴) **اشباعِ فروشِ عمده** (bulkPriceOf): بیش از N واحد در یک سفارش → هر واحدِ اضافه ٪ ارزان‌تر (کف ۸۰٪) — «بازار به رفتارِ بازیکن واکنش نشان می‌دهد»؛ (۵) **کارنامهٔ پروژه** (projectHist + projectReportOf/projectLessonsOf): تحلیلِ پس از تحویل از اعدادِ واقعیِ خودِ پروژه (سود/بازده، تأخیر نسبت به days0، پیش‌فروش، رویدادها، استراتژی) — کارتِ «🎓 کارنامهٔ پروژه‌ها» در /empire؛ (۶) **کارتِ استخدام با اثرِ عددی** (engineerEffectsOf): مذاکره +٪، هزینهٔ رویدادِ کارگاه −٪ (مهارت≥۵۰ — اثرِ واقعی در progressBuild)، پروانه −روز؛ کنسولِ اقتصاد: کارتِ «🎯 گیم‌پلی پروژه» با همهٔ knobها (هدف‌ها/عمده/رویداد/امکانات) | commit «Empire فاز ۱۵» در git log |

| ۱۶ | **Hook + حافظهٔ مذاکره + اعتبارِ اثرگذار (سند ۱۴ — GDD فصل ۴ بخش‌های ۹–۱۶ + پیوتِ «مدل B»)**: (۱) **🔥 فرصت‌های طلاییِ امروز** — Hook ورود: N آگهیِ «واقعیِ» قیمت‌دار، انتخابِ قطعی از هشِ کاربر+روز (dailyDealPickOf)، شمارشِ معکوسِ واقعی تا مرزِ روزِ بازی؛ کارت قضاوت نمی‌کند (بعضی واقعاً زیرِ میانهٔ محله‌اند، بعضی نه) — مذاکره/تحلیلِ ژتونی/خرید روی همان کارت؛ knobهای deals.enabled/count؛ (۲) **حافظهٔ مذاکره** (بخش ۹ «هر شخصیت حافظه دارد»): stats.negoTries (هر آگهی یک بار — کلیدِ claims)، negoMemoryOf: نرخِ بردِ واقعی پایین → مالک‌ها محتاط‌تر (−۵)؛ خوش‌معامله → اعتمادِ سریع‌تر (+۳)؛ یادداشتِ حافظه در UI؛ (۳) **اعتبارِ برند اثرِ واقعی دارد** (بخش ۱۵): هر ⭐ بالای ۱ → شانسِ مذاکره +٪knob و نرخِ وام −٪knob (کفِ نصفِ نرخِ باند، repCutPct شفاف در UI)؛ (۴) **یکسان‌سازیِ فرمولِ مذاکره** (negoSkillOf مشترک بین «مذاکره» و «خرید» — رفعِ ناهمخوانیِ قدیمی: خرید قبلاً شخصیتِ مالک/تیم را حساب نمی‌کرد) | commit «Empire فاز ۱۶» در git log |

| ۱۷ | **Progression و اقتصاد (سند ۱۵ — فصل ۴ بخش‌های ۱۷–۲۰ + فصل ۵)**: (۱) **سطح‌گشاییِ قابلیت‌ها** («امکانات باید بیشتر شوند، نه اعداد»): بازارِ سرمایه از سطح ۳، ثبتِ شرکت از سطح ۴، مشارکتِ جمعی از سطح ۶ (همه knob در `unlocks`)؛ فقط ورودی‌های جدید قفل‌اند — دارایی/فروش/بازخرید هرگز؛ قفل‌ها در UI شفاف‌اند («از سطحِ X باز می‌شود»)؛ (۲) **ظرفیتِ پروژهٔ همزمان** (فصل ۵ «منابع»: ظرفیت هم دارایی است): maxProjectsOf = پایه + سطح ÷ گام (هر دو knob) — کلنگِ بیش از ظرفیت رد می‌شود، چیپِ «ظرفیتِ ساختِ همزمان X/Y» در نقشهٔ دارایی‌ها؛ (۳) **فروشِ پروژهٔ نیمه‌کاره** (فصل ۵ «پروژهٔ در حالِ ساخت هم دارایی است»): sellProject به ٪knob از بهای تمام‌شده (زمین+پرداختی‌ها) منهای مالیات→خزانه؛ با پیش‌فروشِ فعال ممنوع (تعهدِ تحویل)؛ زیان/سود در realized؛ دکمهٔ «🏳» در کارگاهِ زنده؛ کارتِ ادمین «📈 سطح‌گشایی و ظرفیت» | commit «Empire فاز ۱۷» در git log |

| ۱۸ | **Progression تکمیلی (سند ۱۶ — فصل ۶ بخش‌های ۱–۱۰)**: (۱) **پاداشِ Level Up**: هر سطحِ جدید (از XPِ واقعی) × knob `levelUpCoins` ملک‌کوین + نقطهٔ تایم‌لاین؛ اولین اجرا فقط سطح را ثبت می‌کند (بدونِ پاداشِ گذشته‌نگرِ یکجا)؛ (۲) **لیدربوردِ «📅 رشدِ این هفته»** (قانونِ صریحِ سند: دوره‌ای کنارِ دائمی تا بازیکنِ تازه هم شانس داشته باشد): weekSnap هفتگی از netWorth واقعی در اولین بازدیدِ هفته — دلتا از نقطهٔ ورودِ خودِ بازیکن؛ (۳) **Title فعال** (بخش ۹): انتخابِ عنوان فقط از نشان‌های واقعاً کسب‌شده (کلیک روی نشان)، نمایش در سربرگ و ردیف‌های لیدربورد | commit «Empire فاز ۱۸» در git log |

| ۱۹ | **اجتماعیِ الان-واقعی (سند ۱۷ — فصل ۷ بخش‌های ۱–۱۰)**: (۱) **پروفایلِ عمومیِ امپراتوری** («بازدید از شهرِ دیگران» — بخش ۹): کلیک روی هر ردیفِ لیدربورد → viewEmpire: نام/عنوان/سطح/نشان‌ها/امتیاز/ارزشِ خالص (از قیمت‌های زندهٔ واقعی)/محله‌ها/خطِ آسمانِ کوچک/شرکتِ ⭐/عضویت؛ (۲) **👏 تحسین (kudos)**: هر بازیکنِ واقعی یک‌بار برای هر امپراتوری (کلیدِ claims روی تحسین‌کننده)، شمارنده + نقطهٔ تایم‌لاین برای گیرنده، **بدونِ هیچ پاداشِ پولی** (ضدِ P2W/سوءاستفاده)؛ چیپِ 👏 در سربرگِ داشبورد | commit «Empire فاز ۱۹» در git log |

| ۲۰ | **LiveOps (سند ۱۸ — فصل ۸ بخش‌های ۱–۱۰)**: (۱) **🎪 استودیوی رویداد v1** («هیچ رویدادی نباید نیاز به آپدیت داشته باشد»): ادمین در کنسولِ LiveOps رویدادِ زمان‌دار می‌سازد (عنوان/آیکن/متریک از رفتارِ واقعیِ REOS: بازدید/ذخیره/جستجو/محله‌ها/هدف/پاداشِ کوین+XP/مدت/روشن‌وخاموش — ذخیره در `empire.events` کانفیگ) — همان لحظه کارتِ رویداد با نوارِ پیشرفتِ «واقعی» و شمارشِ معکوس در داشبوردِ همهٔ بازیکنان ظاهر می‌شود؛ claim از همان مسیرِ امنِ claim با بازسنجیِ سمتِ سرور (کلیدِ ev_id)؛ (۲) **پاداشِ نقاطِ عطفِ استریک** (بخش ۱): کوین در روزهای ۷/۱۴/۲۱/۳۰ِ ورودِ پیاپیِ واقعی (knob در LiveOps)؛ کلیدِ claim شاملِ روزِ شروعِ دوره است تا با شکستنِ زنجیره دوباره قابلِ‌دریافت شود | commit «Empire فاز ۲۰» در git log |

| ۲۱ | **UI/UX (سند ۱۹ — فصل ۹ بخش‌های ۱–۱۰)**: (۱) **کارتِ «💼 پرتفوی»** (تغییرِ نامِ صریحِ سند از Inventory): ارزشِ کلِ پرتفوی از قیمت‌های روزِ واقعی همیشه بالای کارت؛ فیلترِ نوع (زمین/آپارتمان/تجاری/ویلا) + فیلترِ **«⚠️ مشکل‌دار»** (اتفاقِ کارگاهِ معطل، اعتراضِ پروانهٔ حل‌نشده، کارگاهِ ایستاده از بی‌پولی)؛ مرتب‌سازی (جدیدترین/باارزش‌ترین/بیشترین رشد)؛ (۲) **قانونِ ۴ سؤالِ هر صفحه** (Part 05) در ENGINE-MAP ثبت شد؛ (۳) نگاشتِ ۱۰ صفحهٔ سند به پیاده‌سازیِ موجود در تراکر | commit «Empire فاز ۲۱» در git log |

| ۲۲ | **Art Direction الان-قابل‌اجرا (سند ۲۰ — فصل ۱۰ بخش‌های ۱–۱۰)**: (۱) **استپرِ بصریِ ۷ مرحلهٔ ساخت** در کارگاهِ زنده (Part 03 «هر مرحله از ظاهر قابلِ تشخیص») — از پیشرفتِ واقعیِ paidDays؛ (۲) **برجِ در حالِ ساخت در خطِ آسمان** متمایز: کم‌نور + قابِ نقطه‌چین + 🏗؛ (۳) **قانونِ «بدونِ انیمیشنِ اجباریِ طولانی» (سقف ۱.۵ث)** اعمال شد: توالیِ خرید ۳.۶ث→۱.۴ث، مکثِ نمایشیِ پیشنهاد ۳ث→۱.۲ث؛ (۴) معناشناسیِ رنگ (سبز=سود، قرمز=ضرر/هشدار، نارنجی=فرصت، طلایی=ویژه) به‌عنوان قانونِ توکن در ENGINE-MAP | commit «Empire فاز ۲۲» در git log |

| ۲۳ | **Visual Pass — پوستهٔ واقعیِ بازی (به خواستِ صریحِ امین)**: (۱) **🎮 منوی بازی** ثابت در پایینِ /empire با ۵ صفحه: 🏙 شهر / 💼 پرتفوی / 🎯 مأموریت‌ها / 📊 بازار / 🏆 رتبه‌ها — پایانِ «همه‌چیز در یک اسکرول»؛ (۲) **HUD چسبان** (سربرگِ سطح/کوین/ژتون/استریک/👏 همیشه دیدرس — فصل ۹)؛ (۳) **🗺 نقشهٔ شهر** در صفحهٔ شهر (NeshanMap شب): پینِ دارایی‌های بازیکن (مختصات از متای آگهیِ واقعی — liveInfoOf) + پینِ 🔥 فرصت‌های امروز؛ کلیک → آگهیِ واقعی؛ (۴) تب‌بارِ عمومیِ سایت داخلِ بازی پنهان (بازی منوی خودش را دارد)؛ راستی‌آزمایی با مرورگرِ واقعی: هر ۵ تب اسکرین‌شات، بدونِ خطای JS | commit «Empire فاز ۲۳» در git log |

| ممیزی | **ممیزیِ کاملِ پایانِ جلسه (به دستورِ امین)**: (۱) دو نقضِ قانونِ برندینگ در /empire رفع شد («یک بازیِ تازه…» → «فصلِ تازه‌ای از امپراتوری…» + tooltip «بازیکنان» → «امپراتورها»)؛ (۲) پوششِ knobها راستی‌آزمایی شد: هر ۸۷ کلیدِ empire در کانفیگ فیلدِ زندهٔ ادمین دارد — بدونِ شکاف؛ (۳) نقضِ قانونِ digitsOf در کلِ EmpireAdminPanel رفع شد: همهٔ ۸ نقطهٔ parse عددی (cin/setC، capIn/setCap، پروانه، امکانات، تنظیمِ بازیکن، کارمزدِ صندوق، rolloutPct، EventComposer) حالا رقمِ فارسی/عربی و «٫» و جداکنندهٔ هزارگان را می‌پذیرند (deFa/numOf) | commit «ممیزی پایان جلسه» در git log |

| ۲۴ | **بازیِ واقعاً قابل‌بازی (گزارشِ مستقیم امین: نقشه کار نمی‌کند + ساخت‌وساز دست‌نیافتنی + «گیم دکوری است»)**: (۱) **ریشهٔ خرابیِ نقشه**: URL قدیمیِ SDK نشان (`1.4.0/…/dist`) روی CDN مرده بود — همه‌جا بی‌صدا به fallback استاتیک می‌افتاد و در /empire (بی‌fallback) رو شد؛ NeshanMap حالا مسیرِ رسمیِ مستندات (`v1.9.4/neshan-sdk/v1.0.8`) را با نامزدِ پشتیبان امتحان می‌کند؛ (۲) **🏞 بازارِ زمین** (اکشن `lands` + کارتِ تبِ شهر): زمین‌های واقعیِ قیمت‌دارِ متراژدار، اول در حدِ سرمایه بعد قفل‌دارِ صادقانه — دروازهٔ موتورِ ساخت که تا امروز فقط از شانسِ فرصت‌های روزانه ممکن بود؛ خرید = buy + برنامهٔ «ساخت» یکجا؛ (۳) **🧭 مسیرِ برجِ اول**: استپرِ ۴قدمی از وضعیتِ واقعیِ بازیکن (زمین→پروانه→کلنگ→تحویل) با CTA به قدمِ بعدی — بعد از تحویلِ برجِ اول محو می‌شود؛ (۴) **نوارِ ادمینِ «مشاهدهٔ پنل» (z:9999) منوی بازی را می‌پوشاند و پرتفوی/شرکت عملاً غیرقابل‌کلیک بود** → در /empire بالای منو می‌نشیند؛ راستی‌آزمایی: حلقهٔ کامل با مرورگر و APIِ واقعی بازی شد (خرید زمین → برنامه → پروانهٔ صادرشده → نقشهٔ ۹گزینه‌ای → کلنگ → کارگاهِ ۲۱روزهٔ ۴واحدی) — بدونِ خطای JS | commit «Empire فاز ۲۴» در git log |

| ۲۵ | **تجمیع و تخریب (دستورِ صریح امین: «۶ واحدی؟ تک‌تک بخر؛ تا همه را نخریدی تخریب نه»)**: (۱) **🧩 تجمیعِ واحدها** — کلِ واحدهای ساختمانِ آپارتمانی/تجاری اول از متای واقعیِ آگهی («طبقه: X از Y» → Y)، وگرنه قطعی از هش (unitsMin..unitsMax — knob)؛ هر واحدِ بعدی به قیمتِ روزِ واقعیِ همان آگهی + ٪ پرمیومِ تجمیع (knob، شفاف در UI)؛ **مالکِ هر واحد شخصیتِ قطعیِ خودش را دارد** (کلیدِ `listingId#uN` در ownerPersonaOf/negotiationOutcome) و مذاکرهٔ per-unit؛ پول: قیمت از سرمایه، مالیات→خزانه، بهای تمام‌شده جمع می‌شود؛ ارزش/فروش/netWorth × واحدهای مالکیت‌شده؛ (۲) **🧨 تخریب** — آپارتمان/تجاری فقط با مالکیتِ کاملِ همهٔ واحدها؛ ویلایی مستقیم؛ هزینه = ٪ ارزشِ روز (knob) → `demolitionPaid` (مصرفِ شفاف)؛ دارایی → زمین با `landAreaOverride` (بنا×واحدها ÷ تراکم؛ ویلایی = متراژِ آگهی) → سه مسیرِ زمین → پروانه → کلنگ (buildPlan/startBuild با override)؛ ارزشِ تخریب‌شده تا ساخت = بهای تمام‌شده؛ نشانِ «First Demolition»؛ (۳) کارتِ ادمین «🧩 تجمیع و تخریب» (۵ knob) + اکشنِ **rename** ادمین (ویرایشِ نامِ امپراتوریِ هر بازیکن با ثبتِ ممیزی — کنارِ تزریقِ منابع/حذف/Ghost که از فاز ۵ بود)؛ راستی‌آزمایی: حلقهٔ کامل با API+مرورگرِ واقعی (خرید ۱ از ۳ → مذاکره با «مالکِ واحدِ ۲» → خرید تا ۳/۳ → تخریب → زمینِ ۸۲متری → پروانه → کلنگ) — بدونِ خطای JS | commit «Empire فاز ۲۵» در git log |

| ۲۶ | **نقشهٔ شهرِ کامل (گزارش امین: «زوم می‌کنی برمی‌گردد» + «ملکِ خریده باید پینِ متفاوت داشته باشد» + فصل ۹ City Screen)**: (۱) **ریشهٔ پریدنِ زوم**: صفحهٔ بازی هر ثانیه رندر می‌شود (شمارشِ معکوس) → آرایهٔ points هویتِ تازه → هر ثانیه بازسازیِ مارکر + fitBounds؛ حالا NeshanMap امضای دادهٔ پین‌ها را نگه می‌دارد (بدونِ تغییرِ واقعی هیچ کاری نمی‌کند) و بعد از اولین زوم/جابه‌جاییِ خودِ کاربر دیگر **هرگز** auto-fit نمی‌کند (تفکیکِ حرکتِ برنامه‌ای از کاربر با پرچم)؛ (۲) **پین‌های متمایز** (divIcon مشترک در NeshanMap — icon/color per point): 🏛 طلایی = دارایی خودِ بازیکن، 🏗 = کارگاهِ در حالِ ساخت، 🔥 نارنجی = فرصتِ امروز، 🏞 سبز = زمینِ بازار؛ (۳) **لایه‌های قابل‌تغییر** با چیپ‌های شمارش‌دار (دارایی/فرصت/زمین) + راهنمای پایینِ نقشه + ارتفاع ۳۸۰؛ (۴) کلیکِ پینِ دارایی → صفحهٔ پرتفوی؛ فرصت/زمین → آگهیِ واقعی؛ زمین‌های بازار هم روی نقشه آمدند | commit «Empire فاز ۲۶» در git log |

| ۲۷ | **زمان‌خری (دستور امین: «هر چیزِ زمان‌بری پاداشِ کم‌شدنِ زمان داشته باشد» — دقیقاً قانون ۵ سند: پرداخت فقط برای سرعت)**: ممیزیِ مکانیک‌های زمان‌دار → دو شتاب‌دهندهٔ کوین‌سوز: (۱) **⚡ پیگیریِ حضوریِ پروانه** — هر کوینِ knob یک روز از بررسی کم می‌کند (اعتراض/عوارض سرِ جایشان — انتظار قابلِ‌خرید است، نتیجه نه)؛ (۲) **⚡ شیفتِ شبانهٔ کارگاه** — هر روزِ جلوافتاده = کوین + هزینهٔ تومانیِ همان روز از سرمایه (پول غیب نمی‌شود) و **از چک‌پوینت‌های ۳۰٪/۷۰٪ فرار نمی‌کند** (منطقِ رویداد/تکمیل به توابعِ مشترکِ fireCheckpointEvent/completeIfBuilt استخراج شد و boost از همان می‌گذرد)؛ کارتِ ادمین «⚡ سرعت و زمان» (enabled + کوین/روزِ هر دو)؛ نرخ‌ها شفاف در UI (state.speed)؛ + **ممیزیِ هاردکدها**: شانس/بازهٔ تخفیفِ مذاکره (۲۵٪ پایه، ۲..۶٪) که کاربر می‌دید knob شد (`nego.baseChancePct/discountMin/discountMax` + کارتِ ادمین «🤝 مذاکره») — پیش‌فرض‌ها رفتارِ قبلی را عیناً حفظ می‌کنند؛ ⏳ ضرایبِ باندِ اعتباریِ وام (۰٫۷۵/۰٫۹/۱٫۴) و سقف‌ها (۱٫۲/۱/۰٫۷/۰٫۳) هنوز ثابتِ مستندند — تریگر: درخواستِ تنظیمِ باندها | commit «Empire فاز ۲۷» در git log |

| ۲۸ | **فروشگاهِ ملک‌کوین + نجاتِ کاشی‌های نقشه (گزارش امین: نقشهٔ خاکستری + «جای خرید سکه نیست»)**: (۱) **🪙 فروشگاهِ کوین (زرین‌پال)** — صفِ #۴ تراکر بسته شد: بسته‌ها کاملاً از ادمین (کارتِ «🪙 فروشگاه»: نام/کوین/قیمت/فعال + افزودن/حذف)، کارتِ خرید در تبِ «بازار» با یادآوریِ صریحِ بدونِ P2W (کوین فقط سرعت/تحلیل)، `/api/empire/coins` (POST ساختِ پرداخت + GET بازگشتِ درگاه: verify → `creditCoinPurchase` با **ایدمپوتنسیِ authority** تا رفرش دوبار شارژ نکند + تایم‌لاین + audit)، config-gated (بدونِ مرچنت → پیامِ صادقانه؛ مرچنت از «اتصال‌ها → زرین‌پال» که از قبل بود)؛ (۲) **نگهبانِ کاشیِ نقشه**: نقشهٔ خاکستری = کلیدِ نشان بدونِ مجوزِ «نقشهٔ پویا (Web SDK)» — حالا NeshanMap اگر بعد از ۶ ثانیه کاشی‌ای لود نشود یک‌بار با استایلِ پایهٔ «neshan» دوباره می‌سازد (شاید استایلِ شب در پلنِ کلید نباشد) و اگر باز نشد راهنمای دقیقِ فعال‌سازیِ سرویسِ کلید را روی نقشه می‌گذارد؛ راستی‌آزمایی: POST بدونِ مرچنت پیامِ درست، GET لغو → redirect ‎/empire?coins=fail، بسته‌ها در state، ۳ تستِ PG ایدمپوتنسی | commit «Empire فاز ۲۸» در git log |

| ۲۹ | **نقش‌های سایت در سناریو + قوانینِ واقعیِ ساخت (دستور امین: «تعداد واحد/طبقه با قوانین واقعی + بتونه خلاف کنه و جریمهٔ ماده۱۰۰ بده + همهٔ نقش‌ها فعلاً سیستم بازی کند»)**: (۱) **📐 طراحیِ معمار پیش از پروانه** — بازیکن طبقات و واحد در طبقه را خودش انتخاب می‌کند؛ قوانینِ شفافِ knob: سطحِ اشغال (٪زمین)، تراکم → طبقاتِ مجاز، حداقل متراژِ واحد؛ طراحی designDays طول می‌کشد (⚡ قابلِ‌تسریع)، حق‌الزحمهٔ معمار ٪هزینهٔ ساخت → servicesPaid؛ پروانه بدونِ نقشه رد می‌شود و فقط طبقاتِ قانونی را می‌پوشاند؛ (۲) **⚖️ تخلف و ماده۱۰۰** — تا ۲ طبقهٔ مازاد «می‌شود» ساخت (تصمیمِ آگاهانه با جریمهٔ برآوردیِ اعلام‌شده)؛ واحدهای مازاد از کلنگ علامت می‌خورند: پیش‌فروش فقط روی قانونی‌ها، فروش تا حلِ پرونده بسته؛ تکمیل → کمیسیونِ ماده۱۰۰ با سه راه: 💰 جریمه (متراژ مازاد × costPerM × ضریبِ knob) → خزانه/شهرداری؛ 🧑‍⚖️ وکیل (یک‌بار، قطعی از هش، حق‌الوکاله ٪جریمه، بردن = ٪کاهش)؛ 🧨 تخریبِ طبقاتِ مازاد (ازدست‌دادنِ واحدها)؛ (۳) **🏛 نقش‌های حرفه‌ای، فعلاً سیستم** (proPersonaOf قطعی از هش): دفترخانه در هر خرید (حق‌الثبت)، مشاورِ املاک برای اجاره (کمیسیونِ ۲۵٪ یک ماهِ میانهٔ واقعیِ محله — بدونِ مشاور اجاره ممکن نیست) و فروش (٪کمیسیون)، کارشناسِ رسمی در وام (هزینه از مبلغ کسر)، پیمانکار در کلنگ، وکیل در ماده۱۰۰ — همه با کارمزدِ شفاف → `servicesPaid` و تایم‌لاین؛ واگذاری به متخصصانِ واقعیِ سایت با تریگر در ENGINE-MAP؛ (۴) **🛠 بازسازیِ واقعی** — ۳ گزینهٔ knob (آشپزخانه/نما/کامل): هزینهٔ ٪ارزشِ روز → بهای تمام‌شده، ارزش‌افزودهٔ +٪ با سقف؛ در netWorth/فروش لحاظ می‌شود؛ هر گزینه یک‌بار؛ (۵) ۴ کارتِ ادمینِ جدید (نقش‌ها/طراحی‌وتراکم/ماده۱۰۰/بازسازی) + فرمِ طراحی و کارتِ ماده۱۰۰ و ردیفِ بازسازی در پرتفوی + گامِ «معمار» در مسیرِ برجِ اول؛ ۶ یونیت + ۲۱ تستِ PG + e2e زندهٔ API (پروانه بدونِ نقشه رد، قرارداد، تسریع، صدور) | commit «Empire فاز ۲۹» در git log |

| ۳۰ | **hotfix نقشه + خطِ آسمانِ خوانا (گزارش امین: «نقشه رو همه‌جای سایت از کار انداختی»)**: تغییرِ ترتیبِ SDK در فاز ۲۴/۲۸ (v1.9.4 اول) نقشهٔ همهٔ صفحات را خاکستری کرد — v1.9.4 لود می‌شود اما کاشیِ نشان نمی‌آورد؛ **بازگشت به URLِ اثبات‌شدهٔ قدیمی به‌عنوانِ اصلی** (v1.9.4 فقط پشتیبان)، حذفِ بازسازیِ خودکارِ استایل (نگهبانِ تایل فقط «راهنما» شد)، حذفِ برچسبِ پیش‌فرضِ «Leaflet» (setPrefix('') — اعتبارِ نشان می‌ماند)؛ بهبودهای بی‌خطر ماندند (پایداریِ زوم، پینِ divIcon چون opt-in است)؛ **قانونِ ۱۱ ثبت شد: رفتارِ پیش‌فرضِ کامپوننت‌های مشترک قفل است**؛ + خطِ آسمان خوانا شد: ارزشِ روز بالای هر برج، آیکنِ نوع، نامِ محله زیرِ برج، اسکرولِ افقی | commit «فاز ۳۰» در git log |

| ۳۱-۳۲ | **کلیک‌های قابل‌اعتماد + سند ۲۱ (فصل ۱۱ Audio)**: فاز ۳۱ — ریشهٔ «ده بار باید بزنم»: رندرِ هرثانیهٔ کلِ صفحه (شمارشِ معکوس) → Countdown ایزوله شد؛ خطاها فقط بالای صفحه بودند → توستِ شناور + نشانگرِ «⏳ در حالِ انجام» + ErrorFence (کرشِ رندر = کارتِ قرمزِ پیامِ دقیق) + شنودِ window.error → `/api/client-log` (خطای مرورگرِ کاربر در لاگِ pm2 با برچسبِ [client-error])؛ فرمِ معمار دیگر «بی‌صدا قفل» نمی‌شود (دلیلِ دقیقِ عدد خارج از محدوده)؛ نقشهٔ امضاشده خودکفا شد (landArea داخلِ design — کلنگ به زنده‌بودنِ آگهی وابسته نیست). فاز ۳۲ — **بازخوردِ صوتیِ سنتزشده** (`empire-sound.ts`، WebAudio بدونِ فایل): success/error/reward/levelup/coin/build/warn — قوانینِ سند: <۱ث، مثبت/منفی متمایز، شروع <100ms، حجمِ صفر، بدونِ تداخل (۱۵۰ms فاصله)، هیچ خبری فقط صوتی نیست؛ کنترلِ 🔊 کاربر در HUD (خاموش/حجم + صدای تست، localStorage) + knob ادمین `sound.enabled` | commit «فاز ۳۱/۳۲» در git log |
| ۳۳ | **سند ۲۲ (فصل ۱۲ Monetization)**: 🎨 **فروشگاهِ ظاهری** (Part 03) — قاب/نشان با ملک‌کوین (`cosmetics.items` کاملاً از ادمین: آیکن/نام/نوع/قیمت/فعال)؛ «هیچ آیتمِ ظاهری روی اقتصاد/سرعت/قدرت اثر نمی‌گذارد» — فقط نمایش: قاب روی پرسونا و نشان کنارِ نام در HUD، لیدربورد و پروفایلِ عمومی (ارزشِ show-off — Part 03 🎯)؛ خرید دائمی، خودکار-فعال‌سازیِ اولین آیتم، در تایم‌لاین ثبت. 🎁 **موتورِ پیشنهادِ هوشمند** (Part 09 — Offer Engine): `offerOf` قطعی از رفتارِ واقعی (اولین شارژ/سطحِ ۵+ بدونِ قاب/برج‌ساز با پروژهٔ تحویلی/مذاکره‌گر با ۳ برد)، حداکثر ۱ در روز، بستن با یک لمس → `offerHist` تا `cooldownDays` پنهان، `minAgeDays` روزِ اولِ بازیکنِ تازه پیشنهاد نمی‌بیند، بدونِ پاپ‌آپ/تایمرِ ساختگی (موارد ممنوعِ سند ✓). 🪙 **بستهٔ زمان‌دار** (Part 07 Bundles): فیلدِ `until` روی هر پکِ فروشگاهِ کوین (`activeCoinPacks` — تا پایانِ روزِ until، تایمرِ واقعی نه نمایشی)؛ UI «⏳ فقط تا تاریخ»؛ callbackِ پرداختِ انجام‌شده حتی بعدِ انقضا شارژ می‌شود (forVerify). knobهای ادمین: کارتِ 🎨 (ویرایشگرِ آیتم‌ها) + کارتِ 🎁 (enabled/cooldownDays/minAgeDays) + ستونِ «تا تاریخ» در ویرایشگرِ پک‌ها | این commit |

### سند ۲۱ (فصل ۱۱ — Audio Design، بخش‌های ۱–۱۰)
✅ **فاز ۳۲ (قابل‌ساختِ الان):** بازخوردِ صوتی (Part 02/06) با سنتزِ WebAudio — بدونِ فایل، بدونِ حجم (Part 09 ✓)؛ تنظیماتِ کاربر: خاموش/روشن + حجم + تستِ فوری (Part 05 ✓)؛ دسترسی‌پذیری: همهٔ پیام‌ها متنی‌اند و هیچ اطلاعی فقط صوتی نیست (Part 08 ✓)؛ «هیچ صدایی فقط برای پرکردنِ سکوت نیست» (Part 10 ✓)؛ کلیدِ سراسریِ ادمین.
⏳ **باقی با تریگر:** موسیقیِ ده‌گانه/Dynamic Music (Part 01/07)، صداهای محیط و Spatial (Part 03)، گوینده/Voice چندزبانه (Part 04)، همگام‌سازیِ تنظیمات بینِ دستگاه‌ها (Cloud Save) → همه نیازمندِ اسِت‌های صوتیِ واقعی و کلاینتِ World/اپ (Visual Bible Pass 2 / نسخهٔ اپ) — سنتز جای موسیقی را نمی‌گیرد؛ فایلِ صوتی هم فعلاً نداریم و نمی‌سازیم.

### سند ۲۲ (فصل ۱۲ — Monetization، بخش‌های ۱–۱۰)
📜 **قوانینِ لازم‌الاجرا (Part 01/02/10 — با قوانینِ ۵/۶ تراکر هم‌راستا):** بدونِ Pay-to-Win؛ تمامِ محتوای اصلی بدونِ پرداخت؛ پرداخت فقط راحتی/شخصی‌سازی/زمان؛ Gold (=ملک‌کوین) هرگز Cash نامحدود/Reputation/Level/بردِ رقابتی نمی‌خرد؛ قیمت‌ها شفاف؛ هیچ خریدِ اجباری؛ اصلِ طلایی: «بازیکن باید بعد از خرید احساسِ رضایت کند».
✅ **فاز ۳۳ (قابل‌ساختِ الان):** فروشگاهِ ظاهری (Part 03 — قاب/نشان، صفر اثرِ اقتصادی، دیده‌شدن در لیدربورد/پروفایل)؛ Offer Engine (Part 09 — قطعی از رفتارِ واقعی، ۱/روز، قابلِ‌بستن، cooldown، بدونِ تایمرِ ساختگی)؛ بسته‌های زمان‌دارِ فروشگاهِ کوین (Part 07 — until واقعی از ادمین). Gold economy (Part 02) از فاز ۲۸ برقرار بود (زرین‌پال → ملک‌کوین → فقط سرعت/تحلیل/ظاهر).
⏳ **باقی با تریگر:** **Season Pass دوسطحی** (Part 04 — Free/Premium/Elite) → Season Engine/Quest Studio (صفِ #۲؛ بدونِ موتورِ فصل، Pass صادقانه نیست)؛ **VIP «حسابِ حرفه‌ای»** (Part 05 — Heatmap قیمت/پیش‌بینیِ بازار/هشدارِ فرصت/مقایسهٔ مناطق — «اطلاعاتِ بهتر، نه قدرت») → روی سیستمِ پلن‌های موجودِ سایت + فصلِ ۱۴ (Analytics)؛ **Rewarded Ads** (Part 06) → شبکهٔ تبلیغاتِ واقعی نداریم — هرگز تبلیغِ اجباری؛ **اسکینِ ساختمان/شهر و Event Store با Event Token** (Part 03/08) → Visual Bible Pass 2 + Season Engine؛ **بسته‌های مناسبتی (نوروز/یلدا) و Bundle شخصی‌سازی‌شدهٔ خودکار** (Part 07) → ادمین همین حالا پکِ زمان‌دار می‌سازد؛ نسخهٔ خودکارش با Offer Engine v2.

آخرین وضعیت تست: **۳۹۱ یونیت + ۴۱۸ PG = ۸۰۹ سبز.** دودآزمای ادمین: ۴۷ نما، همه سبز.

### سند ۱۲ (جلد۷۳–۷۸ + سه تصمیمِ معماریِ نهایی)
**تصمیم‌های لازم‌الاجرا:** ۱) لیستِ Engineها فریز — Engine جدید فقط با جای مشخص در Core Loop؛ بقیه Subsystemاند. ۲) Master Index جدا از Bible (→ ENGINE-MAP). ۳) ترتیبِ استودیویی: GDD کامل → Technical → Content → Visual؛ «تا Feature List کامل نشود Engine جدید نه».
✅ جلد۷۳ (Construction Engine spec) — پیاده‌شده در فاز ۱۳ و در ENGINE-MAP نگاشت شد (چندپروژه/State/Pause ✓؛ ⏳ وزنِ مرحله‌ای/Health مرکب). ✅ جلد۷۴ (Economy) — بقای پول از فاز ۱ + گزارشِ مالی این فاز (⏳ چنددفتری/ترازنامه). ✅ جلد۷۵ (Asset) — EmpireAsset+تاریخچهٔ تغییرناپذیر (⏳ تجهیزات/برند به‌عنوان دارایی). ✅ جلد۷۶ (Human) هسته — Engineer/ownerPersona (⏳ NPC کامل — همان قفل‌گشا). ✅ جلد۷۷ (Core Loop) و ✅ جلد۷۸ (Pillars) — در ENGINE-MAP به‌عنوان قانونِ اساسیِ طراحی ثبت و انطباقِ ساخته‌ها نگاشت شد.

### سند ۱۳ (GDD جدید — Chapter 04 Complete Feature List بخش‌های ۱–۸)
📌 **بازساختارِ سندها:** دیگر «جلد» نداریم — GDD فصل‌های ۰۱–۱۰، بعد TDD-001+. این سند = فصل ۴ (فهرستِ کاملِ قابلیت‌ها).
📜 **قانون‌های تازهٔ طراحی (در ENGINE-MAP هم ثبت شد):** بازیکن CEO است نه حسابدار/اپراتور؛ «اگر بازیکن از انجامِ کاری لذت نمی‌برد → AI انجامش دهد»؛ هر سیستم فقط ۳–۸ تصمیمِ مهم؛ «اگر یک قابلیت باعث نشود بازیکن تصمیمِ جالبی بگیرد، اصلاً واردِ GDD نمی‌شود»؛ بازار باید به رفتارِ بازیکن واکنش نشان دهد.
🚫 **ردشده به تصمیمِ خودِ سند (هرگز نسازیم):** قابلیت‌های ERPوار بخش‌های ۱–۲ (دفترِ کل، ترازنامه، مغایرتِ بانکی، انبارِ چندسطحی، حضورغیاب و…) — «برای بازی افتضاح است»؛ فقط نسخهٔ تصمیم‌محورشان معتبر است.
✅ **فاز ۱۵ ساخته شد:** هدفِ پروژه (بخش ۸) + امکاناتِ میان‌ساخت و «بفروش یا اجاره بده» (بخش ۴) + اشباعِ فروشِ عمده (بخش ۵ «بازارِ واکنش‌دار» نسخهٔ ۱) + تحلیلِ پس از پروژه (بخش ۴/۸) + کارتِ استخدام با اثرِ عددی (بخش ۳).
⏳ **باقی از این سند (با تریگر):** رقبای شخصیت‌دار و فرصت‌های زمانی بازار (بخش ۵) → NPC Civilization؛ تیم‌سازی سریع/حرفه‌ای/اقتصادی + فرهنگِ سازمانی + بحرانِ سه‌گزینه‌ای شرکت (بخش ۳) → وقتی چندپروژگی و تیمِ بزرگ رایج شد؛ DNA زمین/شایعه‌ها («شنیده می‌شود مترو…»)/زمین‌های داستانی (بخش ۶) → نیازمندِ لایهٔ خبر/شایعهٔ راستی‌آزمایی‌شده (Event Engine v2)؛ گسترش به شهرِ جدید/تغییرِ مسیرِ فعالیت (بخش ۷) → وقتی چند بازیکنِ سطح‌بالای واقعی داریم؛ بازارِ هدف (خانواده/لوکس/دانشجو…) و فازبندیِ سرمایه‌گذاری (بخش ۸) → نیازمندِ Customer Engine.

### سند ۱۴ (GDD فصل ۴ بخش‌های ۹–۱۶ + پیوتِ بزرگِ «مدل B»)
📌 **پیوتِ تعیین‌کننده (لازم‌الاجرا برای همهٔ فازهای بعد):** MelkJet **بازی** است نه شبیه‌ساز — «Tycoon + Strategy + Collection + Live Economy» (مدل B: مونوپولی‌گو/کلش‌وار، نه Football Manager). آزمونِ هر قابلیت: «آیا کاربر می‌گوید *فقط ۵ دقیقهٔ دیگر*؟» + هر قابلیت باید تصمیم/ریسک/فرصتِ جدید بسازد. ترتیبِ طراحی: **Hook → Daily Gameplay → Meta Progression → پشت‌صحنه**. واژه‌های ERP/مدیرعامل/حسابداری/فرآیندِ اداری ممنوع مگر مستقیماً لذت بسازند. Big Data محصولِ طبیعیِ رفتارِ بازیکن است، نه چیزی که حس شود.
✅ **فاز ۱۶ ساخته شد:** Hook فرصت‌های محدودِ روزانه (بخش ۱۰ + «۹ شب چه می‌بیند»)، حافظهٔ مذاکره (بخش ۹)، اعتبارِ اثرگذار روی مذاکره/بانک (بخش ۱۵ §۶). مذاکرهٔ چندگزینه‌ای (قبول/کمتر/اقساط/مشارکت/ترک) نسخهٔ فعلی: مذاکرهٔ قطعی + شخصیتِ مالک + حافظه — گزینه‌های اقساط/مشارکت با NPC/قرارداد می‌آیند.
⏳ **باقی از این سند (با تریگر):** رقبای شخصیت‌دار که پروژه می‌سازند و روی قیمت اثر می‌گذارند + رقابت بر سرِ زمین/مشتری/نیرو + M&A (بخش ۱۱) → NPC Civilization؛ بازاریابی/کمپین (بخش ۱۲) و مشتریِ شخصیت‌دار/رضایت/وفاداری (بخش ۱۳) → Customer Engine (بدونِ شبیه‌سازیِ تقاضا، اثرِ صادقانه ندارند)؛ R&D/مسیرِ توسعه/مزیتِ تخصصی (بخش ۱۴) → همان صفِ Prestige/Skill Tree؛ شراکت/سرمایه‌گذار (بخش ۱۶) → خودِ سند آن را «تلهٔ شبیه‌ساز» خواند — فقط نسخهٔ Gamified (مثلِ مشارکتِ جمعیِ موجود)؛ اعتبارِ محلی/تخصصی (بخش ۱۵ §۸/۹) → وقتی چندشهر فعال شد؛ مذاکره با دولت/طبقهٔ اضافه (بخش ۹ §۵) → Event Engine v2.

### سند ۱۵ (فصل ۴ بخش‌های ۱۷–۲۰ «بستنِ Core Gameplay» + فصل ۵ «قوانینِ اقتصاد»)
📌 **Core Gameplay رسماً بسته شد** — متا-لوپ: کشف → تصمیم → سود/زیان → رشد → فرصتِ جدید؛ ۴ حلقه (۳دقیقه‌ای/۲۰دقیقه‌ای/چندساعته/روزانه)؛ **۷ ستونِ نهایی: کشفِ فرصت، معامله، ریسک، رقابت، رشد، جمع‌آوریِ دارایی، رویدادهای زنده** — قابلیتی که به هیچ‌کدام کمک نکند وارد نمی‌شود. طراحی «Opportunity-محور، نه Task-محور». فصل ۵ فقط قانون است، نه عدد (اعداد → Game Balance بعداً).
✅ **فاز ۱۷ ساخته شد:** سطح‌گشاییِ قابلیت‌ها + ظرفیتِ پروژهٔ همزمان (بخش ۱۹ + فصل ۵ منابع) + فروشِ پروژهٔ نیمه‌کاره (فصل ۵ دارایی‌ها/درآمد #۱۰).
📗 **قوانینِ فصل ۵ که از قبل برقرارند:** بقای پول، نقدینگی>ثروت (بحرانِ نقدینگیِ کارگاه)، هزینهٔ فرصت، درآمدِ غیرتضمینی، ریسکِ واقعی، هزینه‌ها (مالیات/بهره/نگهداری/حقوق)، ۱۰ راهِ درآمد (فروش/اجاره/فلیپ/پیش‌فروش/مشارکت/رویداد/مأموریت/…)؛ **ارزها**: سازگار با سقفِ سند — قابلِ‌خرج فقط Cash (تومانِ شبیه‌سازی) و ملک‌کوین؛ XP و اعتبار غیرقابلِ‌خرید. Gold پرمیوم → با زرین‌پال (صف، بدونِ P2W).
⏳ **باقی با تریگر:** مزایده (فصل ۵ بخش ۸ — همان صفِ مناقصه/مزایده؛ رقابتِ زنده‌اش → NPC)؛ رویدادهای زندهٔ فصلی/جهانی و Community Goal (بخش ۱۸) → Season Engine/Quest Studio (صف #۲)؛ چالش‌های روزانهٔ رقابتی → وقتی چند بازیکنِ فعالِ همزمانِ واقعی داریم؛ شهرهای سطح‌گشا و «اعتبارِ محلی» → چندشهر؛ تورم/رکودِ سراسری → فقط از سیگنالِ واقعیِ بازار (نه شبیه‌سازی)؛ سهامِ شرکت‌ها (فصل ۵ بخش ۹ #۶) → NPC/چندبازیکنه.

### سند ۱۶ (فصل ۶ — Progression & Progress، بخش‌های ۱–۱۰)
📗 **ممیزی: بیشترِ فصل از قبل ساخته بود** — سطح/XP ✓، سطح‌گشاییِ قابلیت‌ها و ظرفیت ✓ (فاز ۱۷)، دستاوردها/مخفی‌ها ✓، کلکسیون ✓، مأموریتِ روزانه/هفتگی ✓، ۵ لیدربرد + لیگِ محله + پاسپورت ✓، شخصی‌سازیِ شرکت (نام/برند/رنگ) ✓.
✅ **فاز ۱۸ شکاف‌ها را بست:** پاداشِ Level Up (بخش ۱ #۸) + لیدربوردِ دوره‌ایِ «رشدِ این هفته» (بخش ۷) + Title فعال از نشان‌ها (بخش ۹).
⏳ **باقی با تریگر:** «جدولِ مرکزیِ مأموریت‌ها» (نکتهٔ خودِ سند در بخش ۶) = همان Quest Studio (صف #۲ — اولویتش بالا رفت)؛ Season Pass (بخش ۸، Free/Premium بدونِ P2W) → بعد از زرین‌پال + Season Engine؛ شهرهای سطح‌گشا و «برترین شهرِ فصل» → چندشهر؛ دفترِ مرکزی/هلدینگ (بخش ۲) → Arc 4؛ رتبهٔ دوستان → گرافِ اجتماعی؛ Endgame (بخش ۱۰: مگاپروژه/تصاحبِ شهر/رقابتِ هلدینگ‌ها) → NPC Civilization + چندبازیکنهٔ واقعی؛ آیتم‌های تزئینی/قاب → فروشگاهِ ظاهریِ زرین‌پال.

### سند ۱۷ (فصل ۷ — Multiplayer & Social، بخش‌های ۱–۱۰)
📌 **آزمونِ فصل (از خودِ سند):** «اگر این قابلیت حذف شود، آیا بازیکن دلیلی برای تعامل با دیگران از دست می‌دهد؟»
✅ **فاز ۱۹ (با بازیکن‌های واقعیِ موجود):** بازدید از امپراتوریِ دیگران از لیدربورد + 👏 تحسینِ یک‌باره. از قبل: دنبال‌کردن/کامنت/کالکشن (لایهٔ REOS Community)، دعوتِ شراکتیِ دوطرفه (Referral — بخش ۹ #۱۰)، رتبه‌بندی‌ها، رأی/رکوردهای روزنامه.
⏳ **باقی با تریگر (همه نیازمندِ جمعیتِ واقعی‌اند — جمعیتِ ساختگی ممنوع):** دوستان/رتبهٔ دوستان/هدیهٔ روزانه → گرافِ دوستی (وقتی چند بازیکنِ فعالِ همزمان)؛ چت (خصوصی/شهر/هلدینگ) → زیرساختِ پیامِ سایت هست، نسخهٔ درون‌بازی با جمعیت؛ هلدینگ/Guild + پروژه‌های گروهی + رقابتِ هلدینگ‌ها (بخش‌های ۳/۵) → NPC Civilization یا جمعیتِ واقعی؛ معاملهٔ بین‌بازیکنی و Marketplace درون‌بازی (بخش‌های ۴/۷ — با کارمزد و ضدِ انتقالِ ناعادلانه) → چندبازیکنهٔ واقعی (توجه: MarketplaceِNo «واقعی» همین سایتِ ملک‌جت است)؛ PvP/دوئل/Matchmaking (بخش ۶ — «تصمیمِ بهتر، نه پولِ بیشتر») → پایه‌اش با لیدربوردِ هفتگی آماده است، دوئل با گرافِ دوستی؛ رویدادهای اجتماعی/مناسبتی (بخش ۸) → Season Engine (صف #۲)؛ مرکزِ اعلان + Push (بخش ۱۰ — «اعلان فقط وقتی تصمیم/فرصتِ واقعی هست» — همین قانونِ mentorLine) → وقتی PWA/پوش فعال شد.

### سند ۱۸ (فصل ۸ — LiveOps & Events، بخش‌های ۱–۱۰)
📗 **از قبل داشتیم:** ورودِ روزانه (استریک + صندوقچهٔ متغیر + نامهٔ روزانه)، رویدادِ روزانهٔ واقعی (محلهٔ داغ/رکوردها/فرصت‌های طلایی با شمارشِ معکوس)، چالشِ روزانه/هفتگی (کوئست‌ها)، لیدربوردِ هفتگی، هدیهٔ بازگشت (Comeback)، رویدادِ غافلگیرِ کارگاه.
✅ **فاز ۲۰:** استودیوی رویدادِ بدونِ دیپلوی (بخش ۸ — هستهٔ Live Content Management بخش ۱۰) + پاداشِ نقاطِ عطفِ استریک (بخش ۱).
⏳ **باقی با تریگر:** تمِ هفتگیِ یکتا + رویدادهای فصلی/مناسبتی با تغییرِ ظاهر (بخش‌های ۳/۴) → Season Engine (استودیوی رویداد پایه‌اش شد؛ تمِ بصری → Visual Bible/Pass 2)؛ Limited-Time Offers و بسته‌ها (بخش ۵) → زرین‌پال (بدونِ P2W)؛ چالشِ ۳۰دقیقه‌ای/هلدینگ/جهانی (بخش ۶) → جمعیتِ واقعی؛ x2 XP هفتگی → knob آینده روی استودیو؛ Push Notification با سقفِ ۳/روز و ساعتِ استراحت (بخش ۹) → زیرساختِ PWA/پوش؛ آرشیو/آمارِ مشارکتِ رویدادها → نسخهٔ ۲ی استودیو.

### سند ۱۹ (فصل ۹ — UI/UX، بخش‌های ۱–۱۰)
📌 **قانونِ ۴ سؤالِ هر صفحه (لازم‌الاجرا، در ENGINE-MAP):** الان وضعیت چیست؟ چه فرصت/مشکلی هست؟ بهترین اقدامِ بعدی چیست؟ با کدام دکمه؟
📗 **نگاشتِ ۱۰ صفحه به وضعِ موجود:** Main Menu/«۲ کلیک تا گیم‌پلی» = /empire مستقیم ✓؛ HUD = سربرگِ فشرده (سطح/کوین/ژتون/استریک/👏) ✓؛ City Screen = خطِ آسمان + Hook + پیشنهاد (نسخهٔ نقشه‌محورِ «اپِ املاکِ زنده» → Art Direction/Visual Bible)؛ Property Details = صفحهٔ آگهی + تحلیلِ ژتونی + Twin ✓ (تاریخچهٔ قیمت → صفِ price_history)؛ Project Management = کارگاهِ زنده (نوار/هشدارِ بحران/تصمیمِ بالادست) ✓؛ Market = /search + بازارِ سرمایه ✓؛ Portfolio ✓ فاز ۲۱؛ Profile = پروفایلِ عمومی + پاسپورت + Title ✓؛ Shop → زرین‌پال (فقط ظاهری/سرعت، بدونِ P2W)؛ Settings کاملِ درون‌بازی (صدا/گرافیک/Cloud Save) → نسخهٔ اپ/PWA.
✅ **فاز ۲۱:** کارتِ پرتفوی (نامِ جدید + ارزشِ کل + فیلترِ نوع و «مشکل‌دار» + مرتب‌سازی).

### سند ۲۰ (فصل ۱۰ — Art Direction، بخش‌های ۱–۱۰)
✅ **فاز ۲۲ (قوانینِ الان-قابل‌اجرا):** استپرِ بصریِ مراحلِ ساخت + برجِ در حالِ ساختِ متمایز در خطِ آسمان + حذفِ انیمیشن‌های اجباریِ بلند (سقفِ ۱.۵ث) + ثبتِ معناشناسیِ رنگ‌ها (وضعِ موجودِ /empire از قبل منطبق بود: #7c6 سود، #e88 ضرر، #e7a14a فرصت/هشدار، طلایی ویژه) + احترام به reduced-motion از فاز ۱۱.
⏳ **بدنهٔ سنگینِ بصری → Visual Bible / Pass 2 (فقط با تأییدِ امین):** محیطِ زندهٔ شهر (شب/روز/آب‌وهوا/عابر/خودرو — Part 02)، طراحیِ کاراکترها (Part 04 — فعلاً پرسوناهای متنی/ایموجی)، VFX آب‌وهوا، دوربین/زوم/نقشهٔ شهریِ «اپِ املاکِ زنده» (Part 09 + نکتهٔ Part 03 فصل ۹)، ساختمان‌های Landmark. Design System کامل (پالت/فونت/فاصله/Radius/Shadow — پیشنهادِ Part 08) → همان Pass، پایه‌اش CSS varهای موجودِ سایت است.

## 📚 وضعیت جلدها — سند به سند

### سند ۱ (جلد۱ Vision + جلد۲ فصل۱–۵)
✅ کامل: قوانین طلایی، Core Loop، معرفی، مهمان، ۵ سؤال، Dream Board، هویت+DNA+Confidence، تولد+نام+نشان، هدیه با جملهٔ سند، تایم‌لاین، ۴ فرصت (یکی برجسته+«اگر جای تو بودم»)، متن‌های خرید+امضا، «تو مالک هستی»+پاداش‌ها، تصمیم معنادار، M1(از رویداد واقعی)/M2/M3، شکست آموزشی، تعلیق جلسه، Identity Evolution (bump ها)، AI Journal.
⏳ مانده: Legacy Page مستقل، Life Goals صریح (نردبان رؤیا پوشش جزئی می‌دهد)، پوش‌نوتیف صبحگاهی (نیاز به push infra)، رقابت با AI Builder.

### سند ۲ (فصل۶/۷/۷.۵ + جلد۲.۵/۲.۶ + جلد۳ فصل۱–۴)
✅: چهار نوع ارزش، Welcome Package §6.3، Property Hunter §6.4، Land System §6.7، Business §6.9 (٪ از رقابت واقعی دایرکتوری)، اقتصاد ضدتورمی (هزینهٔ مالکیت/مالیات)، District League، Referral شراکتی §7.4، Viral Moments §7.12، dedup/گراف/فید (زیرساخت REOS از قبل).
⏳: City War، Company System، Mentor Community، Social Feed اختصاصی بازی، FOMO «فرصت‌های هفتهٔ قبل ۸٪ رشد» (نیاز به تاریخچهٔ قیمت per-listing)، فروش آیتم ظاهری (نیاز به درگاه پرداخت زرین‌پال — pending پروژه).

### سند ۳ (GDD جلد۱–۸)
✅: Opening سینمایی+۶ شخصیت، مأموریت تولد (شامل مذاکرهٔ مرحلهٔ ۵)، کوئست روزانه/هفتگی قطعی، سطح بی‌پایان+مراحل، هزینهٔ مالکیت، Empire Passport، لیدربردها.
⏳ (بزرگ — منتظر سند تفصیلی/جمعیت): سیستم ۱۸گانهٔ کوئست کامل + **Quest Studio ادمین** (جلد۲)، Prestige/Skill Tree (جلد۳)، **NPC Universe** (جلد۴ — پیش‌نیازِ دولت و جنگ شرکتی)، مزایده/سهام پروژه/صندوق (جلد۵)، نقشهٔ گرافیکی جهان (جلد۶)، گیلد/PvP/تورنمنت (جلد۷)، Season/LiveOps Studio+تقویم (جلد۸).

### سند ۴ (جلد۹ ادمین + جلد۱۰ کسب‌وکار + جلد۱۱ روانشناسی)
✅: مرکز فرماندهی ۷بخشی (جلد۹ نسخهٔ ۱: World/Players/Economy/RealEstate-Sync/LiveOps/Analytics-lite)، ۸ موتور روانشناسی جلد۱۱ (مالکیت‌آیین، پیشرفت خرد، هویت، میراثِ تایم‌لاین، کنجکاوی صندوقچه/مخفی، وابستگی سالم).
⏳: NPC Studio، شبیه‌ساز تورم، Ghost «Time Machine»، جلد۱۰ عمیق (کارمند/برند/فرانچایز/State Machine کامل کسب‌وکار)، Psychology Studio (Friction/Emotion heatmap — نیاز به حجم کاربر).

### سند ۵ (جلد۱۶–۲۱)
✅ جلد۱۶: بانک/CreditScore/وام/مالیات کامل v1. جلد۱۸ جزئی (اعتبار/گراف اعتماد از REOS trust).
⏳: Crowdfunding/صندوق/بیمه/بازار ثانویه (جلد۱۶ بقیه)، شهر هوشمند (جلد۱۷ — پیش‌نیاز NPC/شهروند)، منتورشیپ/آکادمی/قرارداد (جلد۱۸)، اتصال B2B جلد۱۹ (بانک/بیمهٔ واقعی — نیاز به شریک واقعی)، NPC Civilization (جلد۲۰)، تاریخ زنده (جلد۲۱ — پایه‌اش تایم‌لاین/آرشیو هست).

### سند ۶ — این سند (جلد۲۴–۲۸)
✅ جلد۲۶: Daily Loop کامل (نامه+کوئست+صندوقچه+استریک+سود دیروز+مخفی+کلکسیون+Comeback+رؤیا+زمان امروز). جلد۲۸ هسته: پل Game→Lead (لینک آگهی واقعی) + تزریق رفتار به دیتالیک ML.
⏳: جلد۲۴ دولت/انتخابات/مناقصه و جلد۲۵ گیلد/M&A/لیگ شرکت‌ها → **پیش‌نیاز: NPC Civilization + جمعیتِ بازیکن** (ساختنش الان = صفحهٔ خالی/فیک = ممنوع). Battle Pass/تقویم LiveOps → منتظر Season Engine.

### سند ۷ — جدیدترین (جلد۳۳–۴۰: Engineering Bibles)
ماهیت: Blueprint معماریِ مقیاس ۵۰M (Event Bus/CQRS/Saga/Multi-Region/MLOS/ADE/قانون اساسی/بانکداری عمیق/بورس املاک/REIT) + قولِ بلوک ۱۰-۱۵ جلدی Experience Design.
وضعیت: **مرجعِ معماری آینده** — روی VPS تک‌سروریِ فعلی صادقانه قابل‌پیاده‌سازی نیست و خود سند هم آن را نقشهٔ فاز رشد می‌داند. آنچه از روحش الان رعایت شده: event-driven بودن REOS، dual-mode PG، فلگ‌ها/canary (rollout%)، AutoML/registry/drift، تست‌های واقعی. جلد۳۹ (بانکداری) و ۴۰ (REIT/بورس املاک) وقتی نسخهٔ تفصیلی گیم‌پلی‌شان آمد روی بانکِ موجود سوار می‌شوند.

### سند ۸ (جلد۴۰–۴۸ + ایدهٔ نقشهٔ جهانی/Block 8)
**دو قانون معماری جدید (از خودِ سند — از این به بعد لازم‌الاجرا):**
- هر موتور دو خروجی دارد: Simulation Output (دنیای زنده) + Gameplay Output (تجربهٔ بازیکن).
- «Every Entity Has a Location. Every Action Happens in a Workspace.» — هر موجودیت مختصات دارد (آگهی‌های واقعی دارند؛ موجودیت‌های جدید هم باید داشته باشند).

✅ **جلد۴۰ (بازار سرمایه) — فاز ۹، پیاده‌سازیِ کاملِ بخشِ real-data:** صندوق REIT شاخصی (فصل۸)، Crowdfunding کسری روی آگهی واقعی (فصل۷)، بازار ثانویه=NAV (فصل۱۱)، شاخص‌ها (فصل۱۲)، پرتفوی+تنوع (فصل۱۳)، سود دوره‌ای از اجارهٔ واقعی (فصل۱۵)، روان‌شناسی ترس/طمع از رویداد واقعی (فصل۱۶)، رتبهٔ AAA-BB قطعی (فصل۱۷)، خروج=بازخرید (فصل۱۸)، کنسول سرمایهٔ ادمین (فصل۱۹)، KPIها: MarketCap/AUM/حجم (فصل۲۰).
⏳ جلد۴۰ باقی: IPO/اوراق/سهام شرکت (فصل۶/۱۰) و صندوق زیرساخت (فصل۹) → پیش‌نیاز: شرکت‌ها/نهادها (NPC).
⏳ **جلد۴۱ (نهادها/M&A)، جلد۴۴ (نیروی کار)، جلد۴۵ (NPC زنده)** → همگی زیرمجموعهٔ NPC Civilization — ساختن الان = جمعیتِ ساختگی = ممنوع. جلد۴۵ خودش سندِ تفصیلیِ همان چیزی است که در صف #۱ منتظرش بودیم.
⏳ **جلد۴۲ (صنعت ساخت/WBS/CPM)** — ERP ساختِ واقعی؛ پایه‌اش پنل سازنده (builder-store با پروژه/واحد/مایلستون واقعی) موجود است؛ نسخهٔ گیم‌پلی منتظر NPC/پیمانکار.
⏳ **جلد۴۳ (زنجیرهٔ تأمین)** — نکتهٔ real-data: «شاخص قیمت مصالح» از بازار مصالحِ واقعی ملک‌جت قابل‌ساخت است (تریگر: وقتی فروشگاه‌های مصالح قیمتِ کافی ثبت کردند).
⏳ **جلد۴۶ (DNA شهرها)** — پروفایل شهر از دادهٔ واقعی (میانهٔ متری/تعداد آگهی/رشد) تا حدی در شاخصِ شهرهای فاز ۹ ساخته شد؛ هویت بصری → Block 8.
⏳ **جلد۴۷ (MelkGIS)** — سیستم عصبی جغرافیایی؛ زیرساخت موجود: geo-store، geo-heatmap REOS، نقشهٔ نشان. نسخهٔ کامل (Parcel/Layer/Timeline) پروژهٔ بزرگِ مستقل — منتظر سندِ Block 8 و تصمیمِ کاربر.
⏳ **جلد۴۸ (Workspace OS)** — فقط اعلام شده؛ محتوایش هنوز نیامده.
📌 **Block 8 (World Experience — نقشهٔ جهانی/3D/Simulation Mode/Time Machine)**: خودِ سند می‌گوید «جای آن الان نیست» و در Block مستقل ثبتش کرده — هر وقت جلدهایش آمد، Phase B شروع می‌شود.

### سند ۹ (جلد۴۸–۵۳)
**قانونِ سه‌گانهٔ جدیدِ سند (از جلد ۵۲):** هر اتفاقِ مهم سه خروجی دارد — Simulation (اثر روی موتور) + News (روایت) + Memory (ثبتِ همیشگی). وضعیتِ فعلی: Memory = لاگِ رویدادهای REOS + تایم‌لاین/claims (پاک نمی‌شوند)؛ News = روزنامهٔ فاز ۱۰؛ Simulation = رویدادمحوریِ REOS.

✅ **جلد۴۹ (Engagement/Retention) — فاز ۱۰ + قبلی‌ها:** کوئست روزانه/هفتگی، استریک، صندوقچه، نشان‌ها/مخفی، کلکسیون، لیگ‌ها، Comeback (فازهای ۲–۸) + حالا: کنسول Engagement ادمین (فصل ۱۹/۲۰: DAU/WAU/MAU/D1/D7/D30/churn) و Mastery (فصل ۵). ⏳ فصلی/Battle Pass/رویداد زنده → Season Engine (صف #۲)؛ Retention AI پیشنهاددهنده → فعلاً لیستِ churn + هدیهٔ دستی ادمین.
✅ **جلد۵۲ (روایت/Event Bus) — فاز ۱۰ نسخهٔ ۱:** روزنامهٔ ملک‌جت از رویدادهای واقعی (بدونِ اسکریپت). ⏳ روزنامهٔ per-city، شایعه، افکار عمومی، سنجشِ NPC → منتظر NPC.
✅ **جلد۵۱ (زمان/تاریخ) — جزئی:** Memory Layer (رویدادها/تایم‌لاین حذف‌نشدنی)، آرشیو رکوردها (فصل ۹)، ردِ فعالیت تاریخ‌دار. ⏳ تاریخچهٔ قیمتِ هر آگهی (نیاز به snapshot دوره‌ای — تریگر: تصمیم برای جدول price_history)، نقشهٔ تاریخی/نسل‌ها → NPC/Block 8.
⏳ **جلد۴۸ (Workspace OS):** روحش تا حدی هست (پنل‌ها + منوی مستقل + AI copilot در پنل‌ها)؛ نسخهٔ کامل (Command Palette، پنجره‌های شناور، Multi-Workspace، همکاری Realtime) = بازطراحی کامل UI → Block 8/Phase B با تأیید کاربر.
⏳ **جلد۵۰ (سازمان/گیلد چندبازیکنه):** «واحدِ بازی = سازمان» — نیازمندِ جمعیتِ همزمانِ بازیکنانِ واقعی؛ ساختنش الان = گیلدهای خالی. تریگر: چند ده بازیکنِ فعالِ واقعی. زیرساختِ آماده: referral دوطرفه، لیدربرد، trust.
⏳ **جلد۵۳ (دنیای خودگردان):** «The World Never Waits» — همین حالا جزئی برقرار است (هزینهٔ مالکیت/بهرهٔ وام/درآمد اجاره/سود صندوق در غیابِ کاربر جلو می‌روند + Comeback). نسخهٔ کامل (شرکت‌های خودمختار) → NPC.

### سند ۱۰ (جلد۵۳–۵۷ + سه بازنگری معماری)
**قوانینِ معماریِ رسمیِ سند (تا امروز ۴ قانون):** ۱) Every Entity Has a Location ۲) Everything Is Extendable ۳) Never Show Data If You Can Show Experience ۴) Every Object Must Be Inspectable. + اصلِ World-first (دنیا حولِ بازیکن نمی‌چرخد) + از جلد ۵۴ هر جلد لایهٔ Production Blueprint دارد.

✅ **جلد۵۶ (UX/Motion/Delight) — فاز ۱۱ نسخهٔ ۱:** motion پلکانی، میکرواینترکشن، CountUp پول، جشنِ موفقیت، خطِ آسمان (قانون ۳ روی دارایی‌ها). ⏳ صدا/هپتیک، دوربینِ سینمایی، Scene-محوری کامل، حالتِ 3D/World Mode → Block 8 (نیازمند موتور نقشه/3D و تصمیمِ کاربر).
✅ **جلد۵۴ (انسان در برابر AI) — روحش برقرار:** AI همه‌جا مشاور است نه تصمیم‌گیر (تحلیل/Twin/پیشنهاد با اطمینان٪)، تصمیمِ نهایی با کاربر؛ حالا توضیح‌پذیری (چیپ‌های چرایی) هم اضافه شد؛ مانیتورِ AI ادمین (ai-cost/مدل‌ها) از قبل. ⏳ تیمِ چند-AI شخصیت‌دار (AI CFO/Lawyer/…) → نیازمند تعریفِ agentهای جدید در AI Studio.
⏳ **جلد۵۳ (دنیای خودگردان کامل):** NPC-قفل؛ برقرارِ فعلی: اقتصادِ بازیکن در غیابش جلو می‌رود + cron نامه‌ها. «AI شرکت/دولت/شهر» → تمدن NPC.
⏳ **جلد۵۵ (Platform SDK/افزونه/Marketplace):** بازطراحیِ Core در مقیاسِ تیمی — روی این کدبیس تک‌مخزنه فعلاً واقع‌بینانه نیست؛ روحِ فعلی: flags، config زنده، منوهای ماژولار. تریگر: تیمِ چندنفره/تصمیمِ کاربر.
⏳ **جلد۵۷ (CityOS/ترافیک/زیرساخت/Digital Twin شهر):** نیازمند GIS کامل (جلد۴۷) + NPC + Block 8؛ «Inspectable» تا حدِ لینکِ آگهیِ واقعی و پروندهٔ بازیکن برقرار است.

### سند ۱۱ (جلد۵۸–۶۵ + قفلِ نقشه‌راه روی گیم‌پلی)
**قانونِ ۵ سند:** از جلد ۶۱ هر جلد باید به ۴ سؤال جواب دهد (بازیکن امروز چه می‌کند/چه مأموریتی جلو می‌رود/چه Unlock می‌شود/چطور اعتیادآورتر). **قولِ سند:** تا تحویلِ اولین برج، هیچ Feature خارج از چرخهٔ اولین پروژه.

⏳ **جلد۵۸ (معماری جهانی World→Country→…)**: درختِ geo فعلی استان→شهر→منطقه→محله است؛ لایهٔ کشور/ارز/قانونِ چندکشوری → تریگر: ورودِ Region دوم. ⏳ **جلد۵۹ (Enterprise/Government/Platform Economy)**: خودِ سند هم آن را به بعدِ نسخهٔ ۱ موکول کرد — فقط ثبت.
⏳ **جلد۶۰ (Workflow خودِ بازی)**: MelkJet برای پنل‌ها workflow-store دارد؛ نسخهٔ گیم‌پلی (Trigger/Condition/Action روی رویدادهای بازی + Builder بصری) → صف، بعد از چرخهٔ اولین پروژه.
✅ **جلد۶۱ — فاز ۱۲**: شرکت+برند+اعتبار ستاره‌ای+استخدام (دفتر/تجهیزات → با جلد ۶۴). ✅ **جلد۶۲ هسته**: زمین از آگهی‌های واقعی + شخصیتِ مالک + مذاکره + توضیحِ AI (رنگ‌بندی نقشه/رقیبِ زنده → NPC/نقشه). ✅ **جلد۶۳ هسته**: پروانه (درخواست/عوارض/اعتراض/کمیسیون/صدور). ⏳ ورودِ سه‌بعدی شهرداری/بازرسِ متحرک → Block 8.
✅ **جلد۶۴–۷۲ (هستهٔ Engine) — فاز ۱۳**: چرخهٔ کاملِ ساخت+نقدینگی+پیش‌فروش+فروش (بالا). ⏳ باقی از این جلدها: انتخابِ برند/پیمانکارِ چندگزینه‌ای برای MEP (جلد ۶۷ جزئیات)، هویتِ نما/سبکِ معماری با Preview (جلد ۶۸ — بخشی qualityFactor پوشش می‌دهد؛ Preview بصری → Visual Bible/Pass 2)، Project Health مرکب، گزارشِ شبانهٔ کارگاه در نامهٔ روزانه، Customer Engine (شخصیتِ خریدارِ واحد — مثل ownerPersona)، Auction/Tender Engine (مناقصه/مزایده/مشارکت به‌جای خریدِ مستقیم — «شروعِ هر پروژه خودش بازی است»).
📌 **تصمیمِ سند: از این به بعد جلدها فقط Engine یا Core Loop** — سناریو/محتوا بعداً (Scenario Packs). Visual/Art Bible به‌عنوان Pass 2 جداگانه.

## 🎯 صفِ بعدی (به ترتیب پیشنهادی)

1. **NPC Civilization (جلد۲۰/۴ + حالا ۴۱/۴۴/۴۵)** — قفل‌گشای دولت (۲۴)، جنگ شرکتی (۲۵)، شهر (۱۷)، نهادها (۴۱)، نیروی کار (۴۴). جلد۴۵ سندِ تفصیلیِ آن است؛ طرح: Digital Citizen با DNA قطعی + اقتصاد خودگردان روی آگهی‌های واقعی.
2. **Quest Studio + Season Engine (جلد۲/۸/۹)** — ادمین بدون کدنویسی مأموریت/رویداد بسازد؛ زیرمنوی جدید امپراتوری.
3. ~~Crowdfunding/صندوق~~ ✅ (فاز ۹). باقی از جلد۱۶: **بیمه** — روی بانک موجود.
4. **درگاه پرداخت زرین‌پال** → بعدش فروشگاه آیتم ظاهری + Empire Pass (بدون P2W).
5. Prestige + Skill Tree (جلد۳) وقتی بازیکنِ سطح‌بالا واقعی وجود داشت.
6. شاخص قیمت مصالح از بازار مصالح واقعی (جلد۴۳) — وقتی دادهٔ قیمتِ کافی جمع شد.

## 🔁 روالِ هر سند جدید (برای سشن‌های آینده)

1. سند را **کامل** بخوان (چند Read با offset).
2. همین فایل را به‌روز کن: جلد جدید + وضعیت + آنچه قابل‌ساختِ «واقعی» است.
3. فقط چیزهایی را بساز که با دادهٔ واقعی کار کنند؛ بقیه را با دلیل و Trigger در ⏳ ثبت کن.
4. تست یونیت+PG، tsc، build، کامیت (تریلر استاندارد)، push، و این فایل را هم در همان کامیت به‌روز کن.
